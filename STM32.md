# 		 单片机是什么

单片机，也称为微控制单元，他是一种集成的电路芯片，在这个小小芯片中，包含有中央处理器（CPU）、内存、计数器、USB、A/D转换、UART、PLC、MDA、LCD驱动电路等的硬件设备，形成了芯片级别的**微型计算器**。·

## 单片机的作用是什么？

智能设备之所以能够被称为智能，这是因为他们能够像生物一样对外界做出该有的反应，而像生物一样反应可以分为两个步骤：

1. 感知外界环境：智能设备通过特定集成的**传感器**来进行对外界的感知、通过将外界的声、光、温度等世间万物转换为**电信号**，并传递给控制中心
2. 反应：而控制中心再通过计算，并传递反应信号给功率器件来影响外部环境

通过这两个步骤就能使的智能设备实现适应、影响环境的智能化效果

而在上面的例子中，要除了传感器以及功率器件，最重要的还是**控制中心**，而控制中心，自然就是我们的微型计算机来担当，因此单片机的具体作用其实就相当于我们的大脑

<img src="E:\Markdown\markdown\STM32.assets\image-20250426212923150.png" alt="image-20250426212923150" style="zoom:67%;" />

# 		 STM32架构

**![1699322490229](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1699322490229.png)**



这里的32指的是32位的内存地址，也就是总共4G的内存大小，规定所有的ARM得到设置以及所有外设所占的内存都必须在这4G中，不可以超过内存大小范围

但是内存中存放的东西是不能随便放的

以下为STM32的内存映射

![1699323247734](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1699323247734.png)

# 时钟

LSI：L低速的、S时钟、I内部的，内部的低速时钟

LSE：L低速的、S时钟、E外部的，外部的低速时钟

HSI：H高速的、S时钟、I内部的，内部的高速时钟

HSE：H高速的、S时钟、E外部的，外部的高速时钟



# 芯片命名的各个含义

![在这里插入图片描述](https://img-blog.csdnimg.cn/direct/c93ebad415d941d6a9300d7bdfb9fd92.png)



# 寄存器



## 1.什么是储存器映射

给存储器分配地址的过程就叫**存储器映射**，也就是说，当我们需要往内存中存储数据，而我们需要从存储器中给这些数据分配地址来进行标识，那么这些就叫存储器映射，其映射的就是一个内存地址

 当给已经分配了地址的数据，再次分配一个地址就叫**重映射**



## 2.什么是寄存器映射

**寄存器**对芯片中的某一个具有**特殊功能的内存单元**，并且给这个**内存单元取一个别名**， 这个别名就是**寄存器映射**





# C语言对寄存器的封装

 [6-什么是寄存器（第2集）—C语言对寄存器的封装_哔哩哔哩_bilibili](https://www.bilibili.com/video/BV1Vt411X7PK?p=4&spm_id_from=pageDriver&vd_source=b6ec906023fa46a5d781f4c754d9bf34)

由于一个内存地址对应一个字节（Byte）,而每个对应的功能寄存器都有其偏移值，我们只需要使用结构体，同时给结构体对应的对象内存大小设置为对应的偏移值大小即可，这样我们就能通过将**基地址转换为指针**的形式去访问其他结构体中的对象的对应的地址，即可实现对寄存器的直接操作



**注：当使用结构体来构造寄存器映射时，属性顺序应该与数据手册中的顺序以及数据类型相同，否则无法定位到准确的寄存器地址，也就无法准确的打开对应寄存器的功能**



### 寄存器的封装方式

1. 首先查看原理图，根据原理图来查看引脚的连接情况，例如，我们需要实现按键点亮或关闭的功能，因此我们需要找到按键相关的电路引脚，如下图

![1699673807771](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1699673807771.png)

2. 根据上图得到对应的引脚编号过后，我们就去查找这些引脚所属的对应外设，由于我们需要的引脚号分别为`PA0`、`PC13`从下面的映射地址可知，他们的调用寄存器地址位于GPIOA和GPIOC这些外设中

![1699673954985](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1699673954985.png)

3. 随后我们可以根据上面的外设边界地址得到基本地址，往后的各种外设功能寄存器都将在这个边界地址的范围中，我们使用**结构体的方式进行建立起所有功能寄存器的内存映射**
4. 根据内存地址来设置对应的引脚以及功能



### 如何制作完整的源文件

1. 首先复制完整的已经配置好的工程文件`FWLIB-TEMPALTE`，并且重命名为指定的工程文件名
2. 在`User`文件夹中创建新的功能`.c和.h`文件
3. 然后我们打开keil工程，在工程中的`USER`文件夹中添加刚才新创建好的.c和.h文件，同时我们还需要在`configue target options`功能项指定`bsp_key.c`和`bsp_key.h`文件的编译地址

![1699674561833](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1699674561833.png)

![1699675092866](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1699675092866.png)

![1699675124023](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1699675124023.png)

4. 在对应的功能的`.c`文件中包含其对应的头文件，这种在.c文件中包含头文件.h的方式，是为了能够**声明就相当于函数的主体, 同时C的源文件导入自己的头文件是为了后面在实现函数的过程中，能够让编译器知道这些接口的定义，以便能够正确的生成目标代码**
5. `.h文件`中也需要使用初始化宏定义，这里宏的定义的作用就是防止对应的声明与函数定义被重复的编译导致错误的出现，并且这个.h文件中必须要事先在宏定义内包含进所有的寄存器映射文件`stm32f4xx.h`

![1699675002824](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1699675002824.png)









# GPIO功能图

![1699542390177](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1699542390177.png)

[8-使用寄存器点亮LED（第1集）—GPIO功能框图讲解_哔哩哔哩_bilibili](https://www.bilibili.com/video/BV1Vt411X7PK?p=8&spm_id_from=pageDriver&vd_source=b6ec906023fa46a5d781f4c754d9bf34)



## GPIO输出初始化顺序

1. 确定GPIO是输入、通用输出、复用功能还是模拟输入。（需要使用**模式寄存器：MODER**）
2. 如果输出还要确定是推挽输出还是开漏输出（**输出类型寄存器：OTYPER**）
3. 配置输出的速度（**输出速度寄存器：OSPEEDR**）
4. 输出的时候内部的上/下拉电阻要不要开启（**上拉/下拉寄存器：PUPDR**）
5. 具体要输出的内容（**置位复位寄存器：BSRR** 和**数据输出寄存器：ODR**）





# STM32固件库

我们在查看固件库的时候，应该先对其中的文件进行梳理，找出每个文件中对应的功能，并将这些功能进行分类，大多数都会分成一下的几个类别 

例如下面的统计方式：

![1699536237086](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1699536237086.png)

![1699536208093](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1699536208093.png)





# 使用固件库创建工程模板

![1699537283201](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1699537283201.png)



我们可以根据已经写好的工程来创建工程模板 

 并且工程文件中还有一个`keikill.bat`文件，该文件用于清除程序编译过程中产生的中间键，我们可以在向外发布或公布的时候切除掉所有的中间键文件



# 端口输入数据寄存器（GPIOx_IDR）









# 位带操作

**位操作**就是可以单独的对一个比特位读和写

 位带操作实际上就是像51单片机一样，直接控制某一个IO口幅值即可，简单点来说就是直接对引脚进行操作，而不需要访问其对应的寄存器

位带区里面的每一个位都可以通过位带别名区的地址来访问，位带区的一个位，对应位带别名区的4个字节

![1699708515262](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1699708515262.png)

例如：**我们想要给GPIO端口输出数据寄存器的6号引脚的值设置为0， 那么我们通过位带操作对应的寄存器的位带区地址，我们就可以直接给该GPIO端口寄存器的具体某个位的值进行操作**

  

当位转换为位带地址的时候，一个位将会膨胀为4个字节

## 位带操作的内存地址

![1699709123682](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1699709123682.png)

![1699709148669](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1699709148669.png)



## **一、什么是位带操作？**

使用过51单片机的小伙伴应该有过类似的操作，想要点亮一个LED灯只要一个P2^1 = 0；就可以实现了，非常方便，其实，所谓的位带操作就等价于51单片机的位操作，但是在STM32里面，内核的最小寻址单位是字节，一个字节里面有8bit，那么，STM32又是怎么实现位操作的？要搞清楚这个问题，就要先弄明白下面两个问题，什么是位带区、什么是位带别名区。

## **二、什么是位带区**

介绍位带区之前，我们先来看一下Cortex-M内核里面寻址空间是怎么分配的

这是一张Cortex-M内核寻址空间映射图，来自《Cortex M3和M4权威指南》



![img](https://pic4.zhimg.com/80/v2-3d97b9a388cd24df52bcc150803e9587_720w.webp)

说白了就是支持位带操作的区域就是位带区。我们上面已经说过了，内核的最小寻址单位是字节，那么怎么将寻址单位缩小到bit？要弄明白这个问题，那就要先弄明白什么是位带别名区了。



## **三、什么是位带别名区**

从位带别名区名字上理解，感觉它像是别人的替身，实际使用上它就是别人的替身（电影里出名的武打明星都有自己的替身，STM32这么出名，肯定也有自己的替身啦，危险的操作都让替身去做，自己躺着就行了），**位带别名区就是为位带区服务的，对位带别名区的操作最终都会反映在位带区上，我们操作位带别名区的时候就等价于在操作位带区地址**，那么位带别名区与位带区又是什么样的关联关系呢？

从上面映射图上可以看到，SRAM区里的0X22000000-0X23FFFFFF地址段和外设区里0X42000000-0X43FFFFFF地址段都是位带别名区，两个别名区空间大小都是32M。那么，这32M的**位带别名区**地址空间是怎么与1M的**位带区**地址空间对应起来的呢？其实，工程师们想出了一个很好的办法，地址映射，将1M字节里面的每一个bit映射到32M字节里面去，那么怎么映射呢？看到这里可能有些小伙伴就亿脸懵逼了，懵逼的话可以看一下下面的演算。



各种单位运算关系

1字节 = 8bit

1字 = 4字节

如果对这些单位没有什么概念，可以看下图



![img](https://pic4.zhimg.com/80/v2-ffadbbc6d541778b66e0c3697cd69117_720w.webp)



将**1bit**映射到**1个字空间**里（如下图）

**映射前的一个字节 = 映射后的8个字**



**![img](https://pic1.zhimg.com/80/v2-d6f3e125244f07a103164ce8dbbe8668_1440w.webp)**

好奇的小伙伴可能就要问了，为什么要将1bit空间要映射到一个字空间里去呢？我映射到1字节或者2字节的地址空间不行吗？我只能说，STM32是一个32位的机器，内核按字寻址的话寻址速度是最快的，所以别问这么多为什么，如果问了，答案就是为了速度。



## **四、怎么使用位带操作**

上面我们已经知道了**位带区就是支持位操作的地址段**，**位带别名区就是位带区的地址映射**，**操作位带别名区就等价于操作位带区**，并且我们知道了大致的映射过程，那么在STM32实际使用中又是怎么应用的呢？









# RCC

锁相环时钟的作用：将初始输入的时钟频率经过倍频到指定的大小过后，再作为系统时钟的输入  

RCC ：reset clock control 复位和时钟控制器

![1708998479741](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1708998479741.png)



## RCC的主要作用

设置系统时钟SYSCLK， 设置AHB分频因子（决定HCLK的频率等于多少，这里HCLK由AHB预分频器来决定频率的多少）、

HCLK提供给高速时钟总线AHB的时钟信号

设置APB2分频因子（决定PCLK2等于多少，这里的**PCLK2为外设时钟或APB2外设设备时钟**，由APB2预分频器输出得到）

PCLK提供给低速时钟总线APB时钟信号

设置APB1分频因子（决定PCLK1等于多少，这里的PCLK1同样为外设时钟或APB1外设设备时钟，由APB1预分频器输出得到）







## RCC配置

我们在配置RCC时通常都会选择库函数时钟系统设置函数，**SetSysClock()**。该函数的功能就是使用HSE高速外设时钟将输出的时钟频率设置为：

$$HCLK=SYSCLK=PLLCLK=168M$$，

 $$PCLK2=HCLK/2=84Mhz$$

$$PCLK1=HCLK/4=42Mhz$$

这里也就意味着我们的锁相环PLL输出的时钟频率应该为168Mhz



### HSE高速外部时钟信号

HSE是高速的外部时钟信号，可以由有源晶振或者无源晶振提供，频率从4-26MHZ不等，当
使用有源晶振时，时钟从 OSC_IN 引脚进入，OSC_OUT 引脚悬空，当选用无源晶振时，时钟从
OSC_IN 和 OSC_OUT 进入，并且要配谐振电容。

而霸天虎开发板中使用的是25Mhz的无源晶振，如果我们使用HSE或者HSE经过PLL备品过后的时钟作为系统时钟SYSCLK的话，当HSE晶振损坏，不仅HSE总线会关闭，PLL也会被关闭，此时高速的内部时钟信号HSI就会作为备用的系统时钟，知道HSE回复正常，$$HSI=16Mhz$$



### 锁相环PLL

PLL的主要作用就是对时钟进行倍频，然后将时钟信号输出到各个功能部件。PLL有两个，一个是主PLL，另一个是专用的PLLI2S（音频信号倍频器），它们均**由HSE或者HSI提供时钟输入信号**

主PLL有两路的时钟输出，第一个输出时钟PLLCLK用于系统时钟，F407芯片中最高使用的而频率为168Mhz，第二个输出用于USB_OTG_FS的时钟（48Mhz）、RNG和SDIO时钟（<=48Mhz)。专用的PLLI2S用于生成精准时钟，给I2S提供时钟。

其中HSE或者HSI经过PLL时钟输入分频因子M（2\~63）分频后，成为VCO的时钟输入，VCO的时钟必须在1\~2Mhz之间，我们选择HSE=25M作为PLL时钟输入，则M应该被设置为25，那么VCO的输入时钟就等于1M。（一般来说我们设置VCO的输入频率为1Mhz）

VCO输入时钟经过VCO倍频因子N倍频过后，成为VCO时钟输出，VCO时钟必须在192\~432Mhz之间，我们配置N为336，则VCO的输出时钟等于336Mhz。如果要将系统时钟超频，就得在VCO倍频系数N这里作手脚。一般来说

$$PLLCLK\_OUTMAX=VCOCLK\_OUTMAX/P\_MIN=432/2=216Mhz$$，即F407最高可以超频到216Mhz

VCO输出是中国过后有三个分频因子：PLLCLK分频因子p，USB OTG FS/RNG/SDIO 时钟分频因子Q，分频因子R（F407没有）。p可以取值2、4、6、8，我们将其配置为2，则将会得到PLLCLK=168Mhz，Q 可以取值 4~15，但是 USB OTGFS 必须使用 48M，Q=VCO 输出时钟 336/48=7。有关 PLL 的配置有一个专门的 RCC PLL 配置寄存器 RCC_PLLCFGR，具体描述看手册即可。



PLL的时钟配置具体可使用以下公式表示：

$$VCOCLK\_IN=PLLCLK\_IN/M=HSE/25=1M$$

$$VCOCLK\_OUT=VCOCLK\_IN*N=1M*336=336M$$

$$PLLCLK\_OUT=VCOCLK\_OUT/P=168M$$

$$USBCLK=VCOCLK\_OUT/Q=336/7=48M$$



### 系统时钟SYSCLK

系统时钟的来源可以是：**HSI**、**PLLCLK**、**HSE**，具体的由时钟配置寄存器**RCC_CFGR的SW位**配置。这里我们设置的系统时钟为PLL最后的输出频率168M，如果系统是由HSE经过PLL倍频之后的PLLCLK得到，那么当HSE出现故障的时候，系统时钟将会切换为HSI=16M，直到HSE恢复正常



### AHB总线时钟HCLK

系统时钟SYSCLK经过**AHB预分频器**分频过后得到的时钟叫**AHB总线时钟**，即HCLK，分频因子可以是:[1,2,4，8，16，64，128，256，512]，具体的由时钟配置寄存器**RCC_CFGR的HPRE位设置**。片上大部分外设的时钟都是经过HCLK分频得到的，至于AHB总线上的外设设置为多少就需要在使用外设的时候自行设置，我们在这里只需要设置好APB总线时钟即可。这里我们使用1分频，即HCLK=SYSCLK=168M



### APB2总线时钟PCLK2

APB2总线时钟PCLK2由HCLK经过**高速APB2预分频器**得到，分频因子可以是:[1,2,4，8，16]，
具体由时钟配置寄存器 **RCC_CFGR 的 PPRE2 位**设置。

PCLK2 属于高速的总线时钟，片上高速的外设就挂载到这条总线上，比如全部的 GPIO、USART1、SPI1 等。至于 APB2 总线上的外设的时钟设置为多少，得等到我们使用该外设的时候才设置，我们这里只需粗线条的设置好 APB2 的时钟即可。我们这里设置为 2 分频，即 $$PCLK2 = HCLK /2= 84M$$。



### APB1总线时钟PCLK1

APB1总线时钟PCLK1由HCLK经过**低速APB预分频器**得到，分频因子可以是:[1,2,4，8，16]，具体由时钟寄存器RCC_CFGR的PPRE1位设置。PCLK1属于低速的总线时钟，最高为42M，片上低俗的外设就挂载在这条总线上，比如 USART2/3/4/5、SPI2/3，I2C1/2 等。

至于 APB1 总线
上的外设的时钟设置为多少，得等到我们使用该外设的时候才设置，我们这里只需粗线条的设置
好 APB1 的时钟即可。我们这里设置为 4 分频，即 PCLK1= HCLK/4 = 42M。



**注意**：

![1709041491289](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709041491289.png)

这里APB预分频器过后的倍频器倍频过后的时钟信号供给给的是定时器设备，并且定时器时钟并不包含在APB总线中，因此，因此这里的APB预分频器用公式来解释应该为：

$LAST\_APB1\_OUT=AHB\_OUT/4=42M$

$LAST\_APB2\_OUT=AHB\_OUT/2=84M$

f







# 中断

## 中断编程的顺序

1. 使能中断请求
2. 配置中断优先级分组（只需配置一次即可）
3. 配置NVIC寄存器，初始化NVIC_InitTypeDef
4. 编写中断服务函数



`注意：中断优先级分组不仅针对片上外设，还针对内核，并没有说内核比片上外设的中断优先级要大，总的来说，还是需要使用中断优先级分组来及进行判断，并且内核中的中断优先级分组并不是使用NVIC的IPRx寄存器定义的，	内核的中断优先级分组是使用SCB的SHPRx的寄存器来设置的`



## 编写中断服务函数

### 1.中断服务函数要写在什么地方

首先为了方便我们的管理，我们在编写固件库的时候，我们需要将所有的中断服务函数都写到interrupt文件中，在stm32中将会缩写为**stm32Fxxx_it.c**

当然我们也可以写在别的C文件中



### 2.中断服务函数名的命名规则

中断服务函数的名字必须要与中断向量表中的中断名称一致，这是由于，在C语言中，函数的名字其实就是一个寄存器的地址，而向量表中存放的都是中断服务函数的名字，也就是说**向量表存的都是中断服务函数的地址**，如果我们的名字不一样，那么就无法正常的找到正确的中断服务寄存器地址，也就无法正常的触发相应的中断服务函数









# EXTI--外部中断/事件控制器

EXTI是一个内核中的一个外设，而它的名字也是外设，那么就是说它其实当其他的外设触发的一些中断或者是事件通过EXTI给除了它以外的其他外设设别来产生一个中断或是更新一个事件

并且**EXTI是通过发送一个方波**来给其他外设产生一个中断或是一个事件

![1700204718860](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700204718860.png)



其中

红线为中断电路

绿线为事件电路

#### 输入线

输入线是与其他外设相连的，所有的外设的中断信号或是事件都将通过输入线来传入到EXTI中



#### 边沿检测电路

边沿检测电路是用于检测电信号的变化的，这个电路能够实现触发功能是通过**上升沿触发寄存器**以及**下降沿触发寄存器**来控制的

同时由于我们的这两个寄存器的有效位为23个位，因此也就是说，这两个寄存器能够接收23种不同的信号，也就意味着有23条输入线来输入到EXTI控制器中

![1700205343604](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700205343604.png)



我们使用GPIO来作为我们的EXTI的输入，而这23根输入线种前16根为GPIO的端口输入线

![1700205700236](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700205700236.png)



而剩下的7根输入线则被用作了下图的外设

![1700205777590](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700205777590.png)

而当输入的电信号刚好是设置好的上升沿或者是下降沿的时候，边沿检测电路器将会发出一个高电平的信号，然后经过一个**或门**，或门同时连接**边沿检测电路器的输出**以及**软件中断事件寄存器的输出**，一个为高电平就返回高电平信号



#### 中断屏蔽寄存器、事件屏蔽寄存器

这两个寄存器用于设置是否继续传输中断信号，如果将这两个寄存器的具体的**输入为设置为1的时候，将会通过与门的操作方式来允许中断或事件信号的继续传输**，如果**设置为0，则不允许中断或事件信号的继续传输**



同时如果产生的是中断的话，那么相应的**挂起请求寄存器**就会**质疑**，表示要产生中断了，然后最后产生的中断信号将会输出到NVIC中断控制器中，然后NVIC在将这个中断传递到处理器中，让处理器来处理这个中断



#### 脉冲发生器

脉冲发生器接收的是事件信号，脉冲发生器产生1是有效的，有效过后，它将会产生一个方波来提供给片上的其他外设。让外设执行相应的操作



## 通过配置哪个寄存器来进行选择EXTI控制器的输入线

我们通过**SYSCFG**这个系统配置控制器来选择控制器的输入线，因此当我们需要使用到中断/事件操作的时候，应该在配置**EXTI的时钟**的同时，还要配置**SYSCFG系统配置控制器的时钟**，然后给对应的**SYSCFG_EXTICRx外设中断配置寄存器配置指定的位**，就可以操作下面的各个EXTI总线的某个GPIO口为控制器输入线

![1700207666731](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700207666731.png)

![1700207819493](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700207819493.png)

 



## 使用中断EXTI以及NVIC步骤

/* 上升沿触发选择寄存器（RTSR）选择0-22号端口的输入线是否接收上升沿触发
而这23条输入线具体有分别有16个外部GPIO映射，每个映射连接着GPIOA-GPIOI的x号引脚
剩下的7个映射分别

软件中断事件寄存器（EXTI_SWIER）用于在软件方面产生中断或者是事件信号

中断屏蔽寄存器（EXTI_IMR）用于接收或屏蔽x线的中断请求

挂起请求寄存器（EXTI_PR）用于指明是否要发生以及触发中断

外部中断配置寄存器（SYSCFG_EXTICRx）用于选择EXTIx外部中断的输入引脚线使用具体的哪个GPIOx映射中的第几个引脚      

1.使能GPIO       1
2.使能SYSCFG     1
3.使能EXTI       1
4.给EXTI初始化	 1
5.给EXTI配置输入信号线  1
6.选择接收上升沿或下降沿信号  1
7.开放来自对应线的中断请求    1
8.配置相应的挂起请求寄存器    1
9.配置NVIC，让其在接收到对应的中断信号后执行中断服务函数 */







# SysTick---系统定时器

系统定时器，24位，只能递减，存在于内核，嵌套在NVIC中，所有的Cortex-M单片机内核都具有这个定时器

![1700384045509](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700384045509.png)





## STK_LOAD--重装载寄存器

计数器使用重装载寄存器中保存一个24位的数，并且只能使用**递减计数器**递减



## STK_VAL--递减计数器

STK_VAL：当前数值寄存器

也就是说重装载寄存器的值是固定的，而VAL的值则就是重装载寄存器递减过后的当前值



## STK_CTRL

计数器时钟控制器，该寄存器只有4个位有效

![1700394494500](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700394494500.png)





## STK计数器的工作流程

STK计数器通过**系统时钟（AHB）**或是**系统时钟的8分频**来驱动，对**STK_VAL（递减计数器）**的值进行递减，而当STK_VAL的值被递减为0的时候，时钟控制器的COUNTFLAG为将会置为1，而当控制器的TICKINT置为1的时候，当递减计数器的值减为0的时候，将会触发中断服务函数



## STK计数器使用步骤

1.初始化LED相关的GPIO

2.配置SysTick（STK_CTRL寄存器）

3.编写中断服务函数

4.main函数使用STK计数器



## 重装载计时器设置值公式

![1700398841117](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700398841117.png)



这里是1ms产生一次中断，因为$1s  / 1ms = 1000次$ 所以我们要的值应该设置为1000/168(m/s)





# 通信

通信是指 如：芯片与电脑、芯片与芯片、电脑与电脑之间的数据交换，这个就叫**通信**



## 串行通信

![1700459318618](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700459318618.png)





## 并行通信

通信方式：

![1700459286858](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700459286858.png)



### 串行与并行的通讯之间的特性对比

![1700459533983](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700459533983.png)





## 全双工

从传输方向来看：

![1700459701962](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700459701962.png)





## 半双工

从传输方向来看：

![1700459718947](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700459718947.png)







## 单工

从传输方向来看：	

![1700459733353](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700459733353.png)







## 同步

一个时钟传输一个二进制位



## 异步





### 同步与异步的区别

同步与异步的不同之处在于，是否有时钟信号线，来同步数据





## 通信的速率



### 比特率（bitrate）

每秒钟传输的 **二进制位数**，单位为比特每秒（bit/s）



### 波特率（baudrate）

表示每秒钟传输的**码元个数**

码元：**一个二进制位表示一个码元**

但有时也会使用**两个二进制位表示一个码元**







# 串口

## 串口通信协议

## 物理层

物理层的通信协议是规定通讯系统中具有机械、电子功能部分的特性，确保原始数据在物理媒体的传输，其实就是硬件部分



## RS232标准串口通讯结构图

![1700748754269](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700748754269.png)

1. RS232标准串口主要是用于工业设备直接通信
2. 电平转换芯片一般有MAX3232，SP3232



![1700749726966](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700749726966.png)



### RS——232信号线

在最初的应用中，RS-232串口标准常用于计算机、路由和调制解调器（MODEN，俗称“猫”）之间的通讯，一般通讯的对象有数据终端设备DTE（计算机、路由）以及数据通讯设备DCE(调制解调器)

在旧的台式计算机中一般都会有RS-232标准的COM接口，见下图：

![1710036885580](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1710036885580.png)

其中接线口以针式引出的信号线称之为**公头**，以孔式引出的信号线称之为母头

![1710037000876](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1710037000876.png)

![1710037012385](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1710037012385.png)

上表为DB9数据线的各个引脚的含义，由于两个通讯设备之间的收发信号（RXD->TXD）应交叉连接，所以调制解调器DB9母头的收发信号一般与公头相反，两个设备相连，只要直接使用“直通型”的串口线连接起来即可，见下图

![1710037225175](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1710037225175.png)

串口中的RTS、CTS、DSR、DTR及DCD信号，使用逻辑1时表示信号有效，逻辑0表示数据无效，

例如，当计算机端控制DTR信号线表示为逻辑1，它告知远端的调制解调器，1则表示本设备已经准备好接收数据，0则表示本设备还没准备好。



## 协议层

协议层就是规定通讯的逻辑，统一收发双方的数据打包、解包标准。



简单点来说就是：物理层规定的是我们在说话的时候是使用嘴巴来表达还是肢体来表达，协议层则规定了我们是使用中文还是英文来进行表达

串口通讯的数据包由发送设备通过自身的TXD接口传输到接收设备的RXD接口。在串口通讯的协议层中，规定了数据包的内容，它由起始位、主题数据、校验位以及停止为组成，通讯双方的数据包格式要约定一致才能正常通讯

## 串口数据包结构

![700750439625](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700750439625.png)



### 波特率

本章主要时串口异步通讯，异步通讯中由于没有时钟信号，因此两个通讯设备之间需要约定好的波特率，及每个码元（码元就是一个二进制数）的长度，以便对信号进行解码，上图中用虚线分开的每一格就代表一个码元，常见的波特率为4800、9600、115200等

### 起始位

起始位：由**1个逻辑0的数据位**表示

### 结束位

结束位：由**0.5、1、1.5或2个逻辑1的数据位**表示



### 有效数据

有效数据：在起始位后紧接着的就是有效数据，有效数据的长度常被约定为5、6、7或8位长

（其中有效数据的常用长度为**8位**）



### 校验位

校验位是可选的，为的是数据的抗干扰性。

校验的方法分为：



#### 1. 奇校验（odd）

奇校验指的就是，有效数据中为**1的位数**为奇数（有多少个1）

例如：一个8位长的有效数据为：``01101001`， 此时总共有4个1，为偶数，而为了符合奇校验的规则，那么校验位为1



#### 2.偶校验（even）

有效数据中的**1的位数**为偶数（有效数据的1的个数为偶数），一般来说，最后传输的数据将是8位的有效数据加上1位的校验位，总共9位，当使用偶校验的时候，这9位数据中1的个数必须为偶数，如果有效数据的位数为奇数，那么校验位的值应该为1



#### 3. 零校验

0校验是指不管有效位中的内容的是什么，校验位的值总为“**0**”



#### 4. 一校验

1校验是指不管有效数据位中的数据是什么，校验位的值总是位“**1**”



#### 5. 无校验

无校验就是数据包中不包含校验位







# USART串口通信（通用同步异步收发器）

![1700751537350](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700751537350.png)



为了便于理解，我们讲这个USART的框图分为以下几个部分

![1700751588494](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700751588494.png)



## 引脚

**![1700751935360](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700751935360.png)**

TX：数据发送

RX：数据接收

SCLK：时钟，仅同步通信时使用

nRTS：请求发送

nCTS：允许发送





## 数据寄存器

USART串口的数据寄存器有两个，但具体编程的时候只能赋值到一个寄存器中，因为这个寄存器包含了数据**发送寄存器**以及**数据接收寄存器**

当然我们也可以将这个寄存器想做一个数据中转，在理论上数据寄存器包含了两个寄存器，但实际上我们可以将其想成是一个随操作来选择数据去向或来源的寄存器，当写入操作时将会把数据传输给TDR寄存器中，TDR寄存器将会使用内部总线和输出移位寄存器之间提供的并行接口进行传输数据



TDR和RDR都是介于系统总线和移位寄存器之间。串行通信是一个位一个位传输的，发送时把TDR内容转移到发送移位寄存器，然后把移位寄存器数据每一位发送出去，接收时把接收到的每一位顺序保存在移位寄存器内，然后在转移到RDR，最后来到数据寄存器

因此对于这个寄存器的数据接收以及数据发送的方式也会有所不同



并且USART数据寄存器（USART_DR）只有低9位有效，并且第9位数据是否有效还具体取决于USART_CR1控制寄存器1中**M位**设置，当M位为高电平时，表示9为数据字长，当M位为低电平时，表示8为数据字长





例如：

**USART_DR = 0xFE**，这里给寄存器赋值，可以理解为，**当给该寄存器赋值的时候，串口会自动的将寄存器里的值发送出去**

而当使用别的变量接收寄存器的值，如：**temp = USART_DR**，将USART_DR的值赋给别的变量，这时，我们可以理解为，U**SART_DR寄存器读取到外部的值，存储在寄存器中，而需要我们去读取它**

### 数据发送与接收

![1700889051308](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700889051308.png)



#### 数据发送步骤

1. 数据通过电信号传输给发送数据寄存器（TDR）

2. TDR中的数据传输给发送移位寄存器，此时，如果发送数据寄存器（TDR）的数据已经全部转移到发送移位寄存器的时候，发送数据寄存器将会产生一个标志位： **TXE**（Transmit data register empty），这个标志位的含义为：发送数据寄存器已经全都传输到了发送移位寄存器，

    并且如果想要在产生标志位的时候产生一个中断作为，那么就需要使用**TXEIE**来产生中断进行处理

3. **发送移位寄存器**会通过信号线直接将接收到的数据，一位一位的传输到TX发送数据线用于发送给别的设备或元器件

    当发送移位寄存器全都将数据使用TX传输引脚全部传输出去过后，将会产生一个**TC**的标志位，这个标志位表示，数据传输已经完成

    同样的，我们在传输完成并产生TC标志位的同时，还可以产生一个名为 **TCIE**的中断 

    `注意：这2个标志位都在USART_SR寄存器中，而另外两个中断则都在USART_CR寄存器中使用`



#### 接收数据

1. 接收数据首先通过**RX引脚**一位一位地传入接收移位寄存器中

2. **接收移位寄存器**将接收到的数据传输到**接收数据寄存器（RDR）**中，并且接收数据寄存器完全接收完所有数据过后，此时，接收数据寄存器将会产生一个标志位：**RXNE（接收数据寄存器非空）**，这个标志位在USART_SR寄存器进行使用

    同时接收数据寄存器还能通过**RXNEIE**来产生接收数据寄存器非空中断功能





## 控制器

USART有专门控制发送的发送器、控制接收的接收器，还有唤醒单元、中断控制等，当需要使用USART时需要给USART_CR1中的**UE位**设置为1来使能USART，同时数据帧的大小还需要USART_CR1中的M位来决定长度位8位还是9位。



### 发送器

当USART_CR1寄存器的发送使能位TE置1时，启动数据发送，数据移位寄存器的数据将在Tx引脚输出，如果是同步模式SCLK也将输出时钟信号。

一个数据帧分为三部分：**起始位+数据帧+结束位**，

起始位是一个周期的低电平（<u>位周期就是每一位的占用时间</u>）

数据帧就是我们要发送的8位或9位的数据，<u>数据是从最低位开始传输的</u>

停止位是一定时间周期的高电平，停止位时间长度是可以通过USART控制寄存器2（USART_CR2）的STOP[1:0]位控制的，可选0.5个、1个、1.5个和2个停止位。默认使用1个停止位。2个停止位适用于正常USART模式、单线模式或调制解调器模式

#### 数据帧的编辑

由于我们使用的是异步通信，为了能够提升数据传输的抗干扰性，我们需要将数据分为一个一个的帧数据，下图位发送字符的时序图![1700894678033](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700894678033.png)

例如上图，由于我们使用的是异步通信，因此我们将SCLK时钟线忽略掉，只看发送数据帧的结构

1. 启动位：告诉接收方现在要开始传输数据了，一般的启动位是由低电平来控制的
2. 数据帧：在低电平的启动位后面就是8-9位的数据帧，具体的长度可以通过**USART_CR1**这个控制寄存器的**M位**来控制

![1700894928077](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700894928077.png)

![1700894940420](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700894940420.png)



​	其中n个停止位也是可以控制的

3. 停止位：停止位可以是一个或多个，具体可以自己设置，通常是由**USART_CR2**寄存器的**STOP位**来控制其长度，并且停止位的电平一定是高电平

![1700895139630](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700895139630.png)

​	![1700895158983](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700895158983.png)

4. 奇偶校验位：同时成段的数据帧的停止位处还可能存在奇偶校验位，这个位的功能是用于提高数据帧的抗干扰能力，如果奇偶校验位校验数据出错，那么可能会出现重传或者是使用出错中断来处理错误

    如果需要使用奇偶校验位的话，那么就需要使用

    ​	**USART_CR1的PCE（奇偶校验使能）**：使用奇偶校验功能
    ​	**PS（奇偶校验选择）**：选择奇校验或者是偶校验

    ​	**PEIE（奇偶校验错误中断）**：是否使能中断，当**USART_SR寄存器中PE的值为1时，将生成USART中断**

    这几个寄存器中的位来完成对数据帧的设置

    同时如果还需要获取其对应的数据帧奇偶校验位的状态就需要使用

    **USART_SR的PE（奇偶校验错误）**位来获取其状态，当接收器模式下发生奇偶校验错误的时候，该位将由硬件置**1**， 

![1700895852964](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700895852964.png)



除了上面的数据帧外，当要使用USART串口进行传输数据的时候，还应该配置以下的位：

1. UE（USART enable）USART使能，他是让USART预分频器和输出开启或停止，只有当我们打开了USART串口的时候，我们才能开始传输或接收 	
2. TE（Transmitter enable）发送器使能，将USART串口设置为是否能够使用**发送功能**
3. RE（Receiver enable）接收器使能，将USART串口设置为能否使用**接收功能**



更具上面的发送字符时序图我们可以具体知道具体的操作流程：

1. 当发送使能位TE置1之后，发送器开始会先发送一个空闲帧（一个数据帧长度的高电平），接下来就可以往USART_DR寄存器写入要发送的数据，
2. 数据寄存器将TDR数据寄存器中的内容通过数据移位寄存器进行发送
3. 在写入最后一个数据后，需要等待USART状态寄存器（USART_SR）的TC位为1，表示数据传输完成，如果USART_CR1寄存器的TCIE为置1，将会产生中断

在发送数据时，具体会重点使用到以下的标志位：

![1710050080918](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1710050080918.png)



### 接收器

1. 将USART_CR1寄存器的RE位置1，使能USART接收，使得接收器在RX线开始搜索起始位。
2. 在确定好起始位后就根据RX线电平状态把数据存放在接收移位寄存器中。
3. 在接收完成后就把数据转移到数据寄存器RDR中，并将USART_SR寄存器的RXNE位置1，同时如果USART_CR2寄存器的RXNEIE置1的话可以产生中断。

在接收数据时，编程的时候有几个比较重要的标志位：

![1710050462054](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1710050462054.png)

同时，为了得到一个真实的信号，就需要使用一个比接收器的信号频率高的采样信号去检测，称为**过采样**，这个采样信号的频率大小决定最后得到源信号准确度，一般频率越高得到的准确度也越高，但得到越高频率的采样信号会越困难，运算和功耗等等都会增加，所以一般选择合适就好

接收器可配置为不同过采样技术，以实现从噪声中提取有效的数据。USART_CR1中的OVER8可选择不同的采样方法，<u>*如OVER8位设置为1即采用8倍过采样，即用8个采样信号采样一位数据*</u>，*<u>如果OVER8位设置位0采用16倍过采样，即用16个采样信号采样一位数据。</u>*



USART的其实位检测需要用到特定序列，如果在RX线识别到该特定序列就认为时检测到了起始位。起始位检测对使用16倍或8倍过采样的序列都是一样的。该特定序列为：1110x0x0x0000，其中的x为任意电平，1或0都可以

8倍过采样采样的速度更快，最高速度可达$f_{PCLK}/8$，$f_{PCLK}$为USART时钟，采样过程见下图：

![1710051626073](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1710051626073.png)

其中使用第4、5、6次脉冲的值来决定改为的电平情况



16倍过采样的采样速度没有8倍过采样那么快，但得到的数据准确度更高，其最大速度为$f_{PCLK}/16$，采样过程见下图：

![1710051741273](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1710051741273.png)

其使用第8、9、10次脉冲的值来决定该为的电平状态。



## 波特率速度

波特率——每秒要发送多少数据，波特率其实就是指**每秒传输多少个码元**，而**码元实际上就是一个二进制数据**，因此这个波特率其实也能看成，比特率（bit/s）

控制波特率的速度，其实就是控制串口的通讯速度  

USART的发送器和接收器使用相同的波特率。计算公式如下：

![1710053452882](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1710053452882.png)

其中$f_{PCLK}$为USART时钟；OVER8为USART_CR1寄存器的OVER8为对应的值， USARTDIV是一个存放在波特率寄存器USART_BRR的一个无符号定点数。其中<u>DIV_Mantissa[11:0]位定义位USARTDIV的整数部分</u>，<u>DIV_Fraction[3]位定义USARTDIV的小数部分</u>，DIV_Fraction[3]位只有在OVER8位为0时有效，否则必须清零。





波特率的速度具体使用**USART_BRR寄存器**来控制

![1700898129827](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700898129827.png)

其中DIV_Mantissa是速率的整数部分

​		DIV_Fraction是速率的小数部分

### 波特率的计算

![1700899275855](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700899275855.png)

$F_ck$是串口的时钟频率

USARTDIV：无符号定点数，这个数是设置在USART_BRR

OVER8：过采样值 

计算实例：

**![1700901092777](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1700901092777.png)**

例如，如果OVER8=0，DIV_Mantissa=24且DIV_Fraction=10，此时USART_BRR值位0x18A；那么USARTDIV的小数位10/16=0.625；整数位24，最终USARTDIV的值为24.625。

如果OVER8=0并且知道USARTDIV的值为27.68，那么DIV_Fraction=16*0.68=10.88，最接近的正整数为11，所以DIV_Fraction[3:0]为0xB；DIV_Mantissa=27.68取整数27，即为0x1B

如果OVER8=1情况类似，只是将OVER8中的权值换为8

波特率的常用值有2400、9600、19200、115200。下面为如何设定寄存器值来得到对应的波特率的值。

假设我们使用的是USART1/USART6，其使用的是APB2的时钟总线，最高可大84Mhz，其他的USART的最高频率为42Mhz，即$f_{PCLK}=84Mhz$。当我们使用16倍过采样时，即OVER8=0，为得到115200bps的波特率，此时：

公式为：$115200 = 84000000/(8*[2-0]*USARTDIV)$

解的USARTDIV=45.57，可算的DIV_Fraction=0x9($0.57*2^4=9.12$取整，在BRR寄存器中，表示小数为的有4位)， DIV_Mantissa=0x2D，即应该设置USART_BRR的值位0x2D9。



## 校验控制

USART支持奇偶校验控制，当使用校验位时，串口传输的长度将是8位的数据帧加上1位的校验位总共9位，此时USART\_CR1寄存器的M位需要设置为1，即9数据位。

将USART_CR1寄存器的PCE位置1就可以控制奇偶校验控制，奇偶校验有硬件自动完成，启动了奇偶校验控制之后，在发送数据帧是会自动添加校验位，接收数据时自动验证校验位。接收数据时如果出现奇偶校验位验证失败，会见USART_SR寄存器的PE位置1，并可以产生奇偶校验中断。

使能了奇偶校验控制后，每个字符帧的格式将变成：**起始位+数据帧+校验位+停止位**



如果是8位的数据包，那么8位的数据包将会包含1位的校验位，即有效数据并不会改变，只是在最后的校验位会多出一位，最终将会传输9位数据，

如果是9位的数据包，那么将还是8位的实际数据，1位的奇偶校验，

如果全不使用奇偶校验的话，实际数据的大小将会是数据包的大小。



## 中断控制

![1710055363184](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1710055363184.png)







## USART串口设置步骤

1. USART串口使能（USART_CR1	UE位）

2. TX、RX功能使能（USART_CR1   TE、RE位）

3. 帧的编辑

    1. 数据帧位数（USART_CR1	M位）

    2. 停止位（USART_CR2     STOP位）

    3. 奇偶校验位

        1. 奇偶校验使能（USART_CR1    PCE位）

        2. 奇偶选择（USART_CR1     PS位）

        3. 奇偶中断（USART_CR1     PEIE位）

            这个中断功能触发与否取决于USART_SR的PE位的值



## 初始化结构体

USART使用的初始化结构体为：USART_InitTypeDef，结构体成员设置外设工作参数，并由外设初始化配置函数，比如USART_Init()调用，这些设定参数将会设置外设相应的寄存器，达到配置外设工作环境的目的。



### USART初始化结构体

![1710058527404](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1710058527404.png)



#### USART_BaudRate

波特率设置。一般设置为2400、9600、19200、115200。标准库函数会根据设定值计算得到USARTDIV值，并设置USART_BRR寄存器值



#### USART_WordLength

数据帧字长，它设定USART_CR1寄存器的M位的值，如果没有使能奇偶校验控制，一般使用8位数据位，<u>如果使能了奇偶校验则一般设置位9数据位</u>



#### USART_StopBits

停止位设置，可选0.5个、1个、1.5个和2个停止位，它设定USART_CR2寄存器的STOP[1:0]的位，我们一般选择1个停止位



#### USART_Parity

奇偶校验控制选择，可选USART_Parity_No(无校验)、USART_Parity_Even(偶校验)以及USART_Parity_ODD(奇校验)，它设定USART_CR1寄存器PCE位和PS位的值

#### USART_Mode

USART模式选择，由USART_Mode_Rx和USART_Mode_Tx，允许使用逻辑或运算选择两个，它设定USART_CR1寄存器的RE位和TE位

#### USART_HardwareFlowContorl

硬件流控制选择，只有在硬件流控制模式才有效，可选有使能RTS、使能CTS、同时使能RTS和CTS、不使能硬件流



### USART时钟初始化结构体

当使用同步模式时需要配置SCLK引脚输出脉冲的属性，标准库使用一个时钟初始化结构体USART_ClockInitTypeDef来设置，因此该结构体内容也只有在同步模式才需要设置。

![1710059224627](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1710059224627.png)

![1710059232185](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1710059232185.png)

#### USART_Clock

同步模式下SCLK引脚上时钟输出使能控制，可选禁止时钟输出（USART_Clock_Disable）或开启时钟输出（USART_Clock_Enable）；如果使用同步模式发送，一般都需要开启时钟。它设定USART_CR2寄存器的CLKEN位的值

#### USART_CPOL

同步模式下SCLK引脚上输出时钟极性设置，可设置在空闲时SCLK引脚为低电平(USART_CPOL_Low)或高电平（USART_CPOL_High）。它设定USART_CR2寄存器的CPOL位的值

#### USART_CPHA

同步模式下SCLK引脚上输出时钟相位设置，可设置在使用第一个变化沿捕获数据（USART_CPHA_1Edge）或在时钟第二个变化沿捕获数据，它设定USART_CR2寄存器的CPHA位的值

USART_CPHA与USART_CPOL配合使用可以获得多种模式时钟关系

#### USART_LastBit

选择在发送最后一个数据位的时候时钟脉冲是否在SCLK引脚输出，可以时不输出脉冲（USART_LastBit_Disable）、输出脉冲（USART_LastBit_Enable）。它设定的是USART_CR2寄存器的LBCL位的值











# 复用功能

由于stm32f4xxx内部集成了许多的外设控制器，比如usart、spi、i2c等串口，这些外设控制器，也需要使用引脚来进行控制，“**复用**”就是**相对于单片机的引脚来说，既能作为GPIO的引脚使用，又能用作内部外设控制器来使用**

比如我们来看看STM32F103xx单片机的PA5引脚，如下图：

![img](https://pic4.zhimg.com/80/v2-f3e68f05c575de72486f76a9c7c63e6f_720w.webp)

首先，PA5可以做为普通GPIO来使用；其次，如果作为外设的引脚，它可以作为SPI1的时钟（SPI1_SCK）、DAC的输出通道1（DAC_OUT1）或者ADC的输入通道5（ADC12_IN5）。

PA5支持的三种外设（SPI1、DAC、ADC）在同一时刻只能选择一种，选择的方法是开启相应外设的时钟，并使其它外设的时钟保持关闭状态。如果PA5被配置为复用功能，但是没有开启它支持的任何外设的时钟，它的输出是不确定的。



复用推挽输出和（普通）推挽输出在输出的时候均使用两个MOS管（P-MOS和-MOS），其输出电路是相同的。区别在于控制输出的信号来源：（普通）推挽输出控制MOS管的信号来自输出数据寄存器，而复用推挽输出的控制信号来自单片机的内置外设控制器（比如SPI1）。

复用开漏输出和（普通）开漏输出的道理是一样的。

下面这张图，是普通GPIO输出的引脚配置图，可以看到其输出信号来自输出数据寄存器（Output data register）：

![img](https://pic2.zhimg.com/80/v2-05eb661dab688c98800b658432ad0159_720w.webp)

下面这张图，是选择复用功能后的引脚配置图，可以看到其输出信号来自芯片内置的外设控制器：

![img](https://pic1.zhimg.com/80/v2-0a3a49e009e45644f77296b23aa0324c_720w.webp)

注：虽然复用模式的控制信号来自内置外设控制器，但是单片机（CPU）依然可以读取相应的数据。在复用推挽输出模式下，单片机可以通过读取输出数据寄存器（Output Data Register）的数据来获取上次输出的值；在复用开漏输出的模式下，单片机可以通过读取输入数据寄存器（Input Data Register）的值来获取引脚的状态。







# DMA

DMA，**直接存储器访问**，在STM32中，DMA属于外设设备，他有两个部分组成：**DMA1**、**DMA2**

<u>我们可以将DMA看作一个很厉害的搬运工， 这个搬运工在接收到处理器的命令过后会快速的将指定内存的数据进行搬运，同时搬运的过程中并不需要处理器干涉</u>

DMA的两个部分功能各不相同

> DMA1： P—>M, M—P
>
> ​		DMA1可以从外设设备中搬运数据到SRAM（内部寄存器），同时也可以将SRAM中的数据传输给外设
>
> DMA2：P—>M，M—P，M—>M
>
> ​		DMA2不仅实现了与DMA1的所有功能，而且它还可以 将内部的FLASH的数据搬运到SRAM中



![1701565178126](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1701565178126.png)



从上图中DMA可以分成6个部分，其中：

![1701565304684](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1701565304684.png)



## 流

流是数据传输的一条链路，每个DMA控制器有8条独立的数据流，每次传输的数据量最大为65535， 如果数据的单位为字的话，那一次可以传输256KB

```
1字=4字节(1 word = 4 byte)
1字节=8位(1 byte = 8bit)
```



一般来说，如果数据要从A搬运到B， 而从A搬运到B要经过一条路，这条路就是**流**

**而这里的流，在每次搬运数据的过程中，搬运数据的多少将会自行决定，最后搬运完的数据的大小一定是等于要传输数据的大小**



## 通道

每个数据流又包含有 8个通道选择，每个通道对应不同的DMA请求

具体的来说，**DMA要从某个地方搬运数据到另一个地方时，要走指定的通道以及指定的流** 



例如，以下为

### DMA1的请求映射表

![1701576873343](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1701576873343.png)



### DMA2请求映射表

![1701576938494](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1701576938494.png)

每个外设请求都占用一个数据流通道，相同外设请求可以占用不同数据流通道。比如SPI3_RX请求，即SPI3数据发送请求，占用DMA1的数据流0的通道0，因此当我们使用该请求的时候，就需要把DMA_S0CR寄存器的CHSEL[2:0]设置为“000”， 此时相同数据流的其他通道不被选择，处于不可用状态，比如此时不饿能使用数据流0的通道1即I2C1_RX请求等等。

实际上其他外设基本上都有两个对应的数据流通道，这两个数据流通道都是可选的，这样设计是尽可能提供多个数据流同时使用情况选择



另外如果DMA是将数据从寄存器搬运到寄存器的时候，就**不需要指定流以及通道路径**，但只能使用DMA2



## 仲裁器

一个DMA控制器对应8个数据流，数据流包含要传输数据的源地址、目标地址、数据等等信息。

如果我们需要同时使用同一个DMA控制器（DMA1或DMA2）多个外设请求时，那必然需要同时使用多个数据流。

那究竟哪一个数据流i具有优先传输的权利呢？

仲裁器是用于处理数据流纠纷问题的，当一条DMA请求从数据流0，另一个DMA请求从数据流1进行传输，但是DMA每次只能接收一个请求，因此仲裁器将**会根据数据流配置寄存器的PL位来判断请求的优先级**

同时如果**请求的优先级一致**，那么会根据数据流编号的大小来判断，**数据流编号越小优先级越大**



**同一个数据流只能使用一个通道，同一个DMA控制器可以使用多个数据流**





## FIFO

源与目标之间的数据中转站，每个传输的数据都必须要先到FIFO中，并且按指定的模式来继续传输

每个数据流有4字（一个字=4个字节）FIFO

* 当传输的数据到达FIFO，并且FIFO设置的模式是直接模式时，数据传输到FIFO后就会立即传输给目标

* 当传输的数据到达FIFO，并且FIFO设置的模式是FIFO的容量的1/4、1/2、3/4或满时，传输的数据必须要存入指定的FIFO大小的数据后才能传输给目标（如果大小不够，则并不会传输给目标）并且在使用FIFO模式的时候，当已经存入指定的FIFO大小的数据过后，将会按照**DMA_SxCR(DMA流配置寄存器)**的MBURST、PBURST位的

 通常要使用这个FIFO模式需要先禁止**DMA_SxFCR的DMDIS位的直接模式**，**并在同一个寄存器的FTH位处设置FIFO模式的阈值**

并且如果DMA是在存储器到存储器传输的时候会自动启动FIFO模式，软件并不能禁止



## 突发与节拍

[DMA突发模式的理解_什么是dma突发传输-CSDN博客](https://blog.csdn.net/weixin_43527214/article/details/104710659)

突发传输与FIFO紧密相关，突发传输需要结合FIFO使用

具体的FIFO阈值配置如下：

![在这里插入图片描述](https://img-blog.csdnimg.cn/20200307102756145.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3dlaXhpbl80MzUyNzIxNA==,size_16,color_FFFFFF,t_70)

由于**FIFO有16个字节**

MBURST = INCR4的意思是每次传输4*MSIZE大小的数据，且FIFO必须是此乘积的整数倍；

比如

```
MSIZE = 字节
MBURST = INCR4
因此，一次能传输的数据的最大大小为：4字节；
如果我们设置FIFO级别为1/4，也就是说：将16/4大小的位打包传输，一个节拍相当于一个（MSIZE的单位）
结合上面的配置后，总结就是：

传输的最大大小为4字节，现在每次要对大小为FIFO的几分之几的数据打包，并且需要对打包大小等于传输最大大小的数据包进行发送（突发）
```



这种发送的好处就在**于能够减少传输数据过后对数据要存储位置的寻址**，因为一次就已经传输了四位（甚至更多）位的数据，因此当我们下次继续存储的时候，我们就只需要寻址两次就已经存储了8位的数据了，传输速度大大提高

并且，当使用FIFO传输模式过后，这种传输模式在传输的过程中是无法被打断的













## DMA的系统实现

![1701828973867](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1701828973867.png)

由于DMA1的AHB外设端口并没有在总线矩阵中并没有与RAM寄存器进行连接，因此，DMA1并不能使用M-->M（内存-->内存）传输功能



### 存储端口、外设端口

DMA控制器实现双AHB主接口，更好利用总线矩阵和并行传输。DMA控制器通过存储器端口和外设端口与存储器和外设进行数据传输，关系见上图。

DMA控制器的功能时快速转移内存数据，需要一个连接至源数据地址的端口和一个连接至目标地址的端口。







### DMA的外设与存储器传输的两种方式

#### 外设到存储器

##### 双缓冲

![1704035985584](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1704035985584.png)

如上图，在外设传输数据到存储器中，在经过外设接口以及FIFO缓存器之后，如果此时开启了两个寄存器的时候，例如上图DMA_SxM0AR/DMA_SxM1AR，然后此时先将传输进来的数据先传入DMA_SxM0AR，当这个寄存器满了之后，**传入数据的指针将会指向DMA_SxM1AR**，同时将DMA_SxM0AR的数据通过数据总线传输出去，而当DMA_SxM1AR也满了的时候，**此时又会将传入数据指针将指向DMA_SxM0AR**，然后让DMA_SxM1AR传出数据，这就是**双缓冲**

注`：这种方式也需要使用流请求



#### 存储器到外设

![1704036428222](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1704036428222.png)

`注`：这种方式也需要使用流请求



#### 存储器到存储器

![1704036492569](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1704036492569.png)

这种方式所有流都能随便用





## DMA初始化结构体

![1704069004362](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1704069004362.png)



### DMA_Channel

DMA请求通道选择，可选通道0至通道7，每个外设对应固定的通道 	

每个DMA有8个通道，每个通道有8个数据流 

该结构体配置的是**DMA数据流x配置寄存器（DMA_SxCR）的CHSEL（通道选择）位**



### DMA_PeripheralBaseAddr

外设地址， 这个结构体成员用在当是**外设给寄存器进行传输**的时候，这个外设应该是**外设侦测到的数据寄存器地址**

这个结构体成员配置的是**DMA数据流x外设地址寄存器（DMA_SxPAR）**，这个地址寄存器有32位（因为地址通常都是32位的）

`注意：`这个寄存器的位会受到写保护，只有DMA_SxCR寄存器中的EN为“0”时才可以写入，而通常在**DMA传输结束时**或者**AHB主总线出现传输错误**的时候，这个EN为会被设置为"0"



### DMA_Memory0BaseAddr

![img](https://pic4.zhimg.com/80/v2-2100e8ed40a40e3bc110681dbde0aac3_1440w.webp)



存储器0地址，要传输到的存储器的地址，这个地址就是目标数据寄存器的地址，**双缓冲其实就是有两个不同的目标数据寄存器，然后在传输的时候使用交替指针的方式来进行存储，当一个寄存器满了过后，两个寄存器交换指针，使得一个寄存器接收，另一个寄存器可以被CPU读取数据进行处理。**

该结构体成员配置的是**DMA数据流x存储器0地址寄存器（DMA_SxM0AR）**这个寄存器的位

同时，如果使用的寄存器是双缓冲的话，那么还需要配置**DMA数据流x存储器1地址寄存器（DMA_SxM1AR）**，



**！问题**：这个结构体成员需要配置的是什么，要实现双缓冲应该怎么配置，具体什么含义，两个寄存器分别需要配置什么





### DMA_DIR

传输方向，可以选择**外设到存储器**、**存储器到外设**以及**存储器到存储器**

该结构体成员配置的是 **DMA数据流x配置寄存器（DMA_SxCR）**中的**DIR**位



### DMA_BufferSize

数据数目，它**表示这次传输的数据的大小**，由于该寄存器有16位，因此此次传输数据的大小应该为**65532MSize**

*MSIZE可以是字、半字、字节* **MSIZE由DMA_SxCR的MSIZE位控制**

该结构体成员配置的是**DMA数据流x数据项数寄存器（DMA_SxNDTR）**，该寄存器的位数位16位

运作工作：

假设要传输1000位的数据，并且每次传输一位，那么该寄存器的值会被设置为1000，然后每传输1位，该寄存器的值将会被减少1，并且在传输的过程中，该寄存器的值将为**只读模式**1



### DMA_PeripheralInc

外设是否递增

当传输的时候，外设的地址是否需要改变

如**使用外设**来传输数据到**寄存器**，那么此时就只有外设的寄存器是能产生数据然后进行传输的，因此就不需要地址递增



### DMA_MemoryInc

存储器地址是否递增

当存储器接收数据的时候，是否需要在存储器内递增存储器内部地址

如：当接收数据的时候，假设一次接收一位，我们将存储器想象成一个数组，那么第一次数据就会传输到数组的第一位，也就是数组名地址中，此时如果不递增地址，那么当接收到第二位的数据时，由于没有递增地址，新传入的数据将会覆盖第一次传输的数据，因此通常情况下需要地址递增



### DMA_PeripheralDataSize

外设数据宽度

传输时的数据宽度，通常情况下，传输源和接收源的数据宽度应该一致，否则会出现下面的状况

1. 传宽 > 接宽：数据丢失

由于一次传数的数据宽度大于接收的数据的宽度，因此接收方无法一次性接收这么大宽度的数据，因此会出现数据丢失

2. 传宽 < 接宽：内存浪费



### DMA_MemoryDataSize

存储器数据宽度

接收时的数据宽度



### DMA_Mode

模式选择

DMA的传输模式可以选择一次传输或者循环传输，

* 一次传输：DMA所有数据传输一次过后结束传输
* 循环传输：DMA所有数据传输完一次过后再继续不断传输，一般来说，循环传输是用在：**外设向寄存器传输**或者是**寄存器向外设传输**

该结构体的成员配置的是**DMA_SxCR:CIRC位的值**



### DMA_Priority

优先级

配置流的优先级，**当多个流的优先级被设置为同样的时候，流号的号数越小优先级反而越高**

该结构体的成员配置的是**DMA_SxCR：PL位**的值



### DMA_FIFOMode

FIFO模式，是否使用FIFO中转站传输模式

该结构体成员与下面三个结构体成员共同使用的，该成员配置的是**DMA_SxFCR:DMDIS位**

并且在存储器到存储器传输的时候，FIFO自动开启，软件禁止不了



### DMA_FIFOThreshold

FIFO阈值，FIFO阈值选择，1/4，1/3， 3/4或满，这个阈值决定了当FIFO接收到的数据占FIFO寄存器的几分之几时就传输一次

该结构体成员配置的是**DMA_SxFCR:FTH[1:0]**位



### DMA_MemoryBurst

存储器突发传输，存储器一次传输或接收多少个位，通常与下一个成员共同使用上面的 FIFO阈值配置表



如配置表中的，假设MSIZE为**字节**， FIFO级别配置为1/4，此时如果存储器突发传输大小设置为了**INCR4**，那么也就是意味着，当FIFO接收的数据达到FIFO的1/4（4字节），那么FIFO存储器就会一次将4个字节打包发出

又如FIFO级别设置为了1/2， 其他不变，那么也就是说：当FIFO接收的数据达到FIFO的1/2（8字节）时，由于传输大小为INCR4（一次传输4字节），因此将会发送两次，一次4字节，而如果将传输大小换成INCR8，则将会发送1次，一次8字节 

`注意：MSIZE*MBURST的乘积大小不能超过FIFO大小*FIFO阈值大小 `



### DMA_PeripheralBurst

外设突发传输，外设一次传输或接收多少个位，同上





### 如何将数据放在FLASH和SRAM

![1704073536953](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1704073536953.png)







# 存储器

存储器按其存储介质特性主要分为“**易失性存储器**”和“**非易失性存储器**”两大类。其中“易失/非易失”是指存储器断电过后，它存储的数据内容是否会丢失的特性。由于一般易失行存储器存取速度快，而非易失性存储器可长期保存数据。

在计算机中**易失性存储器最典型的代表是内存**，**非易失性存储器的代表则是硬盘**。

![1702220057193](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1702220057193.png)

**易失性：掉电是否能保存数据的功能**



### RAM存储器

RAM是Random Access Memory 的缩写，被译为**随机存储器**，所谓“随机存取”，指的是当存储器中的数据被读取或写入时，所需要的时间与这段信息所在的位置无关。

随机存取是一种数据存取方式，它可按一定顺序或任意顺序存取存储在计算机寄存器中的数据

随机存取也称为直接存取，是将数据**按**它们在**存储器中的位置作为位置标识符进行访问**，在使用随机存取时，不需要按照特定的顺序来阅读前面的数据，而只要按照指定的位置来方位就可以快速/直接访问所需要的数据。随机存取允许程序读取任何记录的任意位置，而在顺序存取中，只能从记录的第一个字节处开始顺序读取。



#### DRAM



动态随机存储器DRAM的存储单元以电容的电荷来表示数据，有电荷代表1，无电荷代表0，见图*DRAM*存储单元。但是时间一长，代表1的电容会放电，代表0的电容会吸收电荷，因此它需要定期刷新操作，这就是“动态”这一词所形容的特性。

![1704113121658](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1704113121658.png)

同时刷新操作会堆电容进行检查，若电量大于满电量的1/2，则认为其代表1，并把电容充满电；若电量小于1/2，则任务其代表0， 并把电容放电，借此来保证数据的正确性



DRAM的工作过程：

DRAM使用晶体管以及电容来进行存储数据， 当给晶体管的G极通电，此时晶体管导通，给电容充电，此时我们就可以将其看成存储了一位数据

当G极导通，并且给电容输入高电平，此时我们认为存储了数据1

![1704113664087](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1704113664087.png)

当G极导通，并且给电容输入低电平，此时我们认为存储了数据0

![1704113697787](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1704113697787.png)

如果我们要进行读操作，我们也需要让G极为高电平，并不使用G极输入，而是让G极作为输出

![1704113781701](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1704113781701.png)

同时需要在输出中设置一个放大器，当晶体管导通之后，放大器会检测到电容上的电压信息，从而判断其存储的是0还是1

并且由于电容的漏电流（漏电流即给电容施加电压过后历经电容的直流电流）不可能为0，因此产生了电压差，因此电容的会慢慢漏电，因此需要不停的刷新，以此来给电容充电。同时由于需要不停的充电以及放大，因此这种内存的速度会慢一点

————————

##### RC电路

阻容电路，其主要由一个电阻以及一个电容组成

————————





SRAM

根据DRAM的通讯方式，又分为同步和异步两种，这两种方式根据通讯时是否需要使用时钟信号来区分。图<u>同步通讯时序图</u>是一种利用时钟进行同步的通讯时序，它在时钟的上沿表示有效数据。

![1704113275892](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1704113275892.png)

由于使用时钟同步的通讯速度更快，所以同步DRAM使用更加广泛，这种DRAM被成为SRAM

SRAM多用于处理器的缓存，这个缓存一般比DRAM要小的多 

SRAM通常由**存储矩阵、地址译码器、读写控制器**组成

![1704120396248](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1704120396248.png)

##### 或非门

https://www.bilibili.com/video/BV1oL411D77D/?spm_id_from=333.337.search-card.all.click&vd_source=b6ec906023fa46a5d781f4c754d9bf34

https://www.bilibili.com/video/BV1RZ4y1C7zy/?spm_id_from=333.337.search-card.all.click&vd_source=b6ec906023fa46a5d781f4c754d9bf34

![img](https://iknow-pic.cdn.bcebos.com/78310a55b319ebc4eb64d7448e26cffc1f1716ca?x-bce-process=image%2Fresize%2Cm_lfit%2Cw_600%2Ch_800%2Climit_1%2Fquality%2Cq_85%2Fformat%2Cf_auto)

当或非门输入有一个为1，则将会输出1并取反输出0

当或非门输入没有1，则将会输出0并取反输出1

两个或非门便组成了SR锁存器

![1704120742868](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1704120742868.png)

$R_D$复位端

$S_D$置位端

$Q和Q^*$为输出端，其中$Q和Q^*$是相反的，也就是说，当$Q$为1时，$Q^*$将为0，反之亦然

通常来说这个SR锁存器当出现两个输入相同的时候，其输出将会是不确定的，因此种情况需要视情况而定 ![1704121679743](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1704121679743.png)

然后SRAM就是使用这个SR锁存器组成矩阵，即可形成SRAM



#### 六管 CMOS SRAM 单元

![img](http://file.elecfans.com/web1/M00/C7/65/pIYBAF9lfUiAeeLKAABu-OmTKkc580.jpg)









#### SDRAM





### ROM

ROM是“Read Only Memory”的缩写，意为只能读的存储器。



#### MASK ROM

MASK（掩模） ROM就是正宗的“Read Only Memory”，存储在它内部的数据应该是出场时使用特殊工艺固化的，生产过后就不可以改变，其主要的优势就是大批量生产的时候成本较低。



#### OTPROM

OTPROM是以此可变成存储器，这种存储器在出厂的时候内部并没有数据，用户可以使用专用的编程器将自己的数据写入，但是这种存储器只能写入一次，被写入过后，它的内容也不可再修改



#### EPROM

EPROM(Erasable Programmable ROM) 是可重复擦写的存储器，它解决了 PROM 芯片只能写入一次的问题。这种存储器使用紫外线照射芯片内部擦除数据，擦除和写入都要专用的设备。现在这种存储器基本淘汰，被 EEPROM 取代。



#### EEPROM

EEPROM(Electrically Erasable Programmable ROM) 是电可擦除存储器。EEPROM 可以重复擦写，它的擦除和写入都是直接使用电路控制，不需要再使用外部设备来擦写。而且可以按字节为单位修改数据，无需整个芯片擦除。现在主要使用的 ROM 芯片都是 EEPROM。



#### Flash

 Flash存储器又称为闪存，它也是可重复擦写的存储器，它的容量会比一般的EEPROM大得多，且在擦除的时候，会以多个字节为单位，例如，有的FLASH以4096个字节为扇区，**而最小的擦除单位为一个扇区**，当需要更改扇区中的某个值的时候，需要将整个扇区的值都改为例如1的数据，然后在按整片扇区进行写入数据操作

FLASH存储器又分为 *NOR FLASH* 和 *NAND FLASH*，见表 NOR_FLASH 与 NAND_FLASH 特性对比。

![1704336472765](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1704336472765.png)

NOR FLASH与NAND_FLASH最大的区别就在于，读写的单元不同，

NOR FLASH可以基于一个一个的字节进行读写，这种读取速度较高，但写入速度却低，并且NOR FLASH可以执行程序

NAND_FLASH必须要以扇区或块的方式进行读写，这种读取方式读取指定位置的数据将会较慢，而写入数据的速度则会较快， 并且NAND_FLASH不能直接执行程序





# I2C

半双工、同步

I2C是一个支持多设备的总线，“总线”指多个设备共用的信号线。在I2C通讯总线中，可以连接多个I2C的通讯设备，支持多个通讯主机和多个通讯从机 

![1704338692646](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1704338692646.png)

如上图所示，一个I2C总线只使用两条总线线路，一条双向串行数据线（**SDA**），一条串行时钟线（**SCL**）。**数据线即用于传输数据**，**时钟线用于数据的同步收发**。

使用两条线的作用是：

当传输的过程中，如果并没有使用时钟信号线，而当一个主机发送数据给从机的时候，从机可能会由于错误的读取数据，例如主机只发送了一个字节的数据，但是由于从机的错误读取，而导致自身接收了8个字节的数据，这样就会出现严重的错误，因此需要使用时钟信号线，在传输的过程中，使用指定的上升沿或下降沿作为数据信号接受点，以此使得从机能正确的接收主机发送的数据。



并且每个连接到总线的设备都有一个**独立的地址**，主机可以利用这个地址进行不同设备之间的访问。这样就可以让主机与不同的从机进行



* 总线通过上拉电阻接入到电源。当I2C设备全部空闲，也就是说I2C设备全部都为高阻态模式的时候，此时总线将会由上拉电阻把总线拉成高电平。

    开漏模式的作用：https://blog.csdn.net/lx123010/article/details/131420332

    * 这时的设备的GPIO端口都应该设置为开漏模式，这是因为如果有设备为推挽模式，而另一个设备处于开漏模式并且是0v低电平的话，推挽模式的设备的电平将会直接流向低电平的设备，形成短路（高电平的电流直接流向低电平），这有可能直接烧毁低电平的设备

* 而当总线上的设备使用开漏模式并将设备与总线连接的线导通的时候，总线上的电平将会被拉低，整体的电流将会从总线流向设备

* 而当设备与总线连接的线并没有导通的时候，总线的电平将会由于上拉电阻拉到高电平的状态。





## I2C协议层

https://zhuanlan.zhihu.com/p/556505577

I2C的协议定义了通讯的起始和停止信号、数据有效性、相应、仲裁、时间同步和地址广播等环节



### I2C的读写过程

 I2C将**SCL处于高**时**SDA拉低**的动作作为<u>开始信号</u>，**SCL处于高**时**SDA拉高**的动作作为<u>结束信号</u>；传输数据时，**SDA**在**SCL低电平时改变数据**，在**SCL高电平时保持数据**，每个SCL脉冲的高电平传递一位数据



I2C器件开始通信（传输数据）之前，串行时钟线SCL和串行数据线SDA线由于上拉的原因处于高电平状态，此时I2C总线处于空闲状态。如果主机想要开始传输数据，只需在**SCL为高电平**时将**SDA线拉低**，产生一个<u>起始信号</u>

从机检测到起始信号后，准备接收数据，当数据传输完成，主机只需产生一个停止信号，告诉从机数据传输结束，**停止信号的产生是在 SCL 为高电平时，SDA 从低电平跳变到高电平**，从机检测到停止信号后，停止接收数据

![1704894036195](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1704894036195.png)





我们在起始信号之后，主机开始发送传输的数据；**在串行时钟线 SCL 为低电平状态时**，**SDA 允许改变传输的数据位**（1 为高电平，0 为低电平），**在SCL 为高电平状态时**，**SDA 要求保持稳定**，相当于一个时钟周期传输 1bit 数据，经过8 个时钟周期后，传输了 8bit 数据，即一个字节。

第8 个时钟周期末，**主机释放SDA 以使从机应答**，在第 9 个时钟周期，从机将 SDA 拉低以应答；如果第 9 个时钟周期，SCL 为高电平时，SDA 未被检测到为低电，视为非应答，表明此次数据传输失败。第 9 个时钟周期末，从机释放 SDA 以使主机继续传输数据，如果主机发送停止信号，此次传输结束。**我们要注意的是数据以8bit 即一个字节为单位串行发出，其最先发送的是字节的最高位。**

![img](https://pic2.zhimg.com/80/v2-34761e6c9a40bcd763d8b646a4364bb5_720w.webp)

#### **主机到从机中写数据**

![1704897728234](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1704897728234.png)

##### 器件地址

器件地址**（也称从机地址，SLAVE ADDRESS）**：每个I2C器件都有一个器件地址，有些I2C器件的器件地址是固定的， 而有些 I2C 器件的器件地址由一个固定部分和一个可编程的部分构成。当主机想给某个器件发送数据时，只需向总线上发送接收器件的器件地址即可。

进行信号传输时，**主机首先向总线上发送开始信号，对应开始位S**，**然后按照从高到低的位序发送器件地址，一般位7bit**，**第8bit位为读写控制位，当该位为0时将执行写操作，当该位为1时将执行读操作，然后接收从机响应**

![img](https://pic4.zhimg.com/80/v2-c0e3e7cbc9bc596503758d6fed2a3a53_720w.webp)

发送完第一字节（7个器件地址和一位读写控制位）并收到从机正确的应答过后就开始发送，并且每发送1个字节的数据过后都需要一个应答位， 当应答位一直为低电平的时候，就可以一直传输数据，而当从机无法接收数据时，只需要发送一个非应答信号，即1。然后主机变化发送一个停止位，即SCL为高电平，SDA为从低到高的信号



**主机从从机中读取数据** 

![1704899318844](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1704899318844.png)

主机由从机中读取数据与向从机传输数据不一样的就在于，第二个应答位应该时主机发送的，因为当主机从从机处读取数据的时候，第一个应答位为主机与从机建立连接的确认位，而第二个应答位应该是主机发送的，因为从机发送数据，需要主机确定是否需要继续传输，因此应该是主机发送的



#### **通讯的复合格式**

![1704899746423](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1704899746423.png)

这种格式进行通讯是读取与写入的结合，

<u>当主机要读取从机寄存器中的对应的寄存器的对应的存储单元格的内容时</u>，进行通讯时的方式就有所不同，当要进行读取对应存储单元格的内容时，主机应该先发送**起始位**，然后跟着发送**7位的从机地址位**，并发送**写入**的操作指令，并**等待从机应答**，从机应答过后，先给从机的寄存器发送**要读取的存储单元格的地址**，**从机在接收到地址过后发送应答位**，然后从机再次产生**起始信号**，并**再次寻址从机的地址**，然后接上一个**读取位**，即1，再等待从机的应答，然后开始接收数据，当接收完毕过后，主机将产生一个应答信号，即1，然后再传输一个结束位P，本次的复合传输结束

`注`：起始或停止的信号一般都是由主机产生



### 数据的有效性

I2C使用SDA信号线来传输数据，使用SCL信号线进行数据同步。

* SDA数据线在SCL信号线为高电平的时候，SDA表示的数据才有效，即此时的SDA为高电平时表示数据为“1”，而当SDA表示为低电平的时候表示数据为“0”

* 而当SCL为低电平的时候，SDA的数据无效，一般在这个时候SDA进行电平转换，做好下次表示数据的准备



![1705372913823](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1705372913823.png)



### 响应

I2C的数据和地址传输都带有响应，响应包括“应答（ACK）”和非应答“非应答（NACK）”两种信号。

作为数据接收端是，当设备（无论从机还是主机）接收到I2C传输的一个字节的数据或地址过后，若希望对方继续发送数据，则需要向对方发送“应答（ACK）”信号，发送方会继续发送下一个数据；

若希望接收方希望结束数据传输，则向对方发送“非应答（NACK）”信号，发送方接收到该信号过后将会产生一个停止信号，结束信号传输。

![1705373186781](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1705373186781.png)
当传输时主机还将会产生时钟信号，在第9个时钟时，数据发送端会释放SDA的控制权，由数据接收端控制SDA，若数据接收端控制过后的SDA为**高电平**，则表示**非应答信号（NACK）**，主机将会发送停止信号，并结束信号传输

若数据接收端控制过后的SDA为**低电平**， 则表示**应答信号（ACK）**，主机将继续传输数据





## I2C架构

![1705373420189](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1705373420189.png)

I2C 的所有硬件架构都是根据图中左侧 SCL 线和 SDA 线展开的 (其中的 SMBA 线用于 SMBUS 的
警告信号，I2C 通讯没有使用）

STM32芯片有多个I2C外设，它们的I2C通讯信号引出到不同的GPIO引脚上，使用时必须配置到这些指定的额引脚，关于GPIO的引脚的复用功能，可以查阅《STM32F4xx规格书》



## I2C时钟控制逻辑

SCL线的时钟信号，由I2C接口根据**时钟控制寄存器（CCR）**控制，控制的参数主要为时钟频率。配置I2C的CCR寄存器可修改通讯速率相关的参数：

* 可选择I2C通讯的“标准/快速”模式，这两个模式分别I2C对应100/400Kbit/s的通讯速率
* 在快速模式下可选择SCL时钟的占空比，可选$T_{low}/T_{high}=2或T_{low}/T_{high}=16/9$，我们知道I2C协议在SCL高电平时对SDA信号采集，SCL低电平时准备下一个数据，修改SCL的高低电平将会影响数据采样，当其实这两种模式的比例差别并不大，因此，若不是很严格，这里随便选就可以了

在I2C通信时，只有在时钟信号为高电平时，数据线上对应的数据才是有效数据，所以若高电平持续时间短的话，读写时间短，数据可能没有读写完，导致数据传输不准确；若高电平持续时间长的话，读写时间相应变长，可能导致时间的浪费，通信效率变低。所以合理的占空比对I2C通信是很必要的！



* CCR寄存器中还有一个12位的配置因子CCR，它与I2C外设的输入时钟源共同作用，产生SCL时钟，STM32的I2C外设都挂载在APB1时钟总线上，SCL的信号线输出的时钟公式如下：

![1705823357770](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1705823357770.png)



![1705823380329](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1705823380329.png)



## 数据控制逻辑

I2C的SDA信号主要连接到数据移位寄存器上，数据移位寄存器我们可以将其想象成一个搬运工，它的数据来源以及目标是数据寄存器（DR）、地址寄存器（OAR）、PEC寄存器以及SDA数据线。

当向外发送数据的时候，**数据寄存器**将会作为**数据移位寄存器**的数据源，把数据一位一位地通过SDA信号线发送出去；

当从外部接收数据地时候，**数据移位寄存器**<u>将SDA信号线采样地数据一位一位地存储到数据寄存器</u>中，若使能了数据校验，接收到的数据会经过**PCE计算器**运算，运算的**结果存储到PEC寄存器**中。



当STM32的I2C工作在从机模式的时候，接受到的设备地址信号时，数据移位寄存器会把接收到的地址与STM32的自身的I2C地址寄存器的值作比较，以便响应主机的寻址。STM32的自身I2C地址可通过修改自身地址寄存器修改，支持同时使用两个I2C设备地址，两个地址分别存储在OAR1和OAR2中。





## 整体的控制逻辑

![1705824410460](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1705824410460.png)





## 通讯过程

### 主发送器

![1705824912333](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1705824912333.png)

![1705824930607](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1705824930607.png)



### 主接收器

![1705825108893](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1705825108893.png)

![1705825141673](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1705825141673.png)



## 问题

### 为什么stm32中使用I2C协议的时候需要将对应的GPIO引脚设置为开漏模式，而不是推挽模式

1. I2C总线是一个双向传输的串行通信协议，其中SDA（数据线）和SCL（时钟线）需要共享。在I2C总线上，设备之间的通信是通过拉高或拉低SDA线来实现的，而时钟线则由主设备控制。因此，当一个设备需要将SDA线拉低时，其他设备需要释放SDA线以使其变为高阻状态，这样才能正常进行通信。开漏模式可以实现这种共享。
2. 推挽模式的GPIO引脚在输出高电平时会提供驱动能力，输出低电平时会吸收电流。但是，在I2C总线上，当一个设备需要将SDA线拉低时，它只需要提供一个连接到地的电阻，而不需要提供驱动电流。因此，使用开漏模式可以更好地适应I2C总线的通信特点。
3. 在I2C总线上，多个设备共享同一根总线。如果所有设备都使用推挽模式，当其中一个设备输出低电平时，其他设备如果同时输出高电平，则会导致短路。而使用开漏模式，则可以避免这种情况，因为开漏模式下输出高电平时，引脚实际上处于高阻状态。



### 为什么I2C协议连接到相关总线的IC数量受电容的限制

！











# EEPROM

EEPROM是一种掉电过后数据不丢失的存储器，常用于存储一些配置信息



## 硬件设计

![1708158570457](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1708158570457.png)



![1708158597737](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1708158597737.png)

EEPROM电路结构如上，它的SCL和SDA引脚连接到了STM32对应的I2C引脚上，结合上拉电阻，构成了I2C通讯总线，它们通过这两条I2C通讯总线进行交互。

EEPROM芯片的设备地址一共有**7位**，其中**高4位固定为：1010**，**后三位则由A0/A1/A2信号线的电平决定**



按照我们的连接，A0/A1/A2均为0，所以EEPROM的7位设备地址是：1010000，即0x50。

由于I2C通讯时常常时地址与读写方向连在一起构成一个8位数，且当R/W位为0时，表示写方向，所以加上7位地址，其值为“0xA0”，常称该地址为I2C设备的“**写地址**”；

当R/W位为1时，表示读方向，加上7位地址，其值位“0xA1”，常称该值为“**读地址**”



EEPROM芯片中还有一个WP引脚，具有写保护功能，当该**引脚电平为高**时，**禁止写入数据**

​																						   当该**引脚电平为低**时，**可写入数据**

​																								若**引脚直接接地**时，表示**不使用写保护功能**







# SPI

SPI 协议是是，一种高速全双工的通信总线。它被广泛地使用在 ADC、LCD 等设备与 MCU 间，要求通讯速率
较高的场合。

## SPI物理层

![1708870092505](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1708870092505.png)

SPI通讯使用3条总线以及片选线，3条**总线**分别为**SCK、MOSI、MISO**，**片选线**为**SS**

其中

SCK：始终信号线，用于主设备与从设备之间通讯同步，它由通讯主机产生，这条线决定了通讯的速率，不同设备支持的通讯速率不一样，**通讯速率受限于低速设备**



MOSI (Master Output，Slave Input)：数据传输线，主设备输出/从设备输入引脚，主机从这条线输出信号，从设备从这条线接收信号，因此这条线的通讯方向应该是**主设备向从设备**



MISO(Master Input,，Slave Output)：主设备输入/从设备输出设备，从设备通过这条线输出信号，主设备从这条线接收信号，因此这条线的通讯方向应该是**从设备向主设备**



SS（slave select）：从设备选择信号线，常成为片选信号线，也叫NSS、CS当多个从设备连接到主机时，SCK、MOSI、MISO线作为总线将会并联上所有设备， 即无论有多少个连接设备，都使用这三条总线，而每个从设备都有独立的这一条 NSS 信号线，本信号线独占主机的一个引脚，即有多少个从设备，就有多少条片选信号线。I2C通讯使用的是设备地址来寻址或选中总线上的某个设备并与其进行通讯，而SPI中并没有通讯地址，当主机要选择从设备时，把该从设备的NSS信号线设备为低电平，该设备即被选中，即片选有效，接着主机就可以与从设备进行通讯，所以SPI通讯以**NSS线电平为由高到低开始信号**，以NSS线电平为由低到高为结束信号。



## SPI协议层

### SPI基本通讯过程

![1708871251872](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1708871251872.png)

这是一个主机的通讯时序，其中NSS、MOSI、SCK都由主机控制，MISO由从机控制

MISO、MOSI信号线的信号只有在片选信号线NSS被置为低电平的时候才有效，在SCK的每个时钟周期MOSI和MISO传输一位数据



### 通讯的起始和停止信号

如上图所示，SPI的NSS信号线由高到低，是SPI的起始信号

​													由低到高，是SPI的停止信号

NSS 是每个从机各自独占的信号线，当从机在自己的 NSS 线检测到起始信号后，就知道自己被主机选中了，开
始准备与主机通讯。在图中的标号处，NSS 信号由低变高，是 SPI 通讯的停止信号，表示本次通讯结束，从机的选中状态被取消。





### 数据有效性

SPI 使用 MOSI 及 MISO 信号线来传输数据，使用 SCK 信号线进行数据同步。MOSI 及 MISO 数据线在 SCK 的每个时钟周期传输一位数据，且数据输入输出是同时进行的。

数据传输时，MSB先行或 LSB 先行并没有作硬性规定，但要保证两个 SPI 通讯设备之间使用同样的协定，一般都
会采用图 SPI 通讯时序 中的 MSB 先行模式。

观察图中的标号处，MOSI 及 MISO 的数据在 **SCK 的上升沿期间变化输出**，在 **SCK 的下降沿时**
**被采样**。即在 SCK 的下降沿时刻，MOSI 及 MISO 的数据有效，<u>高电平时表示数据“1”</u>，为低电
平时表示数据“0”。在其它时刻，数据无效，MOSI 及 MISO 为下一次表示数据做准备。
SPI 每次数据传输可以 8 位或 16 位为单位，每次传输的单位数不受限制。





#### MSB与LSB

https://blog.csdn.net/woshizuopie/article/details/122921295





### CPOL/CPHA

上图中的时序只是SPI中的一种通讯模式，SPI有4中通讯模式，它们的主要的区别就是总线空闲的时候SCK信号线的时钟状态以及数据采样时刻



时钟极性CPOL是指SPI通讯设备在空闲状态时，SCK时钟信号线的电平（即SPI通讯开始前，NSS片选信号线为高电平是SCK的状态）。当CPOL=0时，SCK在总线空闲的时候为低电平，当CPOL=1时，SCK在总线空闲的时候为高电平。



时钟相位CPHA是指数据被采样的时刻，

当CPHA=0时，MOSI和MISO信号线的信号将会在SCK时钟线的“**奇数沿**”被采样，

当CPHA=1时，MOSI和MISO信号线的信号将会在SCK时钟线的“**偶数沿**”被采样

见下图CPHA=0时，SPI的通讯模式

![1708993727414](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1708993727414.png)



在这个图中，我们的SCK时钟线的空闲时的时钟信号有两种，一种是空闲时由于**CPOL模式=0时的低电平**，一种是**CPOL模式=1的高电平**

但是无论SCK是高电平也好低电平也罢

当CPHA=0时，MOSI与MISO信号线中的数据将会在SCK电平产生**奇数边沿**处被采集，此时如果SCK时钟线处于CPOL=0模式时，我们也可以理解为，<u>上升沿采集数据</u>，当CPOL=1模式时，<u>为下降沿采集数据</u>



![1708994318373](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1708994318373.png)

同样的，

当CPHA=1时，MOSI与MISO信号线中的数据将会在SCK电平产生**偶数边沿**处被采集，此时如果SCK时钟线处于CPOL=0模式时，我们也可以理解为，<u>下降沿采集数据</u>，当CPOL=1模式时，为<u>上升沿采集数据</u>

MOSI 和 MISO 数据线的有效信号在 SCK 的奇数边沿保持不变，数据信号将在 SCK 奇数边沿时被采样，在非采样时刻，MOSI 和 MISO 的有效信号才发生切换。



**注意**：*SPI采样时刻并不是由上升/下降沿决定的，上面这么解释只是好理解，真正决定采样时刻的应该是CPHA以及CPOL的不同状态*



由 CPOL 及 CPHA 的不同状态，SPI 分成了四种模式，见表 SPI 的四种模式 ，**主机与从机需要工作在相同的模式下才可以正常通讯**，实际中采用较多的是“模式 0”与“模式 3”。

![1708994496051](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1708994496051.png)





### SPI外设简介

STM32 的 SPI 外设可用作通讯的主机及从机，支持最高的 SCK 时钟频率为 fpclk/2 (STM32F407 型号的芯片默认 fpclk142MHz，fpclk2 为 84MHz)，完全支持 SPI 协议的 4 种模式，数据帧长度可设置为 8 位或 16 位，可设置数据 MSB 先行或 LSB 先行。它还支持双线全双工 (前面小节说明的都是这种模式)、双线单向以及单线模式。其中双线单向模式可以同时使用 MOSI 及 MISO 数据线向一个方向传输数据，可以加快一倍的传输速度。而单线模式则可以减少硬件接线，当然这样速率会受到影响。我们只讲解双线全双工模式。



### STM32的SPI架构

![1709026586214](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709026586214.png)



### SPI引脚

SPI 的所有硬件架构都从图 SPI 架构图 中左侧 MOSI、MISO、SCK 及 NSS 线展开的。STM32 芯片有多个 SPI 外设，它们的 SPI 通讯信号引出到不同的 GPIO 引脚上，使用时必须配置到这些指定的引脚，**具体的引脚应该查看对应型号的芯片的英文数据手册的GPIO引脚定义表**

例如：

![1709026825261](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709026825261.png)



**stm32F4xx的SPI引脚**

![1709026878304](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709026878304.png)

其中 SPI1、SPI4、SPI5、SPI6 是 APB2 上的设备，最高通信速率达 42Mbtis/s，SPI2、SPI3 是 APB1
上的设备，最高通信速率为 21Mbits/s。其它功能上没有差异。



### 时钟控制逻辑

SCK 线的时钟信号，由波特率发生器根据“控制寄存器 CR1”中的 BR[0:2] 位控制，该位是对 $f_{pclk}$时钟的分频因子，对$f_{pclk}$ 的分频结果就是 SCK 引脚的输出时钟频率，计算方法见表 BR 位对 $f_{pclk}$
的分频 。

![1709028032753](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709028032753.png)

如上图中的频率$f_{pclk}$将由SPI具体是在哪条APB总线上决定的，APB1为$f_{pclk1}$，APB2为$f_{pclk2}$

通过配置“控制寄存器 CR”的“CPOL 位”及“CPHA”位可以把 SPI 设置成前面分析的 4 种 SPI
模式。



### 通讯过程

STM32使用SPI外设通讯时，在通讯的不同阶段它会对“状态寄存器SR”的不同数据为写入参数，我们可以通过这个寄存器来了解SPI通讯的通讯状态

![1709037743807](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709037743807.png)

主模式收发流程如下：

（1）控制NSS信号线，产生起始信号

（2）将要发送的数据写入到“数据寄存器DR”中，该数据会存储到发送缓冲区中

（3）通讯开始，SCK时钟开始运行。MOSI把发送缓冲区中的数据一位一位地传输到从机中；MISO则把数据一位一位地存储到接收缓冲区中。

（4）当发送完一阵数据的时候，“状态寄存器SR”中的TXE标志位将会置1，其含义为：发送缓冲区空，传输完一帧；类似地，当接受完一帧数据的时候，“RXNE标志位”将会置1，表示接收到一帧，接收缓冲区非空

（5）等待到“TXE标志位为1时”，若要继续发送数据，则再次往“数据寄存器DR”写入数据即可；

​          等待到“RXNE”标志位为1时，通过读取“数据寄存器DR”可获取接收缓冲区中的数据



假如我们使能了 TXE 或 RXNE 中断，TXE 或 RXNE 置 1 时会产生 SPI 中断信号，进入同一个中断服务函数，到 SPI 中断服务程序后，可通过检查寄存器位来了解是哪一个事件，再分别进行处理。也可以使用 DMA 方式来收发“数据寄存器 DR”中的数据。



## SPI初始化结构体

### SPI_Direction

本成员设置SPI的通讯方向，可设置为

双线全双工（SPI_Direction_2Lines_FullDuplex）,

双线接收（SPI_Direction_2Lines_RxOnly）,

单线只接收（SPI_Direction_1Line_Rx），

单线只发送模式（SPI_Direction_1Line_Tx）





### SPI_Mode

本成员设置SPI工作在主机模式（SPI_Mode_Master）或从机模式（SPI_Mode_Slave）,这两个模式的最大区别为SPI的SCK时钟信号由哪个设备提供，SCK的时序时由通讯中充当的主机的设备产生的。若被配置为从机模式，STM32的SPI外设将接收外来的SCK信号





### SPI_DataSize

本成员可以选择SPI通讯的数据帧大小是8为（SPI_DataSize_8b）还是16位（SPI_DataSize_16b）.





### SPI_CPOL和SPI_CPHA

这两个成员配置SPI的时钟极性CPOL与时钟相位CPHA，这两个配置影响到SPI的通讯模式，

当CPOL=0时，空闲时SCK时钟信号为低电平

当CPOL=1时，空闲时SCK时钟信号为高电平

当CPHA=0时，奇数沿发送或接收

当CPHA=1时，偶数沿发送或接收

![1708994496051](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1708994496051.png)

时钟极性CPOL成员可以设置为高电平（SPI_CPOL_High）或低电平（SPI_CPOL_Low）

时钟相位CPHA成员则可以设置为SPI_CPHA_1Edge（在SCK的奇数边沿采集数据）或SPI_CPHA_2Edge（在SCK的偶数边沿采集数据）





### SPI_NSS

本成员配置NSS引脚的使用模式，可以选择为硬件模式（SPI_NSS_Hard）与硬件模式（SPI_NSS_Soft），在硬件模式中的SPI片选信号由SPI硬件自动产生，而软件模式则需要我们亲自把相应的GPIO端口拉高或置低产生非片选和片选信号。实际的使用中软件模式应用比较多





### SPI_BaudRatePrescaler

本成员设置波特率分频因子，分频后的时钟即为SPI的SCK信号线的时钟频率，这个成员的参数可设置为fpclk的2、4、6、8、16、32、64、128、256分频





### SPI_FirstBit

所有串行的通讯协议都会有MSB先行（高位数据先存储到低位地址中）还是LSB先行（低位数据先存储到低位地址中）的问题，而STM32的SPI模块可以通过这个结构体成员，对这个特性进行编程





### SPI_CRCPolynomial

这是SPI的CRC校验中的多项式，若我们使用CRC校验时，就是用这个成员的参数（多项式），来计算CRC的值



配置完这些结构体成员后，我们要调用**SPI_Init**函数将结构体中的参数写入到寄存器中，实现SPI的初始化，然后点用**SPI_Cmd**来使能SPI外设









## SPI读写串行FLASH实验

FLSAH 存储器又称闪存，它与 EEPROM 都是掉电后数据不丢失的存储器，但 FLASH 存储器容量普遍大于 EEPROM，现在基本取代了它的地位。我们生活中常用的 U 盘、SD 卡、SSD 固态硬盘以及我们 STM32 芯片内部用于存储程序的设备，都是 FLASH 类型的存储器。在存储控制上，最主要的区别是 **FLASH 芯片只能一大片一大片地擦写**，而在“I2C 章节”中我们了解到 **EEPROM可以单个字节擦写**。



![1709092703225](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709092703225.png)

上图是我们实验所使用的串行FLASH存储器与串口的连接图，因为只有这几个引脚连接了SPI外设，因此，我们需要使用这几个引脚



### 编程要点

1.初始化通讯使用的目标引脚以及端口时钟；

2.使能SPI外设的时钟

3.配置SPI外设的模式、地址、速率灯参数并使能SPI外设；

4.编写基本SPI按字节收发的函数

5.编写对FLASH擦除及读写操作的函数；

6.编写测试程序，对读写数据进行校验



### 1. SPI硬件定义

**预定义宏**

![1709116483011](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709116483011.png)



**初始化指定GPIO引脚函数**

![1709116554408](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709116554408.png)



**初始化SPI设置**

![1709116578652](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709116578652.png)

**注意：**最后一个成员为CRC校验计算式，由于我们的FLASH芯片通讯不需要CRC校验，并没有使能SPI的CRC功能，这是的CRC的值是无效的



### 2.使用SPI发送或接收一个字节的数据



通过这两段代码，我们便可以发送数据，但是**注意**，这里我们只是使用了SPI1进行发送数据，但是没有具体的发送目的地，然后我们在继续使用SPI_I2S_ReceiveData()函数将刚才发送的数据重新读取，因此就可以读取刚才发送的数据（这里的byte值可以为任何值），同时注意的是，SPI_DR既是接收缓冲区又是发送缓冲区

```c
uint8_t SPI_FLASH_SendByte(uint8_t byte)
/* 向FLASH发送数据*/
{
	SPITimeout = 10000;
	// 如果发送缓冲区一直不为空，则退出循环，并打印错误信息
	while (SPI_I2S_GetFlagStatus(SPI1, SPI_I2S_FLAG_TXE) == RESET)
	{
		if ((SPITimeout--)==0) return SPI_TIMEOUT_UserCallback(0);
	}
	
	SPI_I2S_SendData(SPI1, byte);          // 发送传入的数据
	
	SPITimeout = 10000;
	// 如果接收缓冲区一直为空，则认为并没有收到数据，将退出
	while (SPI_I2S_GetFlagStatus(SPI1, SPI_I2S_FLAG_RXNE) == RESET)
	{
		if((SPITimeout--)==0) return SPI_TIMEOUT_UserCallback(1);
	}
	
	return SPI_I2S_ReceiveData(SPI1);
}
```

```c
uint8_t SPI_FLASH_ReadByte(void)
	/* 当主机发送完数据过后，查看SPI数据寄存器中返回来的数据 */
{
	return (SPI_FLASH_SendByte(value));
}
```



### 3.控制FLASH的指令

## 控制FLASH的指令

FLASH芯片定义了很多指令，我们通过控制STM32利用SPI向FLASH发送指令，FLASH芯片收到后就会执行相应的操作

而这些指令，对于STM32来说只是使用SPI发送的一些普通数据，但在FLASH设备中就会将特定的数据解释成不同的意义，所以才成为指令

以下是FLASH的常用指令

![1709128804500](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709128804500.png)

> 该表中的第一列为指令名

> 第二列为指令编码，我们通过这个编码将能对FLASH进行一些操作或得到FLASH中的某些数据

> 第三至第N列的具体内容根据指令的不同而具有不同的含义。

​								>> 其中带括号的字节参数，是FLASH在该指令触发时将会返回给主机的，即命令响应，

​								>> 不带括号的则表示是主机向FLASH传输的数据，

​								>> 同时表中“A0~A23”指FLASH芯片内部存储器组织的地址

​								>> “M0~M7”为厂商号

​								>> “ID0-ID15”为FLASH芯片的ID

​								>> “dummy”指该处可谓任意数据

​								>> “D0~D7”为FLASH内部存储矩阵的内容

### 4.定义FLASH指令编码表

> /* FLASH操作指令 */
> #define FLASH_Write_Enable 						0x06			 // 写使能，通常用于启动写操作
> #define FLASH_Write_Disable						0x04			 // 写失能，通常用于禁止写操作
> #define FLASH_Read_Status_Register				 0x05			 // 读取FLASH的状态寄存器																		#define FLASH_Write_Status_Register 			   0x01			 // FLASH的写入数据的状态
> #define FLASH_Read_Data							0x03	 	    // 读取FLASH中（第二字节、第三字节、第四字节）地址中的数据
> #define	FLASH_Fast_Read						 0x0B		     // 
> #define FLASH_Fast_Read_Dual_Output				0x3B			 //
> #define FLASH_Page_Program						 0x02			 // 整片擦粗
> #define FLASH_Block_Erase						  0xD8			 // 块擦除
> #define FLASH_Sector_Erase						 0x20			 // 扇形查出
> #define FLASH_Chip_Erase						   0xC7			 // 
> #define FLASH_Power_DOWN						   0xB9			 // 掉电
> #define FLASH_Release_Power_DOWN				   0xAB			 //
> #define FLASH_Manufacturer						 0x90			 //
> #define FLASH_JEDEC_ID							 0x9F			 // 查看FLASH生产信息，通常用于测试连接状况





**例如**通过指令表中的读ID指令“JEDEC ID”可以获取这两个编号，该指令编码为“9F h”，其中“9Fh”是指 16 进制数“9F”(相当于 C 语言中的 0x9F)。紧跟指令编码的三个字节分别为 FLASH 芯片输出的“(M7-M0)”、“(ID15-ID8)”及“(ID7-ID0)”。我们通过时序表来解释以下这个过程：

![1709129381394](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709129381394.png)

主机首先通过MOSI线向FLASH芯片发送第一个字节数据为“9F h”，当FLASH芯片接收到该数据的时候，将会将其解释成主机向他发送了“JEDEC指令”，然后它就做出该命令的响应，然后通过MISO线将厂商的ID（ID15）以及芯片类型（ID7）发送回给主机

通常来说，这个**“JEDEC指令”通常被用于测试硬件是否连接正常，或用于识别设备**

### 5.读取FLASH芯片ID

```
uint32_t SPI_FLASH_ReadID(void)
	/* 读取FLASH外设的生产厂商以及外设类型，通常用于测试设备连接情况 */
{
	uint32_t Temp=0, Temp1=0, Temp2=0;    			// 预定义接收数据的容器，每个容器将接收一字节的数据
	
	NSS_LOW();																	// 选中SPI外设
	
	SPI_FLASH_SendByte(FLASH_JEDEC_ID);					// 发送查看外设信息指令
	
	Temp = SPI_FLASH_SendByte(value);
	
	Temp1 = SPI_FLASH_SendByte(value);
	
	Temp2 = SPI_FLASH_SendByte(value);
	
	NSS_HIGH();																	// 释放外设连接
	Temp3 = (Temp << 16) | (Temp1 << 8) | Temp2;
	
	return Temp3;
```



### 6.FLASH写使能以及读取当前状态

在向 FLASH 芯片存储矩阵写入数据前，首先要使能写操作，通过“Write Enable”命令即可写使能，

```c
void SPI_FLASH_WriteEnable(void)
/* 向FLASH发送写使能信号 */
{
	NSS_LOW(); 																	// 选中FLASH
	SPI_FLASH_SendByte(FLASH_Write_Enable);			// 发送写使能信号
	NSS_HIGH();																	// 释放选中
	
}
```









与EEPROM一样，由于FLASH芯片向内部存储矩阵写入数据需要消耗一定的时间，并不是在总线通讯结束的一瞬间完成的，因此在写操作后需要确认FLASH芯片“空闲”时才能再次写入。为了表示自己的工作状态，FLASH芯片定义了一个状态寄存器，见下表：

![1709189320885](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709189320885.png)

通常我们只关注状态寄存器的第0位“BUSY”，当这个位为“1”时，表明FLASH芯片处于忙碌状态，它可能正在内部的存储矩阵进行“擦除”或“数据写入”的操作

利用指令表中的“**Read Status Register**”指令就可以获取FLASH芯片状态寄存器的内容，器时序见下图

![1709189469252](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709189469252.png)

只要向FLASH芯片发送了读状态寄存器的指令，FLASH芯片就会持续向主机返回最新状态寄存器内容，直到收到SPI通讯的停止信号。据此我们编写了具有等待FLASH芯片写入结束功能的代码

### 7.读取FLASH芯片的状态寄存器

```c
void SPI_FLASH_WaitForWriteEnd(void)
	/* 通过读取FLASH状态寄存器的内容，并等待BUSY状态结束 */
{	
	NSS_LOW();																				// 选取FLASH设备
	
	SPI_FLASH_SendByte(FLASH_Read_Status_Register)				// 向FLASH发送读取状态寄存器指令
	
	uint8_t FLASH_Status = 0;     								// 预定义接收FLASH信号的容器
	while （(FLASH_Status & WIP_Flag) == SET）
	{
		FLASH_Status = SPI_FLASH_SendByte(value)				// 不断接收数据
		SPITimeout = SPI_FLAG_TIMEOUT;
		if ((SPITTime --) == 0)
		{
			SPI_TIMEOUT_UserCallback(4);
			return ;
		}
	}
	
	NSS_HIGH();
}	
```

这段代码发送读状态寄存器的指令编码“W25X_ReadStatusReg”后，在 while 循环里持续获取寄存器的内容并检验它的“WIP_Flag 标志”(即 BUSY 位)，一直等待到该标志表示写入结束时才退出本函数，以便继续后面与 FLASH 芯片的数据通讯。

### 8.FLASH扇区擦除

由于FLASH存储器的特性决定了它只能把原来为“1”的数据位改写成“0”，而原来的0的数据位并不能直接改写成1，所以这里就涉及到了“**擦除**”的概念，

**在写入之前，必须要对目标存储矩阵进行擦除操作，把矩阵中的数据位擦除位“1”，在写入数据的时候，如果要存储数据“1”就不进行修改存储矩阵，在要存储数据“0”的时候才进行更改**

通常，对存储矩阵擦除的基本操作的单位都是多个字节进行，例如“扇区擦除”、“块擦除”以及“整片擦除”![1709193605257](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709193605257.png)



FLASH 芯片的最小擦除单位为扇区 (Sector)，而一个块 (Block) 包含 16 个扇区，其内部存储矩阵见下图：

![1709193721253](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709193721253.png)



使用扇区擦除指令“Sector Erase”可控制FLASH芯片开始擦除，其指令时序见下图

![1709194907554](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709194907554.png)

扇区擦除指令的第一个字节为指令编码，紧接着发送3个字节用于表示要擦除的存储矩阵地址。**要注意的是在扇区擦除指令前，还需要先发送“写使能”指令，发送扇区擦除指令过后，通过读取寄存器状态等待扇区擦除操作完毕，**

```c
void SPI_FLASH_SectorErase(uint32_t SectorAddr)
{
	SPI_FLASH_WriteEnable();									// 准许写入
	SPI_FLASH_WaitForWriteEnd();								// 等待准许写入命令发送完毕
	
	NSS_LOW();
	
	// 发送扇区擦除指令
	SPI_FLASH_SendByte(FLASH_Sector_Erase);						// 发送擦除信号
	
	/* 注意：这里的扇区地址&0xFF是为了确保扇区地址不被发送缓冲区的值改变，两个大于0的值进行&操作将会保留大的那个值 */ 
	SPI_FLASH_SendByte((SectorAddr & 0xFF) >> 16);				// 发送高位地址，由于是MSB模式，因此先写入的应该是高位的芯片地址
	SPI_FLASH_SendByte((SectorAddr & 0xFF) >> 8);				// 发送中位地址，写入的是存储块地址
	SPI_FLASH_SendByte(SectorAddr & 0xFF);						// 发送低位地址，扇区地址
	SPI_FLASH_WaitForWriteEnd();							   // 等待发送完毕
	NSS_HIGH();
}
```

这里需要注意：

**由于“Sector Erase”指令后要接上3字节的地址，因此意味着要写入24位地址，并且每个地址要右移一个字节，这是由于使用的是MSB模式，高位的地址需要给后面两位的地址保留空间因此需要右移动两个字节**

![1709198453586](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709198453586.png)





### 9.FLASH页写入

目标扇区被擦除完毕过后，就可以向它写入数据了，与EEPROM类似，FLASH芯片也有页写入命令，使用**页写入命令最多可以一次向FLASH传输256个字节的数据**，这个单位就是**页大小**

![1709201587836](D:\typora\typora\bin\1709201587836.png)

从上图可知，第一字节为“页写入指令”编码，2-4字节为要写入的“地址A”,接着就是要写入的内容，最多可以发送256字节数据，这些数据将会从“地址A”开始，按孙旭写入到FLASH的存储矩阵。若发送的数据超出256个，则会覆盖前面发送的数据。

与擦除指令不一样，页写入指令的地址并不要求按256字节对齐，只要确认目标存储单元是擦除状态即可（即被擦除后没有被写入过）。

因此若对“地址x”执行页写入指令后，发送了200个字节数据后终止通讯，下一次再执行页写入指令，将会从“地址（x+200）”开始写入200个字节也是没有问题的，只要发送的数据不超过256个字节即可

在实际应用中由于基本擦除单元是 4KB，一般都以扇区为单位进行读写

```c
void SPI_FLASH_PageWrite(uint8_t * pBuffer, uint32_t WriteAddr, uint16_t NumByteToWrite)
{
	/* 页写入数据至FLASH中，在使用该函数前需要先擦除扇区
	*	：param	pBuffer,要写入数据的指针
	*	：param	WriteAddr，要写入到Flash的地址	
	* ：param NumByteWrite，要写入数据的数量
	*/
	SPI_FLASH_WriteEnable();
	
	NSS_LOW();
	SPI_FLASH_SendByte(FLASH_Page_Program);						// 页发送指令
	SPI_FLASH_SendByte((WriteAddr & 0xFF0000) >> 16);			 // 发送高位地址
	SPI_FLASH_SendByte((WriteAddr & 0xFF00) >> 8);				 // 发送中位地址
	SPI_FLASH_SnedByteWriteAddr & 0xFF);						// 发送低位地址
	
	if (NumByteToWrite > SPI_FLASH_PerWritePageSize)
	{
		while (NumByteToWrite--)
		{
			SPI_FLASH_SendByte(*pBuffer);					   // 使用指针访问每个字节
			pBuffer++;
		}
	}
	NSS_HIGH();
	
	SPI_FLASH_WaitForWriteEnd();								// 等待发送完毕
```







### 10.不定量数据写入

应用的时候我们常常需要写入不定量的数据，直接调用“页写入”函数并不方便，因此我们需要不定量的写入方式

```c
void SPI_FLASH_Buffer_Write(uint8_t * pBuffer, uint32_t WriteAddr, uint16_t NumByteToWrite)
{
	/* 
	*	@brief: 对传入的数据进行写入到FLASH中
	*	@param pBuffer， 要写入的数据
	*	@param WriteAddr, 写入数据的地址
	*	@param NumByteToWrite， 要写入数据的数量
	*/
	uint8_t Num_of_Page=0, Num_of_Single=0,  		Addr=0, 			count=0,	 				temp=0;
	//			需写入的整页数，剩下需写入数量， 写入地址与整页余值，	剩余空间, 整页差值与剩下数量的差变量
	Addr = WriteAddr % SPI_FLASH_PageSize;							// 需要写入的地址与页写入的余值，如果这个值大于0则表明并不在整页范围内，如果等于0即处于页范围
	
	count = SPI_FLASH_PageSize - Addr;							    // 剩余页的空间
	Num_of_Page =  NumByteToWrite / SPI_FLASH_PageSize;				 // 写入为刚好为整页的数量
	Num_of_Single = NumByteToWrite % SPI_FLASH_PageSize;			 // 不满一页剩余需要写入的数量
	
	if (Addr==0)												 // 如果刚好为整页的地址
	{
		if (Num_of_Page==0)										  // 并且如果写入的数据量不足一页
		{
			SPI_FLASH_PageWrite(pBuffer, WriteAddr, NumByteToWrite);
		}
		else
		{
			while (Num_of_Page > 0)
			{
				SPI_FLASH_PageWrite(pBuffer, WriteAddr, SPI_FLASH_PageSize);
				Num_of_Page --;
				pBuffer += SPI_FLASH_PageSize;
				WriteAddr += SPI_FLASH_PageSize;
			}
			if (Num_of_Single > 0)			// 如果还有数据没写完,就继续写
			{
				SPI_FLASH_PageWrite(pBuffer, WriteAddr, Num_of_Single);
			}
		}
	}
	else														 // 如果并不是刚好一整页就继续跟着原先地址写下去
	{
		if (Num_of_Page == 0)				// 如果不足一页
		{
			if (count < Num_of_Single)		// 并且如果剩余空间不能将不足一页的数据写完
			{	
				temp = Num_of_Single - count;
				SPI_FLASH_PageWrite(pBuffer, WriteAddr, count);		// 将剩下的空间写完
				pBuffer += count;
				WriteAddr += count;
			
				SPI_FLASH_PageWrite(pBuffer, WriteAddr, temp);		// 将剩下的写到另一页
			}
			
			else												// 如果写得完就直接写下去
			{
				SPI_FLASH_PageWrite(pBuffer, WriteAddr, NumByteToWrite);
			}
		}
		else													// 如果大于一页
		{
			if (count < Num_of_Single)	// 如果接着的写不完多出来的
			{
				temp = Num_of_Single - count;
				SPI_FLASH_PageWrite(pBuffer, WriteAddr, count);  	// 将剩下的写完
				pBuffer += count;
				WriteAddr += count;
				
				while (Num_of_Page > 0)
				{
					SPI_FLASH_PageWrite(pBuffer, WriteAddr, SPI_FLASH_PageSize);
					pBuffer += SPI_FLASH_PageSize;
					WriteAddr += SPI_FLASH_PageSize;
				}
				SPI_FLASH_PageWrite(pBuffer, WriteAddr, temp);
			}
			else												// 如果能写完
			{
				temp = Num_of_Single - count;			   		   // 计算如果剩余的少于原剩下的空间的话
				SPI_FLASH_PageWrite(pBuffer, WriteAddr, count);		// 将其写完
				pBuffer += count;
				WriteAddr += count;
				while (Num_of_Page > 0)
				{
					SPI_FLASH_PageWrite(pBuffer, WriteAddr, SPI_FLASH_PageSize);
					Num_of_Page --;
					pBuffer += SPI_FLASH_PageSize;
					WriteAddr += SPI_FLASH_PageSize;
					if ((Num_of_Page == 0) & (temp < 0))		   // 如果确实少于
					{
						SPI_FLASH_PageWrite(pBuffer, WriteAddr, (SPI_FLASH_PageSize + temp));// 将不使用多余的空间
					}
				}
			}
		}
	}
}
```

**注意**：在使用上面这两个页写入函数前，一定要先进行擦除操作

#### 为什么一定要进行擦除操作

因为在FLASH存储器中，1可以写成0，0不可以写成1，

例如：前面已经写入了一个0x11，二进制为0001 0001，而我们没有进行擦除操作，再次写入一个0x55，二进制为0101 0101，在进行写入后值并不会改变，依旧会是0x11





### 11.从Flash中读取数据

如果要从FLASH中读取数据，使用读取指令“Read Data”即可，其指令时序见下图

![1709372932429](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709372932429.png)

读取的数量并没有限制，只要没有停止通讯，FLASH芯片就会一直返回数据。

```c
void SPI_FLASH_BufferRead(uint8_t * pBuffer, uint32_t ReadAddr, uint16_t NumByteToRead)
{
	NSS_LOW();
	SPI_FLASH_SendByte(FLASH_Read_Data);
	
	SPI_FLASH_SendByte((ReadAddr & 0xFF) >> 16);
	SPI_FLASH_SendByte((ReadAddr & 0xFF) >> 8);
	SPI_FLASH_SendByte(ReadAddr & 0xFF);
	
	while (NumByteToRead--)
	{
		pBuffer = SPI_FLASH_ReadByte();											// 获取一字节的数据
		pBuffer ++;
	}
	NSS_HIGH();
}
```

















# 串行FLASH文件系统

## 文件系统

管理存储介质的存储内容的方式即为**文件系统**，它是为了存储和管理数据，而在存储介质建立的一种组织结构，这些结构包括操作系统引导区、目录和文件。常见的windows下的文件系统格式包括FAT32、NTFS、exFAT。

在使用文件系统前，要先对存储介质进行格式化。格式化先擦除原来内容，在存储介质上新建一个**文件分配表**和**目录**。这样文件系统就可以记录数据存放的物理地址，剩余空间。

在使用文件系统时，数据都以文件的形式存储，写入新文件时，先在目录中创建一个文件索引，它指示了文件从怒放的物理地址，在把数据存储到对应的地址中，当需要读取数据的时候，可以从目录中找到该文件的索引，进而读取响应的地址中的数据。具体还涉及到逻辑地址、簇大小、不连续存储等一系列辅助结构或者时处理过程

文件系统的存在使我们在存取数据时，不再是简单地像某物理地址直接读写，而是要遵循它的读写格式。如经过逻辑转换，一个完整地文件可能被分开成多端存储到不连续的物理地址，使用目录或链表的方式来获取下一段的位置

上一章的 SPI Flash 芯片驱动只完成了向物理地址写入数据的工作，而根据文件系统格式的逻辑转换部分则需要额外的代码来完成。实质上，这个逻辑转换部分可以理解为当我们需要写入一段数据时，由它来求解向什么物理地址写入数据、以什么格式写入及写入一些原始数据以外的信息(如目录)。这个逻辑转换部分代码我们也习惯称之为文件系统。

简单点说：**文件系统，就是对数据进行管理的方式。使用文件系统可有效的管理存储介质**



文件系统在计算机中的具体表现：

![img](https://img-blog.csdnimg.cn/1c6739c5514b438c85b5f792e90574a4.png)





## FatFs文件系统

FatFs是面向小型嵌入式系统的一种通用的FAT文件系统，它完全是由ANSIC语言编写且完全独立于底层的I/O介质。因此它可以很容易地不加修改地移植到其他的处理器当中，如8051、PIC、AVR、SH、Z80、H8、ARM等。FatFs支持FAT12、FAT16、FAT32等格式，所以我们利用前面写好地SPI FLASH芯片驱动，将FatFs文件系统代码移植到工程之中，就可以利用文件系统的各种函数，对SPI FLASH芯片以“文件”格式进行读写操作了。



例如我们在下面这种磁盘的扇区中建立一个文件，这个文件会有路径，大小，可存大小等数据，在FLASH中是无法直接获取这些信息的，因此我们就需要文件管理系统了，文件管理系统为了存储和管理数据，在存储介质建立了一些组织结构，这些结构包含**操作系统引导区**、**目录**和**文件**



在使用文件系统前，要先对存储介质进行初始化。初始化时将会在存储介质上新建一个文件分配表和目录，





### FatFs的目录结构

#### **文件系统的空间示意图**

![在这里插入图片描述](https://img-blog.csdnimg.cn/7ccd96e0aa664f99b0708b29296b4c08.png)



#### **目录示意图**

目录其实相当于文件系统中的一张表，这张表上存储了文件的相关信息，例如：文件名、从磁盘的那个簇开始，占多大空间，日期时间等信息

例如当我们想要获取A.txt文件中的内容的时候，我们就需要先到目录中查找A.txt文件的信息，如下图中，A.txt文件的开始簇为磁盘的第2个簇，总共占磁盘10个簇，因此我们就可以从磁盘的第2个簇到第11个簇之间读取A.txt文件的内容

![在这里插入图片描述](https://img-blog.csdnimg.cn/d965a0a5a3cf435b920959d69bef9db0.png)



#### **文件分配表**

文件分配表与目录相反，文件分配表记录的是文件存储了什么东西，

例如：下图中所示，磁盘中的第一个簇存储的是目录这张表，而第二个簇内容为3，具体来说就是第二个簇中存储了两个内容，一个是文件的一部分数据，另外的内容是文件下一簇的内容在哪一簇中（类似指针），当发现下一个指向的是FF，就结束读取，这样我们就可以读取一个完整的文件了

具体来说，**文件分配表与目录相配合就可以实现读取文件的作用**，当我们需要读取文件时，首先查阅目录表，然后通过目录表的信息按照文件分配表中的簇地址来读取文件内容以及下一簇的地址，就可以读取一个完整的文件

![在这里插入图片描述](https://img-blog.csdnimg.cn/48ead46c9e68437680cbf04458985528.png)



##### 为什么要使用文件分配表呢，直接使用目录不好嘛

这是因为文件系统经常需要进行删除操作，然后再往删除掉的空间中写入数据，但是如果写入的数据太大，超过了刚删除释放的空间，那么就会将剩余没空间存放的数据存储到另一个空间中，也就是说文件的内容在磁盘中的位置并不是连续的，那么如果我们通过目录中的文件大小以及开始位置进行连续读取的话就会错误的读取文件的内容

![1709432319639](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709432319639.png)

并且通过删除操作过后的文件分配表将会变成如下形式

![img](https://img-blog.csdnimg.cn/d5099440d0ae4e01a080be45d9232479.png)





### 源码目录结构

FatFs文件系统源码文件有doc和src这两个文件夹，其中doc文件夹里是一些使用帮助文件；src才是FatFs文件系统的源码

![1709440094005](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709440094005.png)



#### FatFs源码

![1709440147485](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709440147485.png)

打开src文件夹，可看到如上图的文件目录

**option文件夹**下是一些可选的外部c文件，包含了多语言支持需要用到的文件和转换函数

**diskio.c文件**是FatFs移植最关键的文件，它为文件系统提供了最底层的访问SPI Flash芯片的方法，FatFs有且仅有用于控制SPI Flash芯片相关的函数。

**diskio.h**定义了FatFs用到的宏，以及diskio.c文件内与底层硬件接口相关的函数声明

**00history.txt**介绍了FatFs的版本更新情况

**00readme.txt**说明了当前目录下diskio.c、diskio.h、ff.c、ff.h、integer.h的功能



src文件夹下的源码文件功能简介如下：

* integer.h：文件中包含了一些整数类型定义、
* diskio.c：包含底层存储介质的操作函数，这些函数需要用户自己实现，主要添加底层驱动函数
* ff.c：FatFs核心文件，文件管理的实现方法。该文件独立于底层介质操作文件的函数，利用这些函数实现文件的读写
* cc936.c：本文件在option目录下，是简体中文支持所需要添加的文件，包含了简体中文的GBK和Unicode相互转换功能函数
* ffconf.h：这个头文件包含了对FatFs功能配置的宏定义，通过修改这些宏定义就可以裁剪FatFs的功能。





### C语言中的文件操作![img](https://img-blog.csdnimg.cn/6473877be62c40c2b9ec0a78eac68c82.png)

 我们可以像之前一样，使用重定向，将printf和scanfC库函数重定向到USART。将C语言的文件操作相关API重定向到FLASH或者SD卡，但是C语言标准的文件操作比较繁琐，移植复杂，因此我们移植了FATFS文件系统。







FatFs层级结构如下：



（1）应用层调用FatFs模块接口，FatFs再调用底层接口，实现对USB、SD卡等存储设备的操作。  

（2）需提供RTC时钟给FatFs模块。

![img](https://img-blog.csdnimg.cn/6d99a117d8424b8d9deb76738a49e6db.png)



如图左侧类似C库文件操作函数，属于中间层接口函数，是FATFS提供好的，用于直接调用。

![img](https://img-blog.csdnimg.cn/4aabca3682314e2ab6c66cb34fe2803f.png)



上图左侧为底层接口函数，**应用层通过使用中间层接口函数，从而间接调用底层接口函数**来实现对最底层存储介质的读写操作（如具体读写FLASH的某个扇区）。如使用f_write函数最终会调用disk_write函数。但底层的所有接口函数都需要用户自己实现。

原文链接：https://blog.csdn.net/qq_25218501/article/details/131689290



## FatFs文件系统移植实验（FatFs版本： R0.15a）

### FatFs程序结构图

![1709449247224](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709449247224.png)



用户应用程序需要由用户编写，想实现什么功能就编写什么的程序，一般我们只用到 f_mount()、f_open()、f_write()、f_read() 就可以实现文件的读写操作。

FatFs 组件是 FatFs 的主体，文件都在源码 src 文件夹中，其中 ff.c、ff.h、integer.h 以及 diskio.h 四个文件我们不需要改动，**只需要修改 ffconf.h 和 diskio.c 两个文件**



### FatFs移植步骤

1) 先拷贝一份 SPI Flash 芯片测试的工程文件 (整个文件夹)，并修改文件夹名为“SPI—FatFs 文件系统”。将 FatFs 源码文件夹整个文件夹拷贝一份至“SPI—FatFs 文件系统 USER”文件夹下并修改名为“FATFS”，见图拷贝 FatFs 源码到工程。

![1709456979674](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709456979674.png)



2) 使用 KEIL 软件打开工程文件 (..SPI—FatFs 文件系统 ProjectRVMDK(uv5)\ BH-F407.uvprojx)，并将 FatFs 组件文件添加到工程中，需要添加有 ff.c、diskio.c 、ffsystem.c、ffunicode.c这四个文件，见图添加 FatFS文件到工程。

![1709457128363](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709457128363.png)

3) 添加 FATFS 文件夹到工程的 include 选项中。打开工程选项对话框，选择“C/C++”选项下的“Include Paths”项目，在弹出路径设置对话框中选择添加“FATFS”文件夹，见图添加 FATFS 路径到工程选项。

![1709457158335](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709457158335.png)



4) 如果现在编译工程，可以发现一个diskio.c文件一直提示一些头文件没找到，这是因为diskio.c文件的内容是与底层设备输入输出接口函数文件，不同硬件设计驱动就不同，需要的文件也不同，因此我们需要修改diskio.c文件





### FatFs底层设备驱动函数

FatFs文件系统与底层介质的驱动分开来，对底层介质的操作都要交给用户去实现，他仅仅只是提供了一个函数接口而已，下表为FatFs移植时用户必须支持的函数。通过下表我们可以清晰直到很多函数是在一定条件下才需要添加的，只有前三个函数是必须添加的。我们可以根据实际使用需求来选择实现用到的函数。

前三个函数是实现读文件最基本需求。接下来三个函数是实现创建文件、修改文件需要的。同时为了实现格式化功能，需要在disk_ioctl添加两个获取物理设备信息选项。我们一般只要实现前面6个函数就可以了，已经足够满足大部分功能

![1709460596795](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709460596795.png)

 

### 1.设备状态获取

```c
#define SD_Card  		0		/* SD卡预留位 */
#define FLASH				1		/* 使用FLASH设备 */

/*-----------------------------------------------------------------------*/
/* Get Drive Status                                                      */
/*-----------------------------------------------------------------------*/

DSTATUS disk_status (
	BYTE pdrv		/* Physical drive nmuber to identify the drive */
)
{
	DSTATUS stat;

	switch (pdrv) {
		case FLASH:
			if (sFLASH_ID == SPI_FLASH_ReadID())
			{
				stat &= ~STA_NOINIT;				// 将状态形参设置为非0x01
			}
			else
			{
				stat |= STA_NOINIT;					// 将状态形参设置为0x01，即设备并未准备好
			}
			break;

	default:
		stat = STA_NOINIT;

	
	return stat;
}
```

disk_status函数只有一个参数pdrv，表示物理编号，一般我们使用switch函数实现对pdrv进行分支判断，对于SPI_FLASH设备，我们直接使用SPI_FLASH_ReadID()来判断设备是否连接成功；如果错误，函数将返回异常状态

### 2.设备初始化

由于disk_initialize()函数作为一个底层函数，如果我们直接使用这个函数，那么FatFs文件系统的接口封装就没了意义，因此我们一般不直接使用这个函数，通常使用**f_mount()函数进行代替**

```c
DSTATUS disk_initialize (
	BYTE pdrv				/* Physical drive nmuber to identify the drive */
)
	/* 初始化FatFs文件系统，一般来说这个函数不直接调用 */
{
	DSTATUS stat;

	switch (pdrv) {
	case SD_Card :
		result = RAM_disk_initialize();

		// translate the reslut code here

		return stat;
		break;

	case FLASH :
		SPI_GPIO_Init();
		SPI_Init_Config();
		int i = 500;
		while (--i);
		stat = disk_status()
		
		// translate the reslut code here
		return stat;
		break;
	}
	return STA_NOINIT;
}
```

disk_initialize函数也是有一个参数pdrv，用来指定设备物理编号。对于SPI_FLASH芯片我们调用SPI_GPIO_Init()以及SPI_Init_Config()函数来进行SPI——FLASH芯片引脚GPIO初始化配置以及SPI通信参数配置。使用SPI_FLASH_WAKEUP()函数唤醒SPI——FLASH芯片，当SPI——FLASH芯片处于睡眠模式是需要唤醒芯片才能进行读写操作。

最后我们调用disk_status函数获取SPI_FLASH芯片状态，并返回状态值





### 3.读取扇区

```c
DRESULT disk_read (
	BYTE pdrv,		/* Physical drive nmuber to identify the drive */
	BYTE *buff,		/* Data buffer to store read data */
	LBA_t sector,	/* Start sector in LBA */
	UINT count		/* Number of sectors to read */
)
{
	DRESULT res;								// 是否读取成功

	switch (pdrv) {
	case SD_Card :
		// translate the arguments here
		break;
//		return res;
	
	case FLASH :
		// 读取SPI_FLASH中指定扇区的数据，并且读取的数量由函数传入
		SPI_FLASH_BufferRead((uint8_t *)buff, (uint32_t)(sector*4096), count);

		// translate the reslut code here

		return RES_OK;
	default:
		return RES_PARERR;
	}
	return res;
}
```

disk_read 函数有四个形参。

**pdrv** 为设备物理编号。

**buff** 是一个 BYTE 类型指针变量，buff 指向用来存放读取到数据的存储区首地址。

**sector** 是一个 DWORD 类型变量，指定要读取数据的扇区首地址。

**count** 是一个 UINT 类型变量，指定扇区数量











# FSMC--扩展外部SRAM

## SRAM控制原理

STM32 控制器芯片内部有一定大小的 SRAM 及 FLASH 作为内存和程序存储空间，但当程序较大，内存和程序空间不足时，就需要在 STM32 芯片的外部扩展存储器了。

在一个程序中，我们会有变量，但是由于FLASH为持久性内存，并不容易更改，因此一般会使用**SRAM可以用****来存储变量值**，而代码则存储在FLASH中。



一般来说，当程序所需要使用的内存过大的时候超过了内核本身的两个4KB的SRAM的存储器的空间大小的时候，我们就可以扩展外部SRAM了





扩展内存时一般使用 SRAM 和 SDRAM 存储器，但 **STM32F407 系列的芯片不支持扩展SDRAM**(STM32F429 系列支持)，它**仅支持使用 FSMC 外设扩展 SRAM**，我们以 SRAM 为例讲解如何为 STM32 扩展内存。

给 STM32 芯片扩展内存与给 PC 扩展内存的原理是一样的，只是 PC 上一般以内存条的形式扩展，内存条实质是由多个内存颗粒 (即 SRAM 芯片) 组成的通用标准模块，而 **STM32 直接与 SRAM 芯片连接**。见图一种 SRAM 芯片的内部结构框图 ，这是我们实验板上使用的型号为 IS62WV51216的 SRAM 芯片内部结构框图，以它为模型进行学习。

![1709474171066](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709474171066.png)

![1709474181511](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709474181511.png)

 地址译码器把N根地址线转换成$2^n$根信号线，每根信号线对应一行或一列存储单元，通过地址线找到具体的存储单元，实现寻址。

如果存储矩阵比较大，地址线会分成<u>行或列地址</u>，或者<u>行、列分时复用同一地址总线</u>（即访问数据寻址时先用地址线传输行地址在传输列地址）。

本实例中使用的SRAM比较小，没有列地址线，他的数据宽度为16位，即一个行地址对应2字节的空间（即一行的地址，对应了有16个存储单元，每个存储单元为1bit）



这里我们将$2^{18}$根信号线解读成$2^{18}=512K$行地址，没有列地址，这意味着一个形状为【512K， 1】的列表，每个地址控制一个行单元格，每个单元行的数据宽度为16位，即2字节，我们将一个字节作为一个单元格，即现在的存储单元表的形状为【512K, 2】

这个时候我们通过发送地址，并使用高低为数据输入输出线来将数据存储到这个单元行中，



* 例如我们要往这个存储器的0x000000（这里我们看成访问第一单元行），使用低位输入输出数据线存储0xFE，使用高位输入输出数据线存储0xFF，这样我们的存储器内部应该时这样的：

![1709687954069](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709687954069.png)

而如果我们想要将某一单元行中的某一个字节存储单元格的时候就可以使用到UB#和LB#，当我们想要更改低位字节存储单元格的数据时，LB#掩码数据线输出低电平时，就可以更改低位字节存储单元格中的内容，而高位的并不会受影响

* 又例如：我们想要将0x000001(第二个单元行)，中的高位字节存储单元格中的数据更改为0x44，则我们可以将UB#信号线设置为低电平，然后通过高位数据输入输出信号线将数据存储到单元格中，最后存储单元状态为：

![1709688495131](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709688495131.png)





### SRAM信号线

第一张图SRAM芯片的内部结构框图中左侧引出的是SRAM芯片的控制引脚，其说明见下图：

![1709474274559](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709474274559.png)

SRAM的控制比较简单，只要**控制信号线使能了访问**，**从地址线输入要访问的地址** ，**即可从I/O数据线写入或读出数据**



### 存储器矩阵

以下为存储器阵列，这个SRAM芯片的空间大小为512k \*16（bits） 

![1709474474229](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709474474229.png)

SRAM内部包含的存储阵列，可以把它理解成一张表格，数据就填在这张表格上。和表格查找一样，<u>指定一个行地址和列地址，就可以精确的找到目标单元格</u>，这是SRAM芯片寻址的基本原理。这样的<u>每个单元格</u>被称为**存储单元**，而这样的<u>表</u>则被成为**存储矩阵**



### 地址编码器，列I/O及I/O数据电路

地址译码器把N根地址线转换成$2^n$根信号线，每根信号线对应一行或一列存储单元，通过地址线找到具体的存储单元，实现寻址。

如果存储矩阵比较大，地址线会分成<u>行或列地址</u>，或者<u>行、列分时复用同一地址总线</u>（即访问数据寻址时先用地址线传输行地址在传输列地址）。

本实例中使用的SRAM比较小，没有列地址线，他的数据宽度为16位，即一个行地址对应2字节的空间（即一行的地址，对应了有16个存储单元，每个存储单元为1bit）

而框线中左侧的A0-A18是行址信号，18根地址总线一共可以表示$2^{18}=2^8*1024=512K$行存储单元（即有$2^{18}=512K$行存储空间），因此它一共能访问$512K*16bits$大小的空间

访问时，使用**UB#或LB#线控制数据宽度**，例如，当要访问宽度为16位的数据时，使用行地址线之处地址，然后把UB#和LB#线都设置为低电平，那么I/O0-I/O15线都有效，它们一起输出该行地址的16位数据（或者传输16位数据到该地址）；当要访问宽度为8位的数据时，使用行地址指出地址，然后把UB#和LB#线其中一个设置位低电平，I/O会对应输出该行地址的高8位或低8位的数据，因此它们也被成为数据掩码信号。



### 控制电路

控制电路主要包含了片选、读写使能以及上面提到的宽度控制信号UB#和LB#。利用CS2或CS1#片选信号，可以把多个SRAM芯片组成一个大容量的内存条。OE#和WE#可以控制读写使能，防止误操作





### SRAM读写过程

**读过程**

![1709689247174](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709689247174.png)





**写过程**

![1709536321143](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709536321143.png)



（1）主机使用地址信号线发出要访问的存储器的目标地址

（2）控制片选信号CS1#及CS2#使能存储器芯片，用于选定通信芯片，从而传输到指定的设备中

（3）若是要进行读操作，则控制读使能信号OE#表示要读操作，若进行写操作则控制写使能信号WE#表示要写数据

（4）使用掩码信号LB#与UB#来指示需要写入或读取高位数据或低位数据

（5）如果是读取过程，则存储器会通过数据线来将目标数据传输给主机，若是写入过程，主要使用数据线来将数据传输到存储器的指定地址中。

写操作中的**tSD**时间是主机传输给SRAM，并且数据在SRAM的输入信号线中接收到数据，到主机写使能结束的时间，通常来说我们都需要通过这个时间来给一点时间给SRAM存储器进行读取



在读写时序中，有几个重要的参数的设置，在使用STM32控制时需要参考一下

![1709690662007](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709690662007.png)



## FSMC

FSMC外设用于管理扩展的存储器，FSMC时Flexible Static Memory Controller的缩写，译为灵活的静态存储控制器。它可以用于驱动包含SRAM、NOR FLASHyiji NANDFLASH类型的存储器，不能驱动如SFRAM这种的动态的存储器



一般来说我们可以通常将FSMC与存储器的关系想象成我们要用电脑拷贝东西到U盘中，我们将电脑想象成是存储器，U盘就是STM32，

当单片机与外部存储器通信时，我们就需要直到数据的地址（电脑中的位置），然后把数据的内容传输（复制粘贴）过来，存储器就需要地址传输线和数据传输线，还要加上一些控制时序引脚，比如复位、写数据、读数据等等，而FSMC则就是控制这两者让这两者能够直接通信的接口，



下图为FMSC外设内部结构：

![1709538761142](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709538761142.png)

### 通讯引脚

在框图的右侧是FSMC外设相关的控制引脚，由于控制不同类型存储器的是皇后会有一些不同的引脚，因此这里的FSMC引脚看起来非常多，其中地址线FSMC_A和数据西安FSMC_D是所有控制器共用的，下图为FSMC中的SRAM控制信号线表：

![1709539875712](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709539875712.png)

其中最为特殊的FSMC_NE信号线，它使用于控制SRAM芯片的片选控制信号线，STM32具有FSMC_NE1/2/3/4号引脚，**不同的引脚对应的STM32内部不同的地址区域**，例如，当 STM32 访问 0x6C000000-0x6FFFFFFF 地址空间时，FSMC_NE3 引脚会自动设置为低电平，由于它连接到 SRAM 的 CE# 引脚，所以 SRAM 的片选被使能，而访问 0x60000000-0x63FFFFFF 地址时，FSMC_NE1 会输出低电平。**当使用不同的 FSMC_NE 引脚连接外部存储器时，STM32 访问 SRAM的地址不一样，从而达到控制多块 SRAM 芯片的目的**。



### 存储器控制器

上面不同类型的引脚时连接到FSMC内部对应的存储控制器中的。NOR/PSRAM/SRAM 设备使用相同的控制器，NAND/PC 卡设备使用相同的控制器，**不同的控制器有专用的寄存器用于配置其工作模式**。

控制SRAM的有FSMC_BCR1/2/3/4控制寄存器、FSMC_BTR1/2/3/4片选时序寄存器以及FSMC_BWTR1/2/3/4写时序寄存器。每种寄存器都有4个，分别对应4个不同的存储区域，各种寄存器介绍如下：

* FSMC_BCR控制寄存器可配置要控制的存储器类型、数据线宽度以及信号有效极与性能参数
* FMC_BTR时序寄存器用于配置SRAM访问时的各种时间延迟，如数据保持时间、地址保持时间等
* FMC_BWTR写时序寄存器与FMC_BTR寄存器控制的参数类似，它专门控制写时序的时间参数





### 时钟控制逻辑

FSMC挂载在AHB时钟总线上，时钟信号来自HCLK(默认168MHz)，控制器的同步时钟输出就是由它分频得到的。例如，NOR控制器的FSMC_CLK引脚输出的时钟，它可用于与同步类型的SRAM芯片进行同步通讯，它的时钟频率可通过FSMC_BTR寄存器的CLKDIV为配置，可以配置为HCLK的1/2或1/3，也就是说，若它与同步类型的SRAM通讯时，同步时钟最高频率为84MHz。 





### FSMC的地址映射

FSMC连接号外部的存储器并初始化过后，就可以直接通过访问地址来读写数据，这种地址访问与I2C_EEPROM、SPI_FLASH的不一样，使用FSMC外接存储器时，其**存储单元时映射到STM32的内部寻址空间**中；在程序中，<u>定义一个指向这些地址的指针，然后就可以通过指针直接修改该存储单元的内容，FSMC外设会自动完成数据访问的过程</u>，读写命令之类的操作不需要程序控制。FSMC的地址映射见下图：

![1709543181308](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709543181308.png)

图中左侧的是Cortex-M4内核的存储空间分配，右侧是STM32——FSMC外设的地址映射。可以看到 FSMC 的 NOR/PSRAM/SRAM/NAND FLASH 以及 PC 卡的地址都在 ExternalRAM 地址空间内。

正是因为存在这样的地址映射，使得访问FSMC控制的存储器时，就跟访问STM32的片上外设寄存器一样 (片上外设的地址映射即图中左侧的“Peripheral”区域)。

FSMC把整个External RAM存储区域分成了4个Bank区域，每个256MB，并分配了地址范围以及使用的存储器类型，如 NOR 及 SRAM 存储器只能使用上图中 Bank1 的地址。

再每个Bank的内部又分成了4个小块，每个小块大小为64MB，每个小块有相应的控制引脚用于连接片选信号，每个片选信号控制的是Bank中的4个小块，当需要访问具体得到Bank中的具体块时，就需要先使用地址选定指定Bank块，并使用片选信号来选定读取的是那一块中的数据，

如 FSMC_NE[4:1] 信号线可用于选择 BANK1 内部的 4 小块地址区域，见图 Bank1 内部的小块地址分配 ，当 STM32 访问 0x6C000000-0x6FFFFFFF地址空间时，会访问到 Bank1 的第 4 小块区域，相应的 **FSMC_NE4** 信号线会输出控制信号（这是由于在这个空间地址是由FSMC_NE4信号线进行片选的）。

![1709545408700](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709545408700.png)





###  FSMC控制SRAM的时序

**注意：**这里的“时序”指的是存储器访问操作中的**时间顺序**和**时钟信号**的控制，对于不同类型的存储器，需要使用不同的且特定的时间序列和时钟信号来确保数据的正确性和稳定性。这些时钟序列包括地址和数据的建立时间、保持时间以及时钟的相对时序等



FSMC 外设支持输出多种不同的时序以便于控制不同的存储器，它具有 ABCD 四种模式，下面我们仅针对控制 SRAM 使用的模式 A 进行解读，下图为FSMC模式A的读时序以及写时序

![1709546503258](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709546503258.png)

![1709546505944](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709546505944.png)

当内核发出访问某个指向外部存储器地址时，FSMC外设会根据配置控制信号线产生时序（存储器访问所需对应的信号）访问存储器，上图就是访问外部SRAM时FSMC外设的读写时序。



以读时序为例，该图表示一个存储器操作周期由**地址建立周期**（ADDSET）、**数据建立周期**（DATAST）以及**2个HCLK周期**组成。

在地址建立周期中，(1)地址线发出要访问的地址，(2)数据掩码信号线指示要读取地址的高、低字节部分，(3)接着存储器通过数据信号线把目标数据传输给FSMC，FSMC把它交给内核。



写时序类似，区别就是它的一个存储器操作周期仅由**地址建立周期**（ADDSET）和**数据建立周期**（DATAST）组成，且在数据建立周期期间写(1)使能信号线发送写信号，接着(2)FSMC把数据通过数据线传输到存储器中。







## SRAM时序结构体

控制FSMC使用SRAM存储器时主要是配置时序寄存器以及控制寄存器，利用ST标准库的SRAM时序结构体以及初始化结构体可以很方便地写入参数

![1709547372527](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709547372527.png)

这个结构体成员定义的都是SRAM读写时序中的各项时间参数，这些成员的参数斗鱼FSMC_BTR及FSMC_BWTR寄存器配置对应

成员如下：

### FSMC_AddressSetupTime

本成员设置**地址建立时间**，即FSMC模式A的读时序中的地址建立周期（ADDSET）的值，

它可以被设置为 0-0xF 个 HCLK 周期数，按 STM32 标准库的默认配置，HCLK 的时钟频率为 168MHz，即一个 HCLK 周期为 1/168 微秒。



### FSMC_AddressHoldTime

本成员设置**地址保持时间**

它可以被设置为0-0xF个HCLK周期数。



### FSMC_DataSetupTime

本成员设置**数据建立时间**，即FSMC读写时序图模式A的写时序中的**数据建立周期**（DATAST）值，

它可以被设置为0-0xF个HCLK周期数



### FSMC_BusTurnAroundDuration

本成员设置**总线转换周期**，

在NOR_FLASH存储器中，地址线与数据线可以分时复用，<u>总线转换周期就是值总线在这两种状态间切换所需要的延时</u>，防止冲突。控制其他存储器是这个参数无效，配置为0即可





### FSMC_CLKDivision

本成员用于设置**时钟分频**

它以<u>HCLK时钟作为输入，经过FSMC_CLKDivision分频后输出到FSMC_CLK引脚作为通讯使用的同步时钟</u>。控制其他异步通讯的存储器时这个参数无效，配置为0即可。





### FSMC_DataLatency

本成员设置**数据保持时间**，它表示在<u>读取第一个数据之前要等待的周期数</u>，该周期指同步时钟的周期，本参数仅同步NOR_FLASH类型的存储器，控制其他存储器时，本参数无效





### FSMC_AccessMode

本成员设置**存储器访问模式**，

<u>不同模式下FSMC访问存储器地址时输出得到时序不一样</u>，可选FSMC_AccessMode_A/B/C/D 模式。一般来说控制 SRAM 时使用 A 模式。



这个 FSMC_NORSRAMTimingInitTypeDef 时序结构体配置的延时参数，将作为下一节的 FSMCSRAM 初始化结构体的一个成员。





## SRAM初始化结构体

FSMC_SRAM初始化结构体成员如下图

![1709599232331](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709599232331.png)

这个结构体，除了最后两个成员时上一节的时序配置外，*其他结构体成员的配置*都对应到<u>FSMC_BCR中的寄存器位</u>。其中括号中的是STM32标准定义的宏：

，

### FSMC_Bank

本成员用于选择FSMC映射的存储区域，它的可选参数以及相应的内核地址映射范围见下表

![1709600080443](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709600080443.png)



### FSMC_DataAddressMux

本成员用于设置**地址总线与数据总线是否复用**（*FSMC_DataAddressMux_Enable/Disable*），如果设置地址总线与数据总线复用的话，那么上面的结构体成员FSMC_BusTurnAroundDuration的设置就非常重要了

在控制NOR_FLASH时，可以地址总线与数据总线分时复用，以减少使用STM32信号线的数量



### FSMC_MemoryType

本成员用于设置**要控制的存储器类型**，

它支持控制的存储器类型为SRAM、PSRAM以及NOR_FLASH（FSMC_MemoryType_SRAM/PSRAM/NOr)



### FSMC_MemoryDataWidth

本成员用于设置要**控制的存储器的数据宽度**，可以选择设置成8或16位（FSMC_MemoryDataWidth_8b/16b）



### FSMC_BurstAccessMode

本成员用于设置**是否使用突发访问模式**（FSMC_BurstAccessMode_Enable/Disable）

<u>突发访问模式是指发送一个地址后连续访问多个数据</u>，非突发模式下每访问一个数据都需要输入一个地址，仅在控制同步类型的存储器时才能使用突发模式



### FSMC_AsynchronousWait

本成员用于设置**是否使能在同步传输时使用等待信号**(FSMC_AsynchronousWait_Enable/Disable), 在控制同步类型的NOR或PSRAM时，存储器可以使用FSMC_NWAIT引脚通知STM32需要等待



### FSMC_WaitSignalPolarity

本成员用于设置***当使用同步传输时等待信号的有效极性**，即要求等待时，使用高电平或者是低电平（FSMC_WaitSignalPolarity_High/Low）



### FSMC_WrapMode

本成员用于设置**是否支持把非对齐的AHB突发操作分割称2次线性操作**（FSMC_WrapMode_Enable/Disable），该配置尽在突发模式下有效



### FSMC_WaitSignalActive

本成员用于配置在突发传输模式时，决定存储器是在等待状态之前的一个数据周期有效还是在等待状态期间有效（FSMC_WaitSignalActive_BeforeWaitState/DuringWaitState）



！问题：这个存储器在哪个期间有效到底指的是什么有效

​	

### FSMC_WriteOperation

这个成员用于设置**是否写使能**（FSMC_WriteOperation_Enable/Disable），<u>禁止写使能的话FSMC只能从存储器中读取数据，不能写入</u>



### FSMC_WaitSignal

本成员用于设置**当存储器处于突发传输模式时，是否允许通过NWAIT信号插入等待状态**（FSMC_WaitSignal_Enable/Disable）



### FSMC_ExtendedMode

本成员用于设置**是否使用扩展模式**（FSMC_ExtendedMode_Enable/Disable），

在非扩展模式下，对存储器读写的读写时序都只能使用FSMC_BCR寄存器中的配置，即下面的FSMC_ReadWriteTimingStruct结构体成员；

在扩展模式下，对存储器的读写时序可以分开配置，读时序使用FSMC_BCR寄存器，写时序使用FSMC_BWTR寄存器的配置，即下面的FSMC_WrriteTimingStruct结构体



### FSMC_ReadWriteTimingStruct

本成员是一个指针，赋值时使用该成员所在的结构体FSMC_NORSRAMInitTypeDef中的设置

* 当不使用扩展模式时，读写时序都使用本成员的参数配置。

* 当使用扩展模式时，该成员用于配置读时序信号



### FSMC_WriteTimingStruct

同样的，本成员也是一个时序结构体的指针，只有在使用扩展模式的时候，本配置才有效，它是写操作使用的时序



**对本结构体赋值完毕过后，调用FSMC_NORSRAMInit库函数接口把配置的参数写入到FSMC_BCR及FSMC_BTR/BWTR寄存器中**





## FSMC--扩展外部SRAM实验

本小节以型号为“IS62WV51216”的SRAM芯片为STM32扩展内存。它的**地址宽度**为19位，（地址宽度决定了可寻址的存储单元数量，通常使用2的次幂来表示，比如地址线的宽度位n，则可寻址的存储单元数量为2的n次方，这里我们使用的芯片的地址宽度为19位，即总共能存储$2^{19}$个存储单元）**数据线宽度**为16位，**容量大小**为1MB

#### 问题：这个地址宽度指的是什么？

地址线宽度通常可以表示为行地址线的数量与列地址线的数量的乘积和。例如，如果一个SRAM有8根地址线用于行地址，8根地址线用于列地址，那么这个SRAM的地址线宽度就是8乘以8，即64。这表示了该SRAM可以寻址的存储单元数量，也决定了它的存储容量。

#### 问题：这个数据线宽度指的是什么

数据线的宽度指的是每个存储单元中可以存储的数据位数。数据线宽度决定了每次读取或写入操作中可以传输的数据量例如，如果一个SRAM的数据线宽度为8位（1字节），那么每个存储单元可以存储8位的数据，即一个字节。这意味着每次从这个SRAM读取或写入数据时，都会以字节为单位进行操作。数据线宽度的大小影响了SRAM的数据处理能力和带宽，较大的数据线宽度通常可以提高数据传输速度和效率。



### 硬件设计

![1709603563264](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709603563264.png)

![1709603568667](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709603568667.png)



根据本硬件设计，SRAM芯片的片选使能信号与FSMC_NE4连接，所以它会被映射到STM32的BANK1 NOR/SRAM4区域，该区域的地址范围为 0x6C000000-0x6FFFFFFF，当该内核访问从基地址0x6C000000开始的1MB空间时，FSMC外设将会自动控制原理图中的引脚产生访问时序，以此来访问这个外部SRAM存储器

由于FSMC是直接存在在微控制器中的，相当于一个扩展系统，因此如果我们用来扩展SRAM时，只需要像直接连接SRAM存储器一样，直接初始化GPIO接口，FSMC的信号线便会通过这些接口与SRAM进行相连，而并不需要初始化FSMC的引脚



这个函数的执行流程如下：

（1）初始化GPIO引脚以及FSMC时钟

（2）时序结构体赋值

对时序结构体 FSMC_NORSRAMTimingInitTypeDef 赋值。在这个时序结构体配置中，由于我们要控制的是 SRAM，所以选择 FSMC 为模式 A，在该模式下配置 FSMC 的控制时序结构体中，<u>实际上只有地址建立时间 FSMC_AddressSetupTime（即 ADDSET 的值）以及数据建立时间FSMC_DataSetupTime（即 DATAST 的值）成员的配置值是有效的，其它 SRAM 没使用到的成员值全配置为 0</u> 即可。而且，这些成员值使用的单位为：1 个 HCLK 的时钟周期，而 HCLK 的时钟频率为 168MHz，对应每个时钟周期为 1/168 微秒。



## 读取数据时的时序

通过下图我们可以总结出部分公式：

[SRAM时序配置文章原文出处](https://blog.csdn.net/Mr_Fuuuuuu/article/details/109743381?ops_request_misc=%257B%2522request%255Fid%2522%253A%2522170969623316800180663450%2522%252C%2522scm%2522%253A%252220140713.130102334.pc%255Fall.%2522%257D&request_id=170969623316800180663450&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~first_rank_ecpm_v1~rank_v31_ecpm-2-109743381-null-null.142^v99^pc_search_result_base1&utm_term=FSMC%E6%89%A9%E5%B1%95SRAM%E7%9A%84%E8%AF%BB%E5%86%99%E6%97%B6%E5%BA%8F%E9%85%8D%E7%BD%AE&spm=1018.2226.3001.4187)



### FSMC在读取信号时的时序（FSMC读取数据）

![1709698626945](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709698626945.png)

![1709698639359](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709698639359.png)

通过上图我们可以知道FSMC在读取通讯过程为：

（1）主机（内核）通过A(地址线)发送要读取的存储器映射地址，以及其他控制信号

（2）经过（ADDSET+1）个周期过后，RD非信号发生负跳变

（3）再经过（DATASET+1）个HCLK时钟后，确保了数据准备好过后，FSMC才从数据总线上读取数据

（4）再经过2个HCLK时钟周期过后，确保FSMC已经读取好数据过后，RD非信号线才跳变为高电平

根据上面FSMC配置表达式的配置要求，并且将时间单位$16Mhz=1/(168*10^6)=1000/168ns$(即1000/168纳秒)代入，可求得ADDSET=0(由于ADDSET的最低要求为0因此可设置为0)，DATAST=8($DATAST=8*1000/168ns≈47ns$)可符合要求，即：

$t_{RC} = ADDSET+1+DATAST+1+2=(0+1+8+1+2)*1000/168=72.4ns>55ns$

$t_{DOE}=DATAST+1=(8+1)*1000/168=53.5>25ns$

其实这个配置相当冗余，可确保访问正常，但会导致访问速度变慢，可根据实际需要进行测试调整，保证访问正常的情况下可提高访问速度。

但需要注意：本实验中配置的FSMC读时序与SRAM写时序是一致的，需要在确保速度的同时，两者的通讯时序要求都符合要求



## 写入数据时的时序

![1709699160255](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709699160255.png)

![1709699166079](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709699166079.png)

FSMC写入时序过程：

（1）FSMC开始将地址传输到ADDRESS信号线，在传输之前可等待或不等待地址建立时间

（2）NE4片选信号线低电平片选SRAM，

（3）写使能信号线在(ADDSET+1)个HCLK周期过后，将数据保持DATAST+1个周期，此时SRAM接收数据并开始存储到指定地址中

（4）将所有信号线的电平状态复原，写入完毕



SRAM写入时序过程：

（1）等待（ADDSET+1）> 0 ns的地址建立时间，待到片选信号线片选过后开始接收地址

（2）继续等待（DATAST+1）个周期的接收写使能信号

（3）接收到后开始存储数据

（4）存取完毕过后传输完毕

根据上面的公式以及配置要求并且带入时间单位，我们可以计算出ADDSET=0,DATAST=8时即可符合要求，如：

$t_{WC}=ADDSET+1+DATAST+1=(0+1+8+1)*1000/168=59.5>55ns$

$t_{PWB}=DATAST+1=(8+1)*1000/168=53.5>40ns$

将计算得到的参数赋值到时序结构体中的FSMC_AddressSetupTime(即ADDSET的值)及FSMC_DataSetipTime(即DATAST的值)中，然后再把时序结构体作为指针赋值到小面的初始化结构体中共，作为读写的时序参数，最后调用FSMC_NORSRAMInit函数即可把参数写入到相应的寄存器中。





完成配置过后我们就可以利用他进行存储数据了，由于SRAM的存储空间是被映射到内核的寻址区域的，我们可以通过映射的地址直接访问SRAM，访问这些地址是，FSMC外设自动读写SRAM。









# LCD-液晶显示

[LCD显示原理](https://www.bilibili.com/video/BV1Sb411W7Zf/?spm_id_from=333.337.search-card.all.click&vd_source=b6ec906023fa46a5d781f4c754d9bf34)

**液晶**是<u>一种介于固体和液体之间的特殊物质</u>，它是一种有机化合物，常态下呈液态，但是它的分子排列却和固体晶体一样非常规则，因此取名液晶。<u>如果给液晶施加电场，会改变它的分子排列，从而改变光线的传播方向，配合偏振光片，它就具有控制光线透过率的作用</u>，再<u>配合彩色滤光片，改变加给液晶电压大小，就能改变某一颜色透光量的多少</u>， 下图中的就是绿色显示结构。利用这种原理，做出可控红、绿、蓝光输出强度的显示结构，把三种显示结构组成一个显示单位，通过控制红绿蓝的强度，可以使该单位混合输出不同的色彩，这样的一个显示单位被称为像素。

![1709737860409](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709737860409.png)

### LED和OLED显示器

LED点阵显示器不存在以上液晶显示器的问题，LED点阵彩色显示器的单个像素点内包含红绿蓝三色LED灯，显示原理类似于上面的LED彩灯，通过控制红绿蓝颜色的强度进行混色，实现全彩颜色输出，多个像素点构成一个屏幕。

[LED发光原理](https://www.bilibili.com/video/BV1EH4y1z7mL/?spm_id_from=333.788&vd_source=b6ec906023fa46a5d781f4c754d9bf34)

LED其实就是一块二极管，这块二极管的P区与N区的空穴与电子进行**本征激发**，并且在空穴与电子的结合过程中会消耗部分能量，这部分能量便会使得二极管的部分P区发光，同时由于自由电子总是与空穴进行复合，因此这个本征激发将会一直持续，并且在不同材料下发出的光的波长也会不一样

![1709771424052](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709771424052.png)

**定色的LED的结构**： 最底下的是基板，中间层为N型半导体，最顶上的为P型半导体，其中P型半导体的厚度最薄，这是为了能够更好的透光

![1709771481794](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709771481794.png)

**问题：为什么LED中的P型半导体的厚度要最薄？**

LED中的P型半导体的厚度要尽量薄是因为减小P型半导体的厚度可以缩短电子和空穴再结合的距离，从而提高光子发射的效率。当电子和空穴相遇时，它们会发生复合并释放出能量，产生光子。通过减小P型半导体的厚度，可以减少电子和空穴再结合的机会，提高光子的发射效率。





**RGB二极管结构：**

RGB二极管的机构与上面的单色二极管的原理差不多，但是具体的P区半导体使用了三对不同的半导体，用来产生红绿蓝三种颜色，每对半导体都有独立的控制器来控制其输入电压，以此来控制颜色的亮度，不同的颜色混合到一起就能获得很多的颜色

![1709771980446](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709771980446.png)





新一代的OLED显示器与LED点阵彩色显示器的原理类似，但由于它采用的像素单元是“有机发光二极管”(Organic Light Emitting Diode)，所以像素密度比普通的LED点阵显示器高得多，见下图为OLED的像素结构

![1709738176495](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709738176495.png)



### 显示器的基本参数

(1) 像素
像素是组成图像的最基本单元要素，显示器的**像素**指它<u>成像最小的点</u>，即前面讲解液晶原理中提到的一个显示单元。
(2) 分辨率
一些嵌入式设备的显示器常常以“行像素值 x 列像素值”表示屏幕的分辨率。如<u>分辨率 800x480 表示该显示器的每一行有 800 个像素点，每一列有 480 个像素点</u>，也可理解为有 800 列，480 行。
(3) 色彩深度
色彩深度指显示器的每个**像素点能表示多少种颜色**，一般用“位”(bit) 来表示。如单色屏的每个像素点能表示亮或灭两种状态 (即实际上能显示 2 种颜色)，用 1 个数据位就可以表示像素点的所有状态，所以它的色彩深度为 1bit，其它常见的显示屏色深为16bit、24bit。
(4) 显示器尺寸
显示器的大小一般以英寸表示，如 5 英寸、21 英寸、24 英寸等，这个长度是指**屏幕对角线的长度**，通过显示器的对角线长度及长宽比可确定显示器的实际长宽尺寸。
(5) 点距
**点距指两个相邻像素点之间的距离**，它会影响画质的细腻度及观看距离，相同尺寸的屏幕，若分辨率越高，则点距越小，画质越细腻。如现在有些手机的屏幕分辨率比电脑显示器的还大，这是手机屏幕点距小的原因；LED 点阵显示屏的点距一般都比较大，所以适合远距离观看。





## 液晶控制原理

![1709774626248](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709774626248.png)

上面这个完整的显示屏由液晶显示面板、电容触摸面板以及PCB底板构成。上图中的触摸面板带有触摸控制芯片，<u>该芯片处理触摸信号并通过引出的信号线与外部器件通讯</u>，触摸面板中间是透明的，它贴在液晶面板上，一起构成屏幕的主体，触摸面板与液晶面板引出的排线连接到PCB底板上，根据实际需要，PCB地板上可能带有“液晶控制芯片”，例如上图右侧的液晶控制器，<u>因为控制液晶面板需要比较多的资源，所以大部分低级微控制器都不能直接控制液晶面板，需要额外配套一个专用液晶控制器来处理显示过程</u>，外部微控制器只要把它希望显示的数据直接交给液晶控制器即可。

而不带液晶控制器的 PCB 底板，只有小部分的电源管理电路，液晶面板的信号线与外部微控制器相连，直接控制。



STM32F429 系列的芯片不需要额外的液晶控制器，也就是说它把专用液晶控制器的功能集成到 STM32F429 芯片内部了，可以理解为电脑的 CPU 集成显卡，它节约了额外的控制器成本。

而 STM32F407 系列的芯片由于没有集成液晶控制器到芯片内部，所以它只能驱动自带控制器的屏幕，可以理解为电脑的外置显卡。

总的来说，这两类屏幕的控制框图如下图所示：

![1709779259249](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709779259249.png)

### 液晶面板的控制信号

![1709779446847](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709779446847.png)

液晶面板的控制信号线即上图中液晶显示面板引出的FPC排线，其说明如下

**注意：**这是不带液晶控制器时的液晶控制原理，这与我们的STM32F407有区别，因为STM32F407的控制的液晶屏是带有液晶控制器，因此这里只是介绍其基本通讯时序原理，方便后面的液晶控制器的理解

![1709779425009](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709779425009.png)

#### 1.RGB信号线

RGB信号线各8根线，分别用于表示液晶屏一个像素点的红、绿、蓝颜色分量。常见的颜色表示会在<u>“RGB”后面附带各个颜色分量值的数据位数</u>，<u>数据线的位数决定了能表示多少种颜色</u>，如 RGB565 表示红绿蓝的数据线数分别为 5、6、5 根，一共为 16 个数据位，可表示 216 种颜色；而这个液晶屏的种颜色分量的数据线都有 8 根，所以它支持 RGB888 格式，一共 24 位数据线，可表示的颜色为 224 种。

通常来说位数越多，能表示的色彩也就越多，色彩更鲜艳并且色准度更高



#### 2.同步时钟信号线CLK

液晶屏与外部使用同步的通讯方式，以CLK信号作为同步时钟，在同步时钟的驱动下，**每一个时钟周期传输一个像素点数据**



#### 3.水平同步信号HSYNC

水平同步信号HSYNC(Horizontal Sync)用于表示**液晶屏一行像素数据的传输结束**，每传输玩液晶屏的一行像素数据时，HSYNC会发生电平跳变，如分辨率为800*480，即有800列、480行个像素，每传输一帧的图像HSYNC的电平将会跳变480次



#### 4.垂直同步信号VSYNC

垂直同步信号VSYNC(Vertical Sync)用于表示液晶屏一帧像素数据的传输结束，每传输完一帧像素数据时，VSYNC会发生一次电平跳变。其中**“帧”是图像的单位**，<u>一幅图像称之为一帧</u>，在液晶屏中，一帧指一个完整屏液晶像素点。人们常常用<u>“帧/秒”来表示液晶屏的刷新特性</u>，即**液晶屏每秒可以显示多少帧图像**，如液晶屏的显示频率为60帧/秒，则表示液晶显示屏的每秒能显示60张不同的图像，VSYNC每秒电平将会跳变60次



#### 5.数据使能信号DE

数据使能信号DE（Data Enable）用于表示数据的有效性，当DE信号线为高电平时，RGB数据信号线表示的数据有效





### 液晶数据传输时序

通过上面的信号线像液晶屏传输像素数据时，各信号线的时序见下图，图中表示的是向液晶屏传输一帧图像数据的时序，中间省略了多行及多个像素点

![1709782160731](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709782160731.png)

![在这里插入图片描述](https://img-blog.csdnimg.cn/ca91714dff434076b50ed464f269cc82.png?x-oss-process=image/watermark,type_d3F5LXplbmhlaQ,shadow_50,text_Q1NETiBAVGFuZ1poZW55ZQ==,size_20,color_FFFFFF,t_70,g_se,x_16)

液晶屏显示的图像可看作一个举行，结合下图理解。液晶屏有一个显示指针，它指向将要显示的像素。显示指针的扫描方向从左到右、从上到下，通过像素显示指针将像素显示成指定地像素值亮度，一个像素点一个像素点地描绘图形。这些像素点的数据通过RGB数据线传输到液晶屏，它们在同步时钟CLK的驱动下一个一个地传输到液晶屏中，交给显示指针，传输完成一行过后，HSYNC电平跳变一次，而传输完一帧时VSYNC电平跳变一次

![1709792351982](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709792351982.png)

但是

液晶显示指针在行与行进行，帧与帧之间切换需要延时，而且HSYNC及VSYNC信号本身也有宽度，就会产生一下的时间参数

![1709792546111](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709792546111.png)

其中的VBP、VFP、HBP以及HFP这些时间参数都是在数据帧指针跳转位置时的延迟。

在这些时间参数控制的区域，数据使能信号线“DE”都为低电平，RGB数据线的信号无效，当“DE”为高电平时，表示的数据有效，传输的数据会直接影响液晶屏的显示区域。



### 显存

液晶屏中的每个像素点都是数据，在实际应用中需要吧每个像素点的数据缓存起来，再传输给液晶屏，一般会使用SRAM或SDRAM性质的存储器，而这些专门用于<u>存储显示数据的存储器</u>，则被称之为**显存**。显存一般至少要存储液晶屏的一帧显示数据，如分辨率为800\*480的液晶屏，使用RGB888格式（每种颜色可显示$2^8$种颜色）显示，它的一帧显示数据大小为：$3*800*480=1152000$字节；若使用RGB565格式显示，一帧显示的数据大小为：$2*800*450=768000$字节。

一般来说，外置的液晶控制器会自带显存，而像 STM32F429 等集成液晶控制器的芯片可使用内部 SRAM 或外扩 SDRAM 用于显存空间。



## 野火4.5寸液晶屏简介

![1709793763506](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709793763506.png)

图中标号1部分就是液晶屏幕的整体，通过引出的排线或FPC引线接入到实验板上可对它进行控制，它分为标号2的液晶触摸面板与PCB底板两部分。

标号3种的液晶触摸面板由液晶屏和触摸面板组成，触摸面板和液晶面板是两个独立的部件，生产厂商将这两个部件贴在了一起，形成了整体。

触摸面板引出的信号线引入到了GT5688**触摸芯片**，<u>该芯片会对触摸信号进行采集并简化信号输出给外部控制器</u>

触摸面板的下方即为液晶面板，在它的内部包含了一个型号为ILI9806G的液晶控制芯片，该液晶控制器使用8080接口与单片机通讯，图中液晶面板引出的FPC信号线及8080接口（RGB接口已在内部直接与ILI9806G相连），且控制器种包含由显存，<u>单片机把要显示的数据通过引出的8080接口发送到液晶控制器，这些数据会被存储到它内部的显存中，然后液晶控制器不断把显存的内容刷新到液晶面板，显示内容</u>

其中标号2处的是PCB底板，它主要包含了液晶屏的电源控制电路，另外，PCB底板与液晶触摸面板通过FPC排线座连接，然后引出到排线，方便与实验班的排母连接，也可以使用FPC接口与核心板连接



### ILI9806G液晶控制器简介

![1709798261435](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709798261435.png)

本液晶屏内部包含一个液晶控制芯片ILI9806G，它的内部结构非常复杂，如上图，并且该芯片最主核心部分是位于中间的GRAM(Graphics RAM)，它就是显存。GRAM中每个存储单元都对应着液晶面板的一个像素点，并且这个GRAM的显存为480\*24\*860，我们使用的液晶显示屏的分辨率为800\*480，色彩格式为RGB565，共需要$800*480*2(16bit)=768000B$的显存才可以驱动这块液晶显示屏，可见这块液晶控制芯片的显存是冗余的。



**框图的左上角的标号1处**为 ILI9806G 的主要控制信号线和配置引脚，根据其不同状态设置可以使芯片工作在不同的模式，如每个像素点的位数是 8、16 还是 24 位；可配置使用 SPI 接口、8080 接口还是RGB 接口与 MCU 进行通讯。MCU 通过 SPI、8080 接口或 RGB 接口与 ILI9806G 进行通讯，从而访问它的控制寄存器 (CR)、地址计数器 (AC)、及 GRAM。

**在 GRAM 的上侧的标号4处**还有一个 LED 控制器 (LED Controller)。LCD 为非发光性的显示装置，它需要借助背光源才能达到显示功能，LED 控制器就是用来控制液晶屏中的 LED 背光源。





### 液晶屏控制器的信号线及8080时序

ILI9806G控制器根据自身的IM[3:0]信号线电平决定它与MCU的通讯方式，它本身支持SPI及8080通讯方式，本示例中液晶屏的 ILI9806G 控制器在出厂前就已经按固定配置好 (内部已连接硬件电路)，它被配置为<u>通过 8080 接口通讯</u>，使用 16 根数据线的 RGB565 格式，且<u>背光 LED 引脚不与 ILI9806G 相连，而是直接引出到排针供外部控制器控制</u>。内部硬件电路连接完，剩下的其
它信号线被引出到 FPC 排线，最后该排线由 PCB 底板引出到排针，排针再与实验板上的 STM32芯片连接，引出的排针信号线见下图：

![1709799795584](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709799795584.png)

**简言之就是ILI9806G液晶控制器的输入信号应该是8080时序的，所以我们后面将使用FSMC扩展NOR_FLASH的方式来模拟8080时序给ILI9806G液晶控制器发送数据等信号，然后ILI9806G将会将收到的数据信号存储到自身的GRAM中，然后再在控制液晶屏来显示图像**

信号线说明：![1709799807741](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709799807741.png)



这些信号即8080通讯接口，带X的表示低电平有效，STM32通过该接口与ILI9806G芯片进行通讯，实现对液晶屏的控制。

通讯的内容主要包含命令和显存数据，显存数据即各个像素点的RGB565；命令是对ILI9806G的控制指令，MCU可通过8080接口发送命令编码控制ILI9806G的工作方式，

例如复位指令、设置光标指令、睡眠模式指令等，具体的指令在《ILI9806G-Data Sheet.pdf》数据手册均有详细说明。



写命令时序见下图：

![1709800366440](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709800366440.png)

由图可知，一切的开始都是在**片选信号线CSX**拉低开始，对<u>数据/命令选择线D/CX也置低电平表示写入的是命令地址</u>（可以理解为命令编码，如软件复位命令：0x01），以<u>写信号WRX为低，读信号RDX为高表示数据传输方向为写入</u>，同时，在数据线D[23:0\](或D[15:0])输入命令地址，在第二个传输阶段传送的是命令的参数，多以D/CX要置高电平，表示写入的是命令数据，命令数据是某些指令带有的参数，例如复位指令编码0x01，它后面还可以带一个参数，该参数表示多少秒后复位（注：这个是例子，复位指令并没有参数）



当需要把像素数据写入GRAM时，过程很类似，把片选信号CSX拉低后，再把数据/命令选择信号线D/CX置高电平，这是通过D[23:0\](使用RGB888模式才使用)或D[15:0\](使用RGB565模式使用)传输的数据则会倍ILI9806G保存到它的GRAM中





## 使用STM32的FSMC模拟8080接口时序

ILI9806G 的 8080 通讯接口时序可以由 STM32 使用普通 I/O 接口进行模拟，但这样效率太低，STM32 提供了一种特别的控制方法——使用 FSMC 接口实现 8080 时序。



### FSMC简介

前面已经学习过如何使用FSMC外设来控制扩展的外部存储器，而MCU对液晶屏的操作实际上就是把显示数据写入到显存中，与控制存储器的操作非常类似，且8080接口的通讯时序完全可以使用FSMC外设产生，因而非常适合使用FSMC控制液晶屏



下图为FSMC外设结构图  

![1709802479926](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709802479926.png)

控制LCD时，是使用FSMC的NORPSRAM模式的，且与前面使用FSMC控制SRAM的稍有不同，控制SRAM时使用的时模式A，而控制LCD时使用的是与NOR_FLASH一样的模式B，因此我们使用控制NOR_FLASH使用到的信号线：

![1709802747565](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709802747565.png)

在控制LCD时，使用的是类似异步、地址与数据线独立的NOR_FLASH控制方式，所以实际上CLK\NWAIT、NADV引脚并没有使用到。

FSMC_NOR/PSRAM中的模式B的写时序见下图：

![1709802866399](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709802866399.png)

根据STM32对寻址空间的地址映射，地址0x6000 0000 ~ 0x9FFF FFFF是映射到外部存储器的，而其中的0x6000 0000~0x6FFF FFFF则是分配给NOR_FLASH这类可直接寻址的器件。

当FSMC外设倍配置成正常工作，并且外部接了NOR_FLASH时，若向0x6000 0000地址写入数据如0xABCD，FSMC会自动在各信号线上产生相应得电平信号，写入数据。FSMC会控制片选信号NE1选择相应得NOR芯片，然后使用A[25:0]输出0x6000 0000，在NWE写使能信号线上发出低电平的写使能信号，而要写入的数据信号0xABCD则从数据线D[15:0]输出，然后数据就能被保存到NOR_FLASH中了。



### 用FSMC模拟8080时序

!	1709803862094](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709803862094.png)

见上图中我们可以发现FSMC_NOR/PSRAM中的模式B时序与ILI9806G液晶控制器芯片使用的8080时序是十分相似的（除了FSMC的地址线A和8080的D/CX线，可以说是完全一样）

![1709807053598](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709807053598.png)

对于FSMC和8080接口，前4种信号线都是完全一样的，仅仅是FSMC的地址信号线A[25:0]与8080的数据/命令选择线D/CX有区别，而对于D/CX线，它为高电平时表示数值，为低电平的时候表示命令，如果能使用FSMC的A地址线根据不同的情况产生相应的电平，那么就完全可以使用FSMC来产生8080接口需要的时序了。

为了模拟出8080时序，我们可以吧FSMC的A0地址线（也可以使用其他A1/A2等地址线）与ILI9806G芯片8080接口的D/CX信号线连接，那么当A0为高电平时（即D/CX为高电平），那么数据线D[15:0]的信号会被ILI9806G理解为数值，若A0为低电平（即D/CX为低电平），传输的的信号则会被理解成命令；



由于FSMC会自动产生地址信号，当使用FSMC向0x6xxx xxx1、0x6xxx xxx3、0x6xxx xxx5...这些奇数地址写入数据时，地址最低为的值均为1，所以它会控制地址线A0(D/CX)输出高电平，那么这时通过数据线传输的信号会被理解为数值；若向0x6xxx xxx0、0x6xxx xxx2、0x6xxx xxx4...这些偶数地址写入数据时，地址最低为的值均为0，所以它会控制地址线A0(D/CX)输出低电平，因此这时通过数据线传输的信号会被理解为命令，见下图：

![1709814619154](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709814619154.png)

有了这个基础，只要配置好FSMC外设，然后在代码中利用指针变量，向不同的地址单元写入数据，就能够由FSMC模拟出8080接口向ILI9806G写入控制命令或GRAM的数据了



## NOR FLASH时序结构体

与控制SRAM时一样，控制FSMC使用NOR FLASH存储器时主要配置的是**时序寄存器**以及**控制寄存器**，利用ST标准库中的时序结构体以及初始化结构体可以很方便地写入参数



NOR FLASH时序结构体的成员见下图：

![1709815081485](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709815081485.png)

这个结构体与SRAM中的时序结构体完全一样，以下仅列出控制NOR FLASH时使用模型B用到的结构体成员：



### （1）FSMC_AddressSetupTime

本成员设置**地址建立时间**，即FSMC读写时序图中的ADDSET值，它可以被设置为0-0xF个HCLK周期数，按STM32标准库的默认配置，HCLK的时钟频率为168Mhz，即一个HCLK周期为1/168微秒

### （2）FSMC_DataSetupTime

本成员设置**数据建立时间**，即FSMC读写时序图中的DATAST值，它可以被设置为0-0xF个HCLK周期数

### （3）FSMC_AccessMode

本成员设置**存储器访问模式**，不同的模式下FSMC访问存储器地址引脚输出时序不一样，可选FSMC_AccessMode_A/B/C/D模式，控制异步NOR FLASH时使用B模式



## FSMC初始化结构体

![1709815594833](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709815594833.png)

![1709815607045](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709815607045.png)

这部分内容与FSMC控制SRAM时完全一致，以下是NOR FLASH模式B相关的结构体成员：

### FSMC_Bank

本成员用于选择FSMC映射的存储区域，它的可选参数以及相应的内核地址映射范围见下表

![1709815769769](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709815769769.png)

### FSMC_MemoryType

本成员用于设置**要控制的存储器类型**，它支持控制的存储器类型为SRAM、PSRAM以及NOR FLASH（FSMC_MemoryType_SRAM/PSRAM?NOR）

### FSMC_MemoryDataWidth

本成员用于设置要控制的存储器的数据宽度，可选择设置成8位或16位（FSMC_MemoryDataWidth_8b/16b）

### FSMC_WriteOperation

这个成员用于设置**是否写使能**（FSMC_WriteOperation_Enable/Disable），禁止写使能的话FSMC只能从存储器中读取数据，不能写入。

### FSMC_ExtendedMode

本成员用于设置**是否使用扩展模式**（FSMC_ExtendedMode_Enable/Disable），在非扩展模式下，对存储器读写的时序都只使用FSMC_BCR寄存器中的配置，即下面的FSMC_ReadWriteTimingStruct结构体成员；在扩展模式下，对存储器的读写时序可以分开配置，读时序使用FSMC_BCR寄存器，写时序使用FSMC_BWTR寄存器的配置，即后面的FSMC_WriteTimingStruct结构体成员。

### FSMC_ReadWriteTimingStruct

本成员是一个指针，赋值时使用FSMC_NORSRAMInitTypeDef设置，当不适用扩展模式时，读写操作时序都使用本成员的参数配置。

### FSMC_WriteTimingStruct

同样的，本成员也是一个时序结构体的指针，只有当使用扩展模式时，本配置才有效，它是**写操作使用的时序**



对本结构体赋值完成后，调用FSMC_NORSRAMInit库函数即可把配置参数写入到FSMC_BCR及FSMC_BTR/BWTE寄存器中



## FSMC-液晶显示实验

### 硬件设计

液晶显示屏PCB背面的电路对应着下图，

![1709816821848](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709816821848.png)

这部分的电路对应着液晶屏与PCB连接部分的电路，这些信号线包含液晶显示屏控制相关的CS（片选信号）、RS（数据/地址选择信号）等信号及DB0-DB15数据线，其中RS为高电平时表述传输数据，低电平表示传输地址；另外还有引出的LCD_K引脚用于控制屏幕的背光供电，可以通过该引脚控制背光的强度，该引脚被设置为低电平时打开背光



除了上面的部分，PCB上还有一个连接触控屏控制器的FPC接口，其原理图见下图：

![1709818038046](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709818038046.png)

触摸检测的主体是GT5688芯片，它接收触摸屏的信号进行处理，把触摸信号使用I2C接口输出到STM32等控制器中





![1709818111595](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709818111595.png)

上图表示的是PCB底板引出的针脚排列，屏幕整体通过这些排线与STM32的其他控制器进行连接



![1709818310223](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709818310223.png)

上图是开发板上的液晶排母接口原理图，它说明了配备的4.5寸屏幕接入到开发板上时的信号连接关系。其中需要重点关注液晶屏**(片选信号线)LCD_CS**及**LCD_RS(数据/指令选定线)**与FSMC存储区**选择分区FSMC_NE**及**地址信号FSMC_A的编号**，它们会决定STM32要使用什么内存地址来控制与液晶屏的通讯



接下来我们将进行外设的引脚初始化

```C
#include "bsp_ili9806G.h"

void GPIO_Init_Config(void)
{
	
	// 初始化所有时钟
	RCC_AHB1PeriphClockCmd(RCC_AHB1Periph_GPIOD | RCC_AHB1Periph_GPIOE | RCC_AHB1Periph_GPIOF | RCC_AHB1Periph_GPIOG, ENABLE);
		
	// 初始化FSMC时钟
	RCC_AHB3PeriphClockCmd(RCC_AHB3Periph_FSMC, ENABLE);
	
	
	GPIO_InitTypeDef GPIO_InitStruct;
	GPIO_InitStruct.GPIO_Mode = GPIO_Mode_AF;
	GPIO_InitStruct.GPIO_Speed = GPIO_Speed_100MHz;
	GPIO_InitStruct.GPIO_OType = GPIO_OType_PP;				// 推挽模式
	GPIO_InitStruct.GPIO_PuPd = GPIO_PuPd_UP;					// 所有引脚都设置为上拉模式
	
	/* 初始化所有的GPIO串口 */
	GPIO_InitStruct.GPIO_Pin = FSMC_D0_Pin;
	GPIO_Init(FSMC_D0_GPIO, &GPIO_InitStruct);
	GPIO_PinAFConfig(FSMC_D0_GPIO, FSMC_D0_Source, FSMC_D0_AF);
	
	GPIO_InitStruct.GPIO_Pin = FSMC_D1_Pin;
	GPIO_Init(FSMC_D1_GPIO, &GPIO_InitStruct);
	GPIO_PinAFConfig(FSMC_D1_GPIO, FSMC_D1_Source, FSMC_D1_AF);
	
	GPIO_InitStruct.GPIO_Pin = FSMC_D2_Pin;
	GPIO_Init(FSMC_D2_GPIO, &GPIO_InitStruct);
	GPIO_PinAFConfig(FSMC_D2_GPIO, FSMC_D2_Source, FSMC_D2_AF);
	
	GPIO_InitStruct.GPIO_Pin = FSMC_D3_Pin;
	GPIO_Init(FSMC_D3_GPIO, &GPIO_InitStruct);
	GPIO_PinAFConfig(FSMC_D3_GPIO, FSMC_D3_Source, FSMC_D3_AF);
	
	GPIO_InitStruct.GPIO_Pin = FSMC_D4_Pin;
	GPIO_Init(FSMC_D4_GPIO, &GPIO_InitStruct);
	GPIO_PinAFConfig(FSMC_D4_GPIO, FSMC_D4_Source, FSMC_D4_AF);
	
	GPIO_InitStruct.GPIO_Pin = FSMC_D5_Pin;
	GPIO_Init(FSMC_D5_GPIO, &GPIO_InitStruct);
	GPIO_PinAFConfig(FSMC_D5_GPIO, FSMC_D5_Source, FSMC_D5_AF);
	
	GPIO_InitStruct.GPIO_Pin = FSMC_D6_Pin;
	GPIO_Init(FSMC_D6_GPIO, &GPIO_InitStruct);
	GPIO_PinAFConfig(FSMC_D6_GPIO, FSMC_D6_Source, FSMC_D6_AF);
	
	GPIO_InitStruct.GPIO_Pin = FSMC_D7_Pin;
	GPIO_Init(FSMC_D7_GPIO, &GPIO_InitStruct);
	GPIO_PinAFConfig(FSMC_D7_GPIO, FSMC_D7_Source, FSMC_D7_AF);
	
	GPIO_InitStruct.GPIO_Pin = FSMC_D8_Pin;
	GPIO_Init(FSMC_D8_GPIO, &GPIO_InitStruct);
	GPIO_PinAFConfig(FSMC_D8_GPIO, FSMC_D8_Source, FSMC_D8_AF);
	
	GPIO_InitStruct.GPIO_Pin = FSMC_D9_Pin;
	GPIO_Init(FSMC_D9_GPIO, &GPIO_InitStruct);
	GPIO_PinAFConfig(FSMC_D9_GPIO, FSMC_D9_Source, FSMC_D9_AF);
	
	GPIO_InitStruct.GPIO_Pin = FSMC_D10_Pin;
	GPIO_Init(FSMC_D10_GPIO, &GPIO_InitStruct);
	GPIO_PinAFConfig(FSMC_D10_GPIO, FSMC_D10_Source, FSMC_D10_AF);
	
	
	GPIO_InitStruct.GPIO_Pin = FSMC_D11_Pin;
	GPIO_Init(FSMC_D11_GPIO, &GPIO_InitStruct);
	GPIO_PinAFConfig(FSMC_D11_GPIO, FSMC_D11_Source, FSMC_D11_AF);
	
	GPIO_InitStruct.GPIO_Pin = FSMC_D12_Pin;
	GPIO_Init(FSMC_D12_GPIO, &GPIO_InitStruct);
	GPIO_PinAFConfig(FSMC_D12_GPIO, FSMC_D12_Source, FSMC_D12_AF);
	
	GPIO_InitStruct.GPIO_Pin = FSMC_D13_Pin;
	GPIO_Init(FSMC_D13_GPIO, &GPIO_InitStruct);
	GPIO_PinAFConfig(FSMC_D13_GPIO, FSMC_D13_Source, FSMC_D13_AF);
	
	GPIO_InitStruct.GPIO_Pin = FSMC_D14_Pin;
	GPIO_Init(FSMC_D14_GPIO, &GPIO_InitStruct);
	GPIO_PinAFConfig(FSMC_D14_GPIO, FSMC_D14_Source, FSMC_D14_AF);
	
	GPIO_InitStruct.GPIO_Pin = FSMC_D15_Pin;
	GPIO_Init(FSMC_D15_GPIO, &GPIO_InitStruct);
	GPIO_PinAFConfig(FSMC_D15_GPIO, FSMC_D15_Source, FSMC_D15_AF);
	
	
	/* FMSC_CS片选信号 */
	GPIO_InitStruct.GPIO_Pin = FSMC_CS_Pin;
	GPIO_Init(FSMC_CS_GPIO, &GPIO_InitStruct);
	GPIO_PinAFConfig(FSMC_CS_GPIO, FSMC_CS_Source, FSMC_CS_AF);
	
	
	/* FSMC_NOE输出使能信号 */
	GPIO_InitStruct.GPIO_Pin = FSMC_NOE_Pin;
	GPIO_Init(FSMC_NOE_GPIO, &GPIO_InitStruct);
	GPIO_PinAFConfig(FSMC_NOE_GPIO, FSMC_NOE_Source, FSMC_NOE_AF);
	
	
	/* FSMC_WE写入使能信号 */
	GPIO_InitStruct.GPIO_Pin = FSMC_WE_Pin;
	GPIO_Init(FSMC_WE_GPIO, &GPIO_InitStruct);
	GPIO_PinAFConfig(FSMC_WE_GPIO, FSMC_WE_Source, FSMC_WE_AF);
	
	
	/* FSMC地址线，使用该线并使用相对应的单数地址，即可将模拟8080接口的D/CX数据命令 */
	GPIO_InitStruct.GPIO_Pin = FSMC_RS_Pin;
	GPIO_Init(FSMC_RS_GPIO, &GPIO_InitStruct);
	GPIO_PinAFConfig(FSMC_RS_GPIO, FSMC_RS_Source, FSMC_RS_AF);
	

	/* RESET复位 */
	GPIO_InitStruct.GPIO_Mode = GPIO_Mode_OUT;			// 由于Reset需要的是将复位信号发送给别的设备，因此这里需要使用上拉推挽
	GPIO_InitStruct.GPIO_OType = GPIO_OType_PP;
	GPIO_InitStruct.GPIO_PuPd =	GPIO_PuPd_UP;
	GPIO_InitStruct.GPIO_Speed = GPIO_Speed_50MHz;
	
	GPIO_InitStruct.GPIO_Pin = FSMC_LCD_RST_Pin;
	GPIO_Init(FSMC_LCD_RST_GPIO, &GPIO_InitStruct);

	/* LCD_BL背光控制引脚 */
	GPIO_InitStruct.GPIO_Pin = FSMC_LCD_BL_Pin;			// 这两个引脚由于并不是FSMC控制的因此我们需要单独控制
	GPIO_Init(FSMC_LCD_BL_GPIO, &GPIO_InitStruct);

}
	

void FSMC_Config(void)
{
	FSMC_NORSRAMInitTypeDef 					FSMC_InitStruct;
	FSMC_NORSRAMTimingInitTypeDef   	FSMC_Read_InitStruct;
	
	// 启动FSMC的时钟
	RCC_AHB3PeriphClockCmd(RCC_AHB3Periph_FSMC, ENABLE);
	
	
	/* 配置FSMC的时序结构体 */
	FSMC_Read_InitStruct.FSMC_AddressSetupTime = 0x04;				// 地址建立时间
	FSMC_Read_InitStruct.FSMC_AddressHoldTime = 0x00;					// 地址保持间
	FSMC_Read_InitStruct.FSMC_DataSetupTime = 0x0b;						// 数据建立时间
	FSMC_Read_InitStruct.FSMC_BusTurnAroundDuration = 0x00;		// 总线转换时间
	FSMC_Read_InitStruct.FSMC_CLKDivision = 0x00;							// 时钟分频
	FSMC_Read_InitStruct.FSMC_DataLatency = 0x00; 						// 数据保持时间
	FSMC_Read_InitStruct.FSMC_AccessMode = FSMC_AccessMode_B; // FSMC控制的时序模式
	
	
	
	/* 配置FSMC的初始化结构体 */
	FSMC_InitStruct.FSMC_Bank = FSMC_BANK_NOR_FLASH;
	FSMC_InitStruct.FSMC_DataAddressMux = FSMC_DataAddressMux_Disable;
	FSMC_InitStruct.FSMC_MemoryType = FSMC_MemoryType_NOR;
	FSMC_InitStruct.FSMC_MemoryDataWidth = FSMC_MemoryDataWidth_16b;  		// 由于屏幕的格式为RGB565，共需要使用16位来进行传输数据，因此使用16b
	FSMC_InitStruct.FSMC_BurstAccessMode = FSMC_BurstAccessMode_Disable;	// 突发传输，仅适用于同步类型的存储器
	FSMC_InitStruct.FSMC_AsynchronousWait = FSMC_AsynchronousWait_Disable;// 是否在同步的时候，使用等待信号
	FSMC_InitStruct.FSMC_WaitSignalPolarity = FSMC_WaitSignalPolarity_Low;// 设置等待信号的有效电平
	FSMC_InitStruct.FSMC_WrapMode = FSMC_WrapMode_Disable; 								// 是否支持对齐的突发模式
	FSMC_InitStruct.FSMC_WaitSignalActive = FSMC_WaitSignalActive_BeforeWaitState;  // 设置等待信号在等待信号前有效还是等待期间有效
	FSMC_InitStruct.FSMC_WriteOperation = FSMC_WriteOperation_Enable;			// 是否支持写操作
	FSMC_InitStruct.FSMC_WaitSignal = FSMC_WaitSignalPolarity_Low;				// 是否使能等待状态插入
	FSMC_InitStruct.FSMC_ExtendedMode = FSMC_ExtendedMode_Disable;				// 是否使用扩展模式
	FSMC_InitStruct.FSMC_WriteBurst = FSMC_WriteBurst_Disable;						// 是否使用写突发操作
	FSMC_InitStruct.FSMC_ReadWriteTimingStruct = &FSMC_Read_InitStruct;
	FSMC_InitStruct.FSMC_WriteTimingStruct = &FSMC_Read_InitStruct;
	
	FSMC_NORSRAMInit(&FSMC_InitStruct);
	FSMC_NORSRAMCmd(FSMC_Bank1_NORSRAM3, ENABLE);
}

```

这两个函数分别做了：

1. 初始化FSMC连接的GPIO
2. 将对应的GPIO的时钟使能
3. 初始化FSMC的初始化配置
4. 使能FSMC的时钟

<u>而在设置FSMC_AddressHoldTime等参数的时候，我们就需要通过查看存储器的数据手册来查看其参数范围</u>，并进行设置，如上面的配置中，

我们的FSMC_DataSetupTime被设置为了0x0b（即10个HCLK时钟周期），因此其数据保持时间将被设置为：$(11+1)/168M=72ns$

​	而FSMC_AddressSetupTime被设置为了0x04（即4个HCLK时钟周期），因此其数据保持时间被设置为：$(4+1)/168M=30ns$

这两个参数都满足下表中的$write\_cycle_{min}= 30ns $, $address\_setup\_time_{min}=0ns$的要求



![1711977174300](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1711977174300.png)



### 计算控制液晶屏时使用的地址

初始化完 FSMC 后，即可使用类似扩展外部 SRAM 中的读取方式：通过访问某个地址，由 FSMC产生时序与外部存储器通讯，进行读写。
同样地，当访问特定地址时，FSMC 会产生相应的模拟 8080 时序，控制地址线输出要访问的内存，并使用数据线接收或发送数据，而其他的片选信号NE、读使能信号NOE、写使能信号NWE则会辅助产生完整的8080时序，并且这些信号通过上面配置好的FSMC_A0信号线（D/CX）决定刚才传输的是命令还是数据。![1711980226060](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1711980226060.png)

计算地址过程如下：

（1）本工程中使用的是FSMC_NE3作为8080_CS片选信号，所以首先可以确认地址范围，当访问0x6800 0000~0x6BFF FFFF地址时，FSMC均会对外产生<u>片选有效</u>的访问时序。

（2）在本工程中使用的是FSMC_A0作为命令/数据选择线，因此在一般情况下，当地址最后一位为奇数的时候，FSMC_A0将输出高电平，表示为数据，而FSMC_A0输出低电平，则表示地址。

* 要使得FSMC_A0地址线为高电平，实质上是输出地址信号的第0位为1即可，使用0x6800 0000~0x6BFF FFFF内的任意地址，做如下的运算即可：
    * 设置传输的数据为数据时，地址的第0位为1：0x6800 0000 |= （1<<0）= 0x6800 0001
    * 设置传输的数据为地址时，地址的第0位为0：0x6800 0000 &= ~(1<<0）= 0x6800 0000



但是实际上，由于FSMC访问的是NOR_FLASH，因此就需要按照NOR_FLASH的地址线说明进行配置，在NOR_FLASH中，地址线按下图进行连接：

![1711980988863](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1711980988863.png)



而由于本实验中使用的是16位的数据宽度，因此HADDR与FSMC_A的地址线连接关系会<u>左移一位</u>，即HADDR[1]将不会与FSMC_A[1]相连，反而是与FSMC_A[0]相连。因此我们在计算过程中需要再往左移一位

即：

* 设置传输的数据为数据时，地址的第0位应该为1：0x6800 0000 |= （1<<1）= 0x6800 0002
* 设置传输的数据为地址时，地址的第0为应该为0：0x6800 0000 &= ~（1<<1）= 0x6800 0000



### 向液晶屏写入初始化配置

写入初始化配置的时候我们可以直接使用相关的地址指针进行写入命令或数据，由于我们使用的是NOR——FLAHS进行模拟，因此当FSMC_A[0]位为0的时候表示命令，当FSMC_A[0]位为1时表示数据，因此我们可以直接封装成函数为：

```C
// 传输命令模式
#define FSMC_Addr_ILI9806G_CMD          ((uint32_t)0x68000000)

// 传输数据模式，这里由于是使用的NOR_FLASH进行模拟，因此所有的地址应该左移动1位
#define FSMC_Addr_ILI9806G_DATA					((uint32_t)0x68000002)

```



并且我们通过这两个预处理函数可以封装成一个独立的写命令或写数据的函数

```C
// 向ILI9806G写入命令
__inline void ILI9806G_Write_Cmd(uint16_t usCmd)
{
	*(__IO uint16_t *)(FSMC_Addr_ILI9806G_CMD) = usCmd;
}

// 向ILI9806G写入数据
__inline void ILI9806G_Write_Data(uint16_t usData)    // __inline内联类型，指在编译过程中遇到使用这个函数的地方，就需要将函数中的内容复制到调用处，相当于#define
{
	*(__IO uint16_t *)(FSMC_Addr_ILI9806G_DATA) = usData; // __IO是一个预处理类型符号，这个类型指示这个指针的值是易变的，使得每次读取或调用该变量的时候都需要读取一次，并在编译的时候不进行优化
}
```



通过这两个函数我们就可以给屏幕进行初始化配置，这个初始化过程中发送的代码及参数主要配置了液晶屏的上电过程、显示屏的伽马参数、分辨率、像素格式等内容，这些配置主要由液晶屏生产厂家提供。

例如厂家的驱动程序：

```C
void LCD_Reg_Config(void)
{
	
	//液晶厂商提供了两种版本的屏幕，性能一样，它们的驱动配置稍有区别，
	//本驱动通过#if #else #endif来设置，若屏幕显示花屏，请把#if 0改成#if 1，或1改成0
	//在2017-12-14日后购买的产品，使用#if 0
#if 0
	//旧版
	/* EXTC Command Set enable register */
	delay_ms(0xFFFFF)  ();
	ILI9806G_Write_Cmd ( 0xFF  );
	ILI9806G_Write_Data ( 0xFF  );
	ILI9806G_Write_Data ( 0x98  );
	ILI9806G_Write_Data ( 0x06  );

	/* GIP 1(BCh)  */
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xBC);
	ILI9806G_Write_Data(0x01); 
	ILI9806G_Write_Data(0x0E); 
	ILI9806G_Write_Data(0x61); 
	ILI9806G_Write_Data(0xFB); 
	ILI9806G_Write_Data(0x10); 
	ILI9806G_Write_Data(0x10); 
	ILI9806G_Write_Data(0x0B); 
	ILI9806G_Write_Data(0x0F); 
	ILI9806G_Write_Data(0x2E); 
	ILI9806G_Write_Data(0x73); 
	ILI9806G_Write_Data(0xFF); 
	ILI9806G_Write_Data(0xFF); 
	ILI9806G_Write_Data(0x0E); 
	ILI9806G_Write_Data(0x0E); 
	ILI9806G_Write_Data(0x00); 
	ILI9806G_Write_Data(0x03); 
	ILI9806G_Write_Data(0x66); 
	ILI9806G_Write_Data(0x63); 
	ILI9806G_Write_Data(0x01); 
	ILI9806G_Write_Data(0x00); 
	ILI9806G_Write_Data(0x00);

	/* GIP 2 (BDh) */
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xBD);
	ILI9806G_Write_Data(0x01); 
	ILI9806G_Write_Data(0x23); 
	ILI9806G_Write_Data(0x45); 
	ILI9806G_Write_Data(0x67); 
	ILI9806G_Write_Data(0x01); 
	ILI9806G_Write_Data(0x23); 
	ILI9806G_Write_Data(0x45); 
	ILI9806G_Write_Data(0x67); 

	/* GIP 3 (BEh) */
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xBE);
	ILI9806G_Write_Data(0x00); 
	ILI9806G_Write_Data(0x21); 
	ILI9806G_Write_Data(0xAB); 
	ILI9806G_Write_Data(0x60); 
	ILI9806G_Write_Data(0x22); 
	ILI9806G_Write_Data(0x22); 
	ILI9806G_Write_Data(0x22); 
	ILI9806G_Write_Data(0x22); 
	ILI9806G_Write_Data(0x22); 

	/* Vcom  (C7h) */
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd ( 0xC7 );
	ILI9806G_Write_Data ( 0x6F );

	/* EN_volt_reg (EDh)*/
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd ( 0xED );
	ILI9806G_Write_Data ( 0x7F );
	ILI9806G_Write_Data ( 0x0F );
	ILI9806G_Write_Data ( 0x00 );

	/* Power Control 1 (C0h) */
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd ( 0xC0 );
	ILI9806G_Write_Data ( 0x37 );
	ILI9806G_Write_Data ( 0x0B );
	ILI9806G_Write_Data ( 0x0A );

	/* LVGL (FCh) */
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd ( 0xFC );
	ILI9806G_Write_Data ( 0x0A );

	/* Engineering Setting (DFh) */
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd ( 0xDF );
	ILI9806G_Write_Data ( 0x00 );
	ILI9806G_Write_Data ( 0x00 );
	ILI9806G_Write_Data ( 0x00 );
	ILI9806G_Write_Data ( 0x00 );
	ILI9806G_Write_Data ( 0x00 );
	ILI9806G_Write_Data ( 0x20 );

	/* DVDD Voltage Setting(F3h) */
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd ( 0xF3 );
	ILI9806G_Write_Data ( 0x74 );

	/* Display Inversion Control (B4h) */
	ILI9806G_Write_Cmd ( 0xB4 );
	ILI9806G_Write_Data ( 0x00 );
	ILI9806G_Write_Data ( 0x00 );
	ILI9806G_Write_Data ( 0x00 );

	/* 480x854 (F7h)  */
	ILI9806G_Write_Cmd ( 0xF7 );
	ILI9806G_Write_Data ( 0x89 );

	/* Frame Rate (B1h) */
	ILI9806G_Write_Cmd ( 0xB1 );
	ILI9806G_Write_Data ( 0x00 );
	ILI9806G_Write_Data ( 0x12 );
	ILI9806G_Write_Data ( 0x10 );

	/* Panel Timing Control (F2h) */
	ILI9806G_Write_Cmd ( 0xF2 );
	ILI9806G_Write_Data ( 0x80 );
	ILI9806G_Write_Data ( 0x5B );
	ILI9806G_Write_Data ( 0x40 );
	ILI9806G_Write_Data ( 0x28 );

	delay_ms(0xFFFFF);

	/* Power Control 2 (C1h) */
	ILI9806G_Write_Cmd ( 0xC1 ); 
	ILI9806G_Write_Data ( 0x17 );
	ILI9806G_Write_Data ( 0x7D );
	ILI9806G_Write_Data ( 0x7A );
	ILI9806G_Write_Data ( 0x20 );

	delay_ms(0xFFFFF);

	ILI9806G_Write_Cmd(0xE0); 
	ILI9806G_Write_Data(0x00); //P1 
	ILI9806G_Write_Data(0x11); //P2 
	ILI9806G_Write_Data(0x1C); //P3 
	ILI9806G_Write_Data(0x0E); //P4 
	ILI9806G_Write_Data(0x0F); //P5 
	ILI9806G_Write_Data(0x0C); //P6 
	ILI9806G_Write_Data(0xC7); //P7 
	ILI9806G_Write_Data(0x06); //P8 
	ILI9806G_Write_Data(0x06); //P9 
	ILI9806G_Write_Data(0x0A); //P10 
	ILI9806G_Write_Data(0x10); //P11 
	ILI9806G_Write_Data(0x12); //P12 
	ILI9806G_Write_Data(0x0A); //P13 
	ILI9806G_Write_Data(0x10); //P14 
	ILI9806G_Write_Data(0x02); //P15 
	ILI9806G_Write_Data(0x00); //P16 

	delay_ms(0xFFFFF);

	ILI9806G_Write_Cmd(0xE1); 
	ILI9806G_Write_Data(0x00); //P1 
	ILI9806G_Write_Data(0x12); //P2 
	ILI9806G_Write_Data(0x18); //P3 
	ILI9806G_Write_Data(0x0C); //P4 
	ILI9806G_Write_Data(0x0F); //P5 
	ILI9806G_Write_Data(0x0A); //P6 
	ILI9806G_Write_Data(0x77); //P7 
	ILI9806G_Write_Data(0x06); //P8 
	ILI9806G_Write_Data(0x07); //P9 
	ILI9806G_Write_Data(0x0A); //P10 
	ILI9806G_Write_Data(0x0E); //P11 
	ILI9806G_Write_Data(0x0B); //P12 
	ILI9806G_Write_Data(0x10); //P13 
	ILI9806G_Write_Data(0x1D); //P14 
	ILI9806G_Write_Data(0x17); //P15 
	ILI9806G_Write_Data(0x00); //P16  

	/* Tearing Effect ON (35h)  */
	ILI9806G_Write_Cmd ( 0x35 );
	ILI9806G_Write_Data ( 0x00 );

	ILI9806G_Write_Cmd ( 0x3A );
	ILI9806G_Write_Data ( 0x55 );

	ILI9806G_Write_Cmd ( 0x11 );
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd ( 0x29 );
	
#else
	//新版
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xFF);
	ILI9806G_Write_Data(0xFF);
	ILI9806G_Write_Data(0x98);
	ILI9806G_Write_Data(0x06);
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xBA);
	ILI9806G_Write_Data(0x60);  
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xBC);
	ILI9806G_Write_Data(0x03);
	ILI9806G_Write_Data(0x0E);
	ILI9806G_Write_Data(0x61);
	ILI9806G_Write_Data(0xFF);
	ILI9806G_Write_Data(0x05);
	ILI9806G_Write_Data(0x05);
	ILI9806G_Write_Data(0x1B);
	ILI9806G_Write_Data(0x10);
	ILI9806G_Write_Data(0x73);
	ILI9806G_Write_Data(0x63);
	ILI9806G_Write_Data(0xFF);
	ILI9806G_Write_Data(0xFF);
	ILI9806G_Write_Data(0x05);
	ILI9806G_Write_Data(0x05);
	ILI9806G_Write_Data(0x00);
	ILI9806G_Write_Data(0x00);
	ILI9806G_Write_Data(0xD5);
	ILI9806G_Write_Data(0xD0);
	ILI9806G_Write_Data(0x01);
	ILI9806G_Write_Data(0x00);
	ILI9806G_Write_Data(0x40); 
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xBD);
	ILI9806G_Write_Data(0x01);
	ILI9806G_Write_Data(0x23);
	ILI9806G_Write_Data(0x45);
	ILI9806G_Write_Data(0x67);
	ILI9806G_Write_Data(0x01);
	ILI9806G_Write_Data(0x23);
	ILI9806G_Write_Data(0x45);
	ILI9806G_Write_Data(0x67);  
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xBE);
	ILI9806G_Write_Data(0x01);
	ILI9806G_Write_Data(0x2D);
	ILI9806G_Write_Data(0xCB);
	ILI9806G_Write_Data(0xA2);
	ILI9806G_Write_Data(0x62);
	ILI9806G_Write_Data(0xF2);
	ILI9806G_Write_Data(0xE2);
	ILI9806G_Write_Data(0x22);
	ILI9806G_Write_Data(0x22);
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xC7);
	ILI9806G_Write_Data(0x63); 
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xED);
	ILI9806G_Write_Data(0x7F);
	ILI9806G_Write_Data(0x0F);
	ILI9806G_Write_Data(0x00);
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xC0);
	ILI9806G_Write_Data(0x03);
	ILI9806G_Write_Data(0x0B);
	ILI9806G_Write_Data(0x00);   
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xFC);
	ILI9806G_Write_Data(0x08); 
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xDF);
	ILI9806G_Write_Data(0x00);
	ILI9806G_Write_Data(0x00);
	ILI9806G_Write_Data(0x00);
	ILI9806G_Write_Data(0x00);
	ILI9806G_Write_Data(0x00);
	ILI9806G_Write_Data(0x20);
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xF3);
	ILI9806G_Write_Data(0x74);
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xF9);
	ILI9806G_Write_Data(0x00);
	ILI9806G_Write_Data(0xFD);
	ILI9806G_Write_Data(0x80);
	ILI9806G_Write_Data(0x80);
	ILI9806G_Write_Data(0xC0);
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xB4);
	ILI9806G_Write_Data(0x02);
	ILI9806G_Write_Data(0x02);
	ILI9806G_Write_Data(0x02); 
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xF7);
	ILI9806G_Write_Data(0x81);
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xB1);
	ILI9806G_Write_Data(0x00);
	ILI9806G_Write_Data(0x13);
	ILI9806G_Write_Data(0x13); 
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xF2);
	ILI9806G_Write_Data(0xC0);
	ILI9806G_Write_Data(0x02);
	ILI9806G_Write_Data(0x40);
	ILI9806G_Write_Data(0x28);  
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xC1);
	ILI9806G_Write_Data(0x17);
	ILI9806G_Write_Data(0x75);
	ILI9806G_Write_Data(0x75);
	ILI9806G_Write_Data(0x20); 
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xE0);
	ILI9806G_Write_Data(0x00);
	ILI9806G_Write_Data(0x05);
	ILI9806G_Write_Data(0x08);
	ILI9806G_Write_Data(0x0C);
	ILI9806G_Write_Data(0x0F);
	ILI9806G_Write_Data(0x15);
	ILI9806G_Write_Data(0x09);
	ILI9806G_Write_Data(0x07);
	ILI9806G_Write_Data(0x01);
	ILI9806G_Write_Data(0x06);
	ILI9806G_Write_Data(0x09);
	ILI9806G_Write_Data(0x16);
	ILI9806G_Write_Data(0x14);
	ILI9806G_Write_Data(0x3E);
	ILI9806G_Write_Data(0x3E);
	ILI9806G_Write_Data(0x00);
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0xE1);
	ILI9806G_Write_Data(0x00);
	ILI9806G_Write_Data(0x09);
	ILI9806G_Write_Data(0x12);
	ILI9806G_Write_Data(0x12);
	ILI9806G_Write_Data(0x13);
	ILI9806G_Write_Data(0x1c);
	ILI9806G_Write_Data(0x07);
	ILI9806G_Write_Data(0x08);
	ILI9806G_Write_Data(0x05);
	ILI9806G_Write_Data(0x08);
	ILI9806G_Write_Data(0x03);
	ILI9806G_Write_Data(0x02);
	ILI9806G_Write_Data(0x04);
	ILI9806G_Write_Data(0x1E);
	ILI9806G_Write_Data(0x1B);
	ILI9806G_Write_Data(0x00);
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0x3A);
	ILI9806G_Write_Data(0x55); 
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0x35);
	ILI9806G_Write_Data(0x00); 
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0x11);
	delay_ms(0xFFFFF);
	ILI9806G_Write_Cmd(0x29);	   
	delay_ms(0xFFFFF);
#endif		
}
```

这个函数是厂家提供的，具体的参数也是由厂家设置好，我们并不需要设置，这部分包括具体的电平等信息，不是我们需要学习的范畴。

而后面学到是只针对常用命令的讲解；关于对命令以及参数可以查询数据手册获知。



### 设置液晶显示窗口

根据液晶屏的要求，在发送显示数据前，需要先设置显示窗口发送的像素数据的显示区域：

虽然我们设置了FSMC_MemoryDataWidth为了16bit，因此我们本应该一次向ili9806的寄存器传输16位的数据，但是由于我们0x2a、0x2b的指令在每一次接收数据时只能每次传输8位的数据，因此我们在后面需要对高位的数据进行处理（右移指定的位数，给将低位的数据去掉，并使用高位进行先写入进入寄存器中）；

![1713411004847](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1713411004847.png)

![1713411057182](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1713411057182.png)



另一个写入行数的指令也是一样的情况，

![1713411102918](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1713411102918.png)

![1713411144158](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1713411144158.png)



而0x2c指令就可以直接传输16位的数据，而不需要进行处理再写入

![1713411208290](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1713411208290.png)



例程：

```C
void LCD_Set_Dis_Wid(uint32_t x0, uint32_t x1, uint32_t y0, uint32_t y1, uint32_t color)
{
	uint16_t i = 0;
//	// 设置横向像素点范围
//	ILI9806G_Write_Cmd(0x2a);
//	ILI9806G_Write_Data((x0));
//	ILI9806G_Write_Data(x1);

//	// 设置竖向像素点范围
//	ILI9806G_Write_Cmd(0x2b);
//	ILI9806G_Write_Data(y0);
//	ILI9806G_Write_Data(y1);
	
	ILI9806G_Write_Cmd(0x2a);
	ILI9806G_Write_Data((x0<<8)&0xFF); //x0高8位
	ILI9806G_Write_Data(x0&0xFF);
	ILI9806G_Write_Data((x1<<8)&0xFF); //x1高8位
	ILI9806G_Write_Data(x1&0xFF);
	
	ILI9806G_Write_Cmd(0x2b);
	ILI9806G_Write_Data((y0<<8)&0xFF); //y0高8位
	ILI9806G_Write_Data(y0&0xFF);
	ILI9806G_Write_Data((y1<<8)&0xFF); //y1高8位，由于数据手册中表格的限制，我们在写入这些设置命令的数据时，需要按照其规定来写入，因为
	ILI9806G_Write_Data(y1&0xFF);			 // 0x2b在传输的时候只能一次设置8位，因此我们需要做右移操作，第一次丢弃掉低位的数据，并将高位的数据移动
																		 // 原来的低8位的数据位中，然后第二次才写入低位的数据
		
	ILI9806G_Write_Cmd(0x2c); //写入像素命令
	
	ILI9806G_Write_Cmd(0x2c);
	for (i; i<(x1-x0+1)*(y1-y0+1); i++)					// 这里+1是因为两列像素值编号相减会少了一列所以要补上，例如第三列到第一列，3-1+1=3列
	{
		ILI9806G_Write_Data(color);								// 这里由于执行0x2c命令时能一次性传输16位的数据，所以我们不需要执行相应的处理操作再写入到寄存器中
	}
}
```

这个例程中向屏幕中指定的窗口大小用于显示， 并且将这个窗口的像素全部设置为红色。这里使用for循环是因为ili9806这些液晶屏控制器是将寄存器中的一个个数据当作屏幕给中的一个像素，我们每次都传输一个像素的值，因此我们需要为所有的像素值都提供一个值，因此需要循环给所有的像素赋值。但是如果仅仅只是使用一个设置窗口命令是远远不足以完成显示一帧图像的任务的。









### 窗口扫描方向

其实我们还需要配置屏幕的另一些参数，例如屏幕的扫描方向等等

而屏幕的==坐标原点==和==XY方向==都可以根据实际需要使用0x36命令来配置，该命令见下图：

![1713485349972](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1713485349972.png)

![1713485401425](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1713485401425.png)

![1713485385157](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1713485385157.png)

这个指令后面需要输入8个参数，每个参数都有其含义，其中<u>MY、MX、MV</u>这三个数据位用于配置==扫描方向==，因此一共有$2^3=8$种模式。

下面的ILI9806_GramScan函数就是根据输入的模式设置这三个数据位，并且根据相应的模式设置XY的方向分辨率信息；

调用ILI9806_GramScan函数设置0-7模式时，各个模式的坐标原点及XY方向将会如下图所示；

 ![1713486769026](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1713486769026.png)

其中模式6最符合我们的阅读习惯，扫描方向与文字方向一致，都是从左到右，从上到下，所以我们默认使用模式6.

其实模式0、2、4、6的液晶扫描方向都与文字方向一致，比较适合显示文字，它们都是横向的显示文字，





























### 各种颜色的映射值

[颜色映射表](https://blog.csdn.net/qq_20222919/article/details/116168044)

从上面的表格中我们发现，不同的颜色格式，需要传入的颜色16进制数都会不一样，这是因为每种颜色格式所用于表示的颜色位数不一样，例如：





### Bresenham直线算法

[直线算法详细](https://zhuanlan.zhihu.com/p/106155534)

在纸张上，给出任意的一个起点和终点，要求我们画出一条连接他们的直线，这在显示中相当简单，但是如果在一个一个像素中画一条直线就相对困难，例如我们在屏幕上斜着画一条线，我们就很贵相对困难，因为像素是行列排列的，并没有斜着的部分。

![img](https://pic1.zhimg.com/80/v2-7455b783e4994aebec54ecc66b7fcb78_1440w.webp)

如上图所示，屏幕是由很多的像素块组成，并且每个像素想当的小，因此这条线在屏幕上就会看起来像是直线，这就是计算机图形学中的直线的呈现方式，可以看到，从红点到蓝点之间，每一个小方块的坐标值都有如下的特征：

1. X轴的值每次增长1
2. Y轴的值会在保持不变或变化1个像素之间选择



因此Bresenham算法可以这样简单描述：这个算法所做的就是**计算在屏幕上的一个像素点到另一个像素点之间画一条直线时下一个需要赋予像素值的像素的位置**

给定两点，起点（x1, y1）以及终点（x2, y2）连接这两个点的理论直线可以使用一元一次方程$y=mx+b$求出斜率m以及截距b，我们就可以获得在该上的所有点

[具体介绍](https://blog.csdn.net/Changemakers/article/details/138026760)









# STM32最小系统板

最小系统板的具体组成结构为：

1. stm32芯片
2. stm32芯片的具体周边电路
3. PCB板
4. stm32必要的外围硬件设备，如：SRAM、Flash等等









































# 问题



## 为什么.c文件与.h文件之间并没有相互包含，但依旧可以只include.h文件就可以使用.c文件中定义的函数

.h中一般放的是同名.c文件中定义的变量、数组、函数的声明，这些声明都是.c文件中定义的变量、数组、函数的封装

同时C在编译的时候并不会在意其后缀，他只会在#include的地方删除该行的代码，同时替换成.h文件中的预定义的代码，而这些代码就相当于函数的**接口**以及**参数类型**，在编译过后的链接是就会找到具体的函数实现

因此，我们并不直接使用.c文件作为include的对象，因为这样可能会导致多次重复的定义导致报错， 而我们使用include.h文件的话，就单纯的保留了对应函数的接口，当我们使用的时候，就直接使用接口，来执行函数体的操作即可



## C源文件为什么要包含自己对应的头文件

C的源文件就像是实际实现好的产品函数，而头文件则是这个函数的大致说明书，当要使用的时候这个.h文件中的声明就相当于函数的主体，并且#include会将这些主体导入到编译文件中，C的源文件导入自己的头文件是为了后面在实现函数的过程中，能够让编译器知道这些接口的定义，以便能够正确的生成目标代码





## 为什么串口发送中文会乱码

C的发送程序源码文件，出现中文的时候，输入中文的程序文件的编码应该与接收程序的文件编码一致，如果不一致那么就会出现乱码，具体如果出现乱码可以使用notepad++来查看文件的编码格式



## 为什么串口无法接收重新发回来的数据

因为中断服务函数名不能随机写，如果想要随机写的话，就需要使用预处理器来给中断服务函数命名的别名，如果并且别名要与**startup_stm32f40xx.s**文件中的中断服务函数别名对应，否则只能空触发，但无法正确运行处理函数





## 为什么FLASH一定要进行擦除操作

因为在FLASH存储器中，1可以写成0，0不可以写成1，

例如：前面已经写入了一个0x11，二进制为0001 0001，而我们没有进行擦除操作，再次写入一个0x55，二进制为0101 0101，在进行写入后值并不会改变，依旧会是0x11





## 为什么要使用文件分配表呢，直接使用目录不好嘛

这是因为文件系统经常需要进行删除操作，然后再往删除掉的空间中写入数据，但是如果写入的数据太大，超过了刚删除释放的空间，那么就会将剩余没空间存放的数据存储到另一个空间中，也就是说文件的内容在磁盘中的位置并不是连续的，那么如果我们通过目录中的文件大小以及开始位置进行连续读取的话就会错误的读取文件的内容

![1709432319639](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1709432319639.png)

并且通过删除操作过后的文件分配表将会变成如下形式

![img](https://img-blog.csdnimg.cn/d5099440d0ae4e01a080be45d9232479.png)





## 为什么FSMC能够通过不同的时序来控制不同的存储器

在这里，“时序”指的是存储器访问操作中的时间顺序和时钟信号的控制。对于不同类型的存储器，其读写操作需要遵循特定的时间序列和时钟信号来确保数据的正确性和稳定性。这些时间序列包括地址和数据的建立时间、保持时间以及时钟信号的相对时序等。

通过调整这些时序参数，可以适配不同类型的存储器，以满足其特定的时序要求。例如，对于高速存储器，可能需要更紧凑和快速的时序来实现最佳性能；而对于非易失性存储器，则需要更宽松和稳定的时序来确保数据的正确读写和稳定性。

因此，通过可编程的时序控制，FSMC 能够适应不同存储器类型的时序要求，从而实现对它们的有效控制和管理。





## 问题：这个地址宽度指的是什么？

地址线宽度通常可以表示为行地址线的数量与列地址线的数量的乘积和。例如，如果一个SRAM有8根地址线用于行地址，8根地址线用于列地址，那么这个SRAM的地址线宽度就是8乘以8，即64。这表示了该SRAM可以寻址的存储单元数量，也决定了它的存储容量。





## 问题：这个数据线宽度指的是什么

数据线的宽度指的是每个存储单元中可以存储的数据位数。数据线宽度决定了每次读取或写入操作中可以传输的数据量例如，如果一个SRAM的数据线宽度为8位（1字节），那么每个存储单元可以存储8位的数据，即一个字节。这意味着每次从这个SRAM读取或写入数据时，都会以字节为单位进行操作。数据线宽度的大小影响了SRAM的数据处理能力和带宽，较大的数据线宽度通常可以提高数据传输速度和效率。



## 问题：为什么LED中的P型半导体的厚度要最薄？

LED中的P型半导体的厚度要尽量薄是因为减小P型半导体的厚度可以缩短电子和空穴再结合的距离，从而提高光子发射的效率。当电子和空穴相遇时，它们会发生复合并释放出能量，产生光子。通过减小P型半导体的厚度，可以减少电子和空穴再结合的机会，提高光子的发射效率。



## 问题：内存是如何计算的

内存的大小是存储器的地址线的数量与存储空间的大小的乘积和的大小，例如，SRAM有19根地址线，即总共有$2^{19}$个地址，并且每个地址代表了一个存储空间，还有16条数据线，由于每条数据线能表示1bit的数据，因此每个存储空间能存储16bit的数据，也就是2字节

因此SRAM的内存大小为：$1MB = 2^{19}*2 = 1,048,576$





## 问题：为什么液晶数据在传输时会有VBP、VFP、HBP、HFP等的无效时序参数产生

液晶数据传输中会有VBP等参数产生，主要是因为液晶显示需要按照一定的时间序列来刷新屏幕上的像素点。在显示器中，VBP（垂直空白间隔）等参数的产生是为了确保图像能够在屏幕上正确、稳定地显示。

具体来说，VBP的存在可以帮助液晶显示器进行垂直同步和刷新操作。垂直同步信号之间的VBP时间可以让显示器在绘制完一幅图像后，有足够的时间来进行垂直回扫和重新定位，以便开始下一幅图像的扫描。这样可以有效避免图像撕裂和闪烁等问题，提高显示效果和稳定性。

因此，VBP等参数的产生是为了协调液晶显示器内部的刷新机制，以保证图像能够以正确的顺序和速率在屏幕上显示。







## 问题：为什么printf后面使用了括号就会出现格式化值不正常的情况

例程代码：

```c
printf("\n写入数据编码地址：%x,写入命令编码地址为：%x\n", (FSMC_Addr_ILI9806G_DATA, FSMC_Addr_ILI9806G_CMD));
```

期望输出：

写入数据编码地址：68000002,写入命令编码地址为：68000000

* 最终输出：写入数据编码地址：68000000,写入命令编码地址为：ffff

    原因：

    这里printf格式化的值并不需要使用括号进行括起来，括号是用于将两个对象看作一个<u>运算整体</u>，从而改变两个对象的优先级，让运算执行完毕过后才进行使用，但是在上面的代码中使用的是逗号运算符，逗号运算符并不会进行计算，它的功能就只是“先计算左边的表达式，再计算右边的表达式，并且最后返回右边的表达式”，因此我们在这里将括号中的两个值看作一个整体，但最终返回的是第二个值，因此最终的结果是第一个格式化值为传入的第二个值，而第二个格式化值却并没有值能够使用

解决办法：

​	要改变这种情况，可以直接将括号去掉，这样两个值就不会被看成一个整体，从而使得两个格式化值都有值能够使用



## 问题：为什么在向ili9806G设置显示矩形显示面积对象的宽度时需要将第一个传入的参数右移呢







## 为什么stm32的cpu会自动的读取寄存器中的程序指令并开始执行呢？

这是因为CPU的物理特性，在上电的不同阶段分别为： 取指令→解码（通过读取到的指令来理解具体要cpu做什么事情）→执行

请自行去计算机原理这本书中寻找答案

stm32与普通的电脑处理器的架构有所不同，普通的单片机使用的是哈佛结构
