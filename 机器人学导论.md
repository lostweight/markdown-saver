# 名词解释

向量（矢量）不仅可以描述一个body frame在世界坐标系中的具体位置

​		还可以描述出一个body frame相对于世界坐标系的方向	



**位置矢量**：表示对于一个坐标系来说，空间中任意的一个点的位置或方向，即
$$
^AP=\begin{bmatrix}
p_x \\
p_y \\
p_z
\end{bmatrix} = p_xi+p_yj+p_zk
$$
而位置矢量中的每个值，相当于**位置矢量**相对于**单位坐标系中各个轴的投影**得到的值

![image-20251015133326115](E:\Markdown\markdown\机器人学导论.assets\image-20251015133326115-1760506532012-1.png)

**姿态描述**：在笛卡尔坐标系下，可以使用固定在物体上的坐标系描述方位（姿态）
如下面所示：

<img src="E:\Markdown\markdown\机器人学导论.assets\image-20251015134035356.png" alt="image-20251015134035356" style="zoom:50%;" />

在上图中，我们可以看到矢量（向量）$^AP$​​ 描述的是机械手指之间的某点的位置，但这远远不够，我们还需要知道手的姿态，我们才能完全知道<u>手的位置</u> ，而姿态描述则是：上图中机械手指间的点为原点的固定坐标轴相对于参考坐标系{A}的描述



### 余弦

余弦：最根本的意义就是量化了两个方向之间的“夹角关系”，或者说描述了两个方向的“重叠程度”，方向越相同，余弦值越靠近1依此类推：

* 若$\theta=0°（方向完全相同）， cos\theta=1$
* 若$\theta=90°（方向垂直）， cos\theta=0$
* 若$\theta=180°（方向相反）， cos\theta=-1$

而姿态在三维的空间中，本质上就是**坐标系相对于参考坐标系的旋转关系**

为什么转换矩阵是3x3的格式，而不是向量矩阵的形式？

**姿态的核心：旋转轴+旋转角**





我们使用单位矢量 $\widehat{X}_B、\widehat{Y}_B、\widehat{Z}_B$ 来表示坐标系 $\{B\}$ 的主轴方向，当这个单位矢量的参考坐标系为 $\{A\}$ 的时候，又会使用 $^A\widehat{X}_B、^A\widehat{Y}_B、^A\widehat{Z}_B$ 来表示，我们将这三个矢量按顺序排列成一个3x3的矩阵，也就形成了我们的旋转矩阵

<img src="E:\Markdown\markdown\机器人学导论.assets\image-20251022134548635-1761111952342-1.png" alt="image-20251022134548635" style="zoom:67%;" />

其中每一列为一个矢量

​	因此一个点的位置能够使用一个矢量表示，而一个姿态则可以使用一个矢量矩阵来表示





----



### 奇异点

即 机器人的末端在一个固定点位，而机器人能够有多个不同的姿势到达这个点，即 逆解有无限个解（即：机器人的末端点位与机器人的姿势不是一一对应的）

当机器人接近或处于奇异点的时候，就会出现机械臂无法向某个方向运动或者承载能力下降的问题，因此我们一般都需要绕过这个点



----

### 姿态坐标系

姿态坐标系：左手定则：食指对应了roll轴、中指对应了yaw轴、大拇指对应了pitch轴，中指向地面，食指向前，其正方向为右手原则，并且其旋转顺序一般为ZYX即Yaw、Pitch、Roll，其中前-右-下的机体坐标系这三个角度为0



----



### **旋转矩阵**

旋转矩阵的每一列，代表旋转的新坐标系的X、Y、Z轴在原坐标系中指向哪里

比如说，
$$
\begin{bmatrix}
 0&-1&0\\
 -1&0&0\\
 0&0&1\\
 \end{bmatrix}
$$
其中第一**列**\[0, -1, 0\] → 新的X轴 = 原来的 -Y 轴



问题：为什么由两个单位矢量的点积可以得到两者之间夹角的余弦？为什么点积就能够计算这个重叠的长度，因为点积本质上不就是矢量相应的轴的长度相乘最后矢量各个轴的乘积相加，但是这为什么能够表示重合程度呢？这种计算和重合程度之间有什么关系？

回答：单位矢量的点积实际上计算出来的是 一条矢量的投影在另一条矢量上的长度（影子长度），而影子的长度实际上可以对应着相应的夹角，因此就说两个单位矢量的点积能够得到两者之间的夹角的余弦，实际上这个**余弦就是影子的长度**



----





### 姿态描述

如果抛开余弦的作用来看，实际上旋转矩阵的每一列就是记录了一个矢量，这个矢量是相对于参考姿态坐标系上的矢量，三个矢量（三列矢量）指的就是目标的姿态坐标系的单位矢量在原点与参考姿态坐标系的地方的各个单位矢量的相对于参考姿态坐标系的相应旋转后的姿态的对应关系（因为是相对于参考姿态坐标系的，所以需要参考姿态坐标系的各个单位矢量乘以这个旋转矩阵后才能得到真正的最终姿态坐标系）

不理解可看下图：

<img src="E:\Markdown\markdown\机器人学导论.assets\_cgi-bin_mmwebwx-bin_webwxgetmsgimg__&MsgID=52979443115804723&skey=@crypt_fd361ce7_647054282b60aea64f47e3d97340f478&mmweb_appid=wx_webfilehelper-1762012586348-2.jpg" alt="_cgi-bin_mmwebwx-bin_webwxgetmsgimg__&MsgID=52979443115804723&skey=@crypt_fd361ce7_647054282b60aea64f47e3d97340f478&mmweb_appid=wx_webfilehelper" style="zoom:50%;" />

![image-20251102130626904](E:\Markdown\markdown\机器人学导论.assets\image-20251102130626904-1762059990286-1.png)

<img src="E:\Markdown\markdown\机器人学导论.assets\image-20251102130751266-1762060073256-3.png" alt="image-20251102130751266" style="zoom:80%;" />

注：两个单位矢量的点积可得到二者之间的夹角的余弦

