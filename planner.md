运动学、动力学、控制：MuJoCo
  视觉、传感器、ROS、整机任务：Isaac Sim

  每个阶段末尾才进行一次跨平台验证，不要日常同时开发两套。

  ## 统一项目目标

  选择一款资料完整的机械臂作为学习对象，例如 Franka Panda，但分两步开展：

  1. 先用自己建立的2自由度平面臂验证数学。
  2. 再在 MuJoCo 和 Isaac Sim 中控制 Panda 完成视觉抓取。

  直接从7自由度机械臂开始，会同时遭遇冗余逆解、奇异点、关节限位和坐标约定，难以判断错误来源。

  最终仿真任务：

  Isaac Sim生成RGB-D图像
  → 实例分割识别目标
  → Mask与深度生成三维目标
  → TF转换到机器人基坐标系
  → 自己的IK或规划模块
  → 生成关节轨迹
  → MuJoCo/Isaac Sim执行
  → 抓取并放置

  ## 调整后的完整路线

  ### 第1～2个月：空间数学与正运动学

  白天：

  - 公司实例分割模型选型与训练
  - 比较高精度、实时和Transformer实例分割路线
  - 建立数据、指标和错误样本体系

  晚上：

  - 旋转矩阵、欧拉角、四元数
  - 主动旋转与坐标变换
  - 齐次变换
  - DH参数
  - 正运动学
  - TF父子关系

  仿真任务：

  1. 自建2自由度机械臂。
  2. 自己计算每个关节和末端的位姿。
  3. 将计算结果与 MuJoCo 中的位姿对比。
  4. 在不同关节角下自动测试100～1000组数据。
  5. 在 Isaac Sim/RViz中显示各级坐标轴。

  通过标准：

  位置误差 < 1 mm
  姿态误差 < 0.1°～0.5°
  能够解释每个TF的方向
  能够解释正pitch为何可能表现为低头

  实际阈值可以根据模型精度调整，重点是建立自动化误差测试。

  ### 第3个月：逆运动学与雅可比

  学习：

  - 2自由度解析逆运动学
  - 多解、无解和关节限位
  - 雅可比矩阵
  - 数值逆运动学
  - 阻尼最小二乘法
  - 奇异点和条件数
  - 7自由度冗余问题基础

  MuJoCo任务：

  随机生成目标点
  → 自己的IK求关节角
  → 下发关节目标
  → 检查末端是否到达

  再增加：

  - 不可达目标必须明确拒绝
  - 超限关节解必须过滤
  - 同一目标的多组解要能选择
  - 接近奇异点时不能数值爆炸

  通过标准：

  - 随机可达目标成功率达到预设水平，例如95%以上
  - 能输出位置和姿态误差
  - 能识别不可达、超限与奇异情况
  - 不依赖仿真器自带IK完成任务

  ### 第4个月：轨迹生成

  学习：

  - 三次、五次多项式
  - 梯形速度曲线
  - S曲线概念
  - 关节空间与笛卡尔空间轨迹
  - 速度、加速度和加加速度约束
  - 姿态插值与SLERP

  MuJoCo任务：

  初始位置
  → 预抓取位置
  → 抓取位置
  → 返回位置

  记录：

  - 关节位置
  - 关节速度
  - 关节加速度
  - 末端轨迹
  - 跟踪误差

  通过标准：轨迹连续、不超关节限制，末端能够稳定到达指定位置。

  ### 第5～6个月：动力学与控制

  学习：

  - 拉格朗日方法
  - 质量矩阵
  - 科氏/离心项
  - 重力项
  - 正动力学和逆动力学
  - PID
  - 重力补偿
  - 计算力矩控制
  - 基础阻抗控制

  建议先在2自由度机械臂上手推，再进入Panda。

  MuJoCo任务：

  1. 只用PD控制，观察重力下的静差。
  2. 加入重力补偿。
  3. 比较PD、PID和前馈加反馈。
  4. 改变负载，观察控制性能。
  5. 加入噪声、延迟和模型参数误差。

  这个阶段开始加入时序知识：

  - 采样与混叠
  - 低通滤波
  - 速度估计
  - 卡尔曼滤波
  - 轨迹误差分析
  - 系统辨识基础

  先解决真实控制信号问题，再学习LSTM或时序Transformer。

  ### 第7个月：碰撞检测与运动规划

  学习：

  - 配置空间
  - 碰撞检测
  - A*
  - RRT、RRT*
  - 轨迹平滑
  - 时间参数化
  - MoveIt的整体架构

  仿真任务：

  起点
  → 绕过桌面障碍物
  → 到达预抓取位姿

  要求：

  - 先手写二维或低维RRT理解算法
  - 再使用成熟规划库处理7自由度机械臂
  - 统计规划成功率、规划时间、路径长度和最小安全距离

  学习规划原理不等于生产项目必须拒绝成熟库。

  ### 第8～9个月：视觉接入机器人

  白天积累的实例分割模型在这里正式接入。

  Isaac Sim任务：

  RGB图像
  → 实例分割Mask
  → 从深度图选取目标点
  → 去除异常深度
  → 转换成相机三维坐标
  → 转换到机器人基坐标系
  → 生成抓取位姿
  → IK与规划

  需要补充：

  - 相机模型
  - RGB与深度对齐
  - 点云滤波
  - 目标三维中心
  - PCA或几何方法估计方向
  - 手眼标定
  - 抓取坐标系定义

  阶段任务逐级增加：

  1. 固定相机、固定物体。
  2. 固定相机、随机物体位置。
  3. 多个物体，按类别抓取。
  4. 遮挡、反光和背景变化。
  5. 抓取后放到指定区域。

  通过标准：

  - 目标位置误差可量化
  - TF方向正确
  - 随机摆放后仍能完成任务
  - 统计识别、定位、规划和抓取各阶段成功率
  - 失败时能判断是哪一个模块造成的

  ### 第10个月：构建机器人技能层

  把已经实现的能力封装为可靠技能：

  detect_objects()
  estimate_pose()
  move_to_pose()
  open_gripper()
  close_gripper()
  pick()
  place()
  return_home()

  这一步很重要。未来语言模型调用的是这些受约束的技能，而不是直接输出关节角或控制电机。

  同时建立状态机：

  感知
  → 选择目标
  → 规划
  → 抓取
  → 验证
  → 放置
  → 失败恢复

  ### 第11～13个月：实体机器人MVP

  再进入：

  - 机械设计与选型
  - 电机、减速器和编码器
  - STM32与CAN
  - 限位、急停和安全保护
  - 单关节台架
  - 多关节联动
  - 手眼标定
  - Sim-to-Real误差处理

  第一版可以使用现成驱动器。自研PCB、FOC和完整关节驱动放在第二版。

  ## 时序模型、语言模型和强化学习的位置

  调整后的顺序是：

  运动学
  → 轨迹
  → 动力学与控制
  → 传统时序处理/状态估计
  → 规划
  → 视觉抓取
  → 技能层
  → 实体MVP
  → 深度时序模型
  → LLM/VLM
  → 强化学习

  ### 时序模型

  从动力学与控制阶段开始学习，因为此时已经有大量仿真数据：

  - 关节位置、速度、电流或力矩
  - 轨迹误差
  - 末端受力
  - 抓取状态
  - 目标运动轨迹

  先学习滤波、卡尔曼估计和系统辨识，之后再用1D CNN、LSTM、GRU或Transformer做异常检测和状态预测。

  ### LLM/VLM

  在技能层完成后接入最合理：

  “把红色瓶子放进左边盒子”
  → LLM生成结构化任务
  → 安全检查
  → 调用detect、pick、place技能

  LLM不直接控制关节或输出PWM。

  ### 强化学习

  你会MuJoCo，这会成为明显优势，但强化学习仍应放在经典控制基线完成后。适合用于：

  - 抓取策略优化
  - 动态目标操作
  - 接触丰富的任务
  - 复杂动作策略
  - 模仿学习与Sim-to-Real

  如果没有传统控制基线，就无法判断强化学习究竟解决了问题，还是掩盖了建模错误。

  ## 每周执行模板

  公司分割方案尚未稳定时：

   时间      内容
  ━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   白天      公司实例分割研究、训练和部署
  ────────  ──────────────────────────────
   周一晚    机器人理论推导
  ────────  ──────────────────────────────
   周二晚    自己实现算法
  ────────  ──────────────────────────────
   周三晚    MuJoCo验证
  ────────  ──────────────────────────────
   周四晚    公司视觉实验复盘
  ────────  ──────────────────────────────
   周五晚    休息
  ────────  ──────────────────────────────
   周六      3～4小时机器人阶段任务
  ────────  ──────────────────────────────
   周日      Isaac Sim集成或测试总结

  视觉方案稳定后：

   时间      内容
  ━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━
   周一晚    新理论
  ────────  ────────────────────────
   周二晚    手写算法
  ────────  ────────────────────────
   周三晚    MuJoCo控制实验
  ────────  ────────────────────────
   周四晚    时序/状态估计
  ────────  ────────────────────────
   周六      阶段性机器人任务
  ────────  ────────────────────────
   周日      Isaac Sim视觉与ROS集成

  ## 必须建立的验证习惯

  每个阶段都保存四类结果：

  理论公式
  实现代码
  自动化测试
  仿真任务录像与指标

  尤其注意两个仿真平台和ROS之间的约定差异：

  - 角度使用度还是弧度
  - 四元数是 wxyz 还是 xyzw
  - 坐标轴定义
  - 父到子还是子到父的变换
  - 主动旋转还是被动变换
  - 关节零位与正方向
  - URDF、MJCF和USD模型的坐标约定