#     Git流程图

![img](https://www.runoob.com/wp-content/uploads/2015/02/0D32F290-80B0-4EA4-9836-CA58E22569B3.jpg)

Git有4个工作区域：工作区域、暂存区域、本地仓库、远程仓库

· workspace：工作区，就是平时用于存放项目代码的地方

· Index/Stage：暂存区，用于临时存放你的代码改动，事实上只是一个文件，保存即将提交到文件列表信息

· Repository：仓库区（或版本去）就是在本地的安全存放数据的位置，这里面有你所有版本的数据，其中HEAD指向最新放入仓库的版本

· Remote：远程仓库，托管代码的服务器，可以简单的任务是你项目组中一台电脑用于远程数据交换





## 将工作区更新的文件添加到暂存区

我们在更新的时候会要将新更新好的文件保存下来，并生成一个版本，而git中有一个暂存区的文件，我们在将更新好的文件保存并生成版本之前，应该先将这些更新好的文件使用`git add 文件地址`保存到暂存区中，然后再使用`git commit -m 版本名称`  来保存到本机的仓库中，并且生成相应的版本



## 查看仓库状态

```
git status

git status -s # 简约显示
```

· 红色表示新建文件或者先修改的文件都在工作区

· 绿色表示文件在暂存区



## 获得版本列表

使用`git reflog`就可以查看所有代码版本

![1697468551162](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1697468551162.png)



## 回退版本

 · HEAD表示当前最新版本

· HEDA^表示当前最新版本的前一个版本

· HEAD^^ 表示当前最新版本的前两个版本，以此类推

· HEAD～1表示当前最新版本的前一个版本

· HEAD～10表示当前最新版本的前10个版本，以此类推

```
git reset --hard HEAD^
```

![1697468333817](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1697468333817.png)

或者说我们直接输入版本号

然后使用上面的代码，我们的代码就可以回滚到我们指定的版本，但是注意，我们前面使用过的版本并不会立即被删除





![1703496021072](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1703496021072.png)

# git pull

如果远程仓库中的项目文件已经发生了更改，我们可以使用git pull这个命令来让我们本地的项目同步仓库中的项目



# git push

将我们要更新的仓库文件项目提交到远程仓库中，在执行这一步前，应该先使用git add命令将要提交的项目添加到提交缓存仓库中，并使用git commit命令将缓存仓库中的项目文件标上标签。最后我们可以将缓存仓库中标注好的文件提交到远程仓库中