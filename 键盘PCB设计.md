键盘的PCB整体由：最小系统的核心电路，只是最小系统电路，而不是最小系统板

1. MCU
2. 时钟电路
3. 复位电路
4. 电源电路
5. 开关矩阵
6. MCU 调试引脚、USB 通信引脚、



最小系统板：

**能让微控制器（MCU）独立正常工作的最小硬件版本**，它并不包含任何额外的功能，只是保留 MCU 运行必须的核心电路

| 核心部分                    | 作用（通俗理解）                                             | 举例（STM32 最小系统板）                                     |
| --------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 1. MCU 芯片                 | 最小系统板的 “大脑”，所有运算、控制都靠它                    | STM32F103C8T6（常用的 “蓝桥杯” 款芯片）                      |
| 2. 电源电路                 | 给 MCU 提供稳定电压（比如 STM32 需要 3.3V），过滤电压杂波（避免 “大脑” 供电不稳） | 3.3V 稳压芯片（如 AMS1117）+ 滤波电容                        |
| 3. 时钟电路                 | 给 MCU 提供 “时间节拍”（相当于人的心跳），决定 MCU 运行速度  | 8MHz 主晶振（负责运算速度）+ 32.768kHz RTC 晶振（负责精准计时） |
| 4. 复位电路                 | 让 MCU “重启”（比如程序卡死时），支持上电自动复位或手动复位  | RC 复位电路（上电自动复位）+ 复位按键（手动复位）            |
| 【辅助 1】下载 / 调试接口   | 给 MCU 烧录程序、调试程序（比如用 JTAG/SWD 接口）            | SWD 接口（2 根线，简化调试）+ USB 转串口模块                 |
| 【辅助 2】电源滤波 / 指示灯 | 进一步稳定供电，或用 LED 灯显示 MCU 是否在运行               | 电源指示灯（通电亮）+ 运行指示灯（程序运行时闪烁）           |







简化版本的 STM32 键盘实现思路目标：做一个 4×4 按键矩阵的有线键盘（USB 连接电脑，电脑识别为标准键盘，按按键输出对应字符）。

#### 1. 硬件选型与接线（核心部件，无多余设计）

| 部件                   | 型号 / 规格                                                  | 作用                                    |
| ---------------------- | ------------------------------------------------------------ | --------------------------------------- |
| MCU 核心（含最小系统） | STM32F103C8T6 + 自制最小系统电路（或直接用现成的 STM32 最小系统板调试） | 键盘 “大脑”，扫描按键、通信             |
| 按键矩阵               | 4×4 机械轴体（或轻触开关）+ 16 个 1N4148 二极管              | 输入按键，二极管防重影                  |
| 通信接口               | USB-C 接口座 + CH340G（USB 转串口芯片，可选，或用 STM32 自带 USB） | 与电脑通信（优先用 STM32 自带 USB HID） |
| 电源 / 辅助部件        | 3.3V 稳压电路（若用 USB 供电可省略，USB 直接转 3.3V）、滤波电容（0.1μF） | 稳定供电，过滤杂波                      |





# 1. 基础实验



具体的任务：

1. 使用现有的 stm32 建立一个基本的键盘矩阵系统，首先是 1x 4 的，然后是 4x4 的

    - [ ] 1 x 4
        - [ ] 能够与电脑进行连接，并识别为键盘
        - [ ] 能够将具体的按键映射为具体的按键，比如按键1、按键2 等等
        - [ ] 
    - [ ] 4 x 4 
        - [ ] 全键无冲

    

    

    各个部件的任务：

    1. 矩阵键盘：能让 stm 32 接收到触发信号
    2. stm32 ： 接收触发信号，并将信号传输给电脑
    3. 

    



# 串口是什么

# 串行是什么？

# 串口硬件是什么？ USB 属不属于 串口 硬件设备

# USB 使用的是什么电平协议？

usb 的通讯时序与信号电平和串口不同

通讯时许协议不同就不能叫做 串口