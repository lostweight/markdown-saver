[TOC]

# 算法





## 一、初识算法



### 1.1算法的定义

算法的定义：算法是指在有限时间内解决特定问题的一组指令或操作步骤，它具有以下的特性

·  问题是明确的，包含清晰的输入和输出的定义

·  具有可行性，能够在有限步骤、时间和内存空间下完成

·  各步骤都有确定的含义，相同的输入和运行条件，输出始终相同



### 1.2数据结构的定义

数据结构是计算机中组织和存储数据的方式，具有以下的设计目标：

· 空间占用尽量减少，节省计算机内存

· 数据操作尽可能快速，涵盖数据的访问、删除、添加、更新等

· 提供简洁的数据表示和逻辑信息， 以便使得算法高效运行









## 深度优先算法



[算法：深度优先遍历和广度优先遍历_广度优先遍历和深度优先遍历-CSDN博客](https://blog.csdn.net/zhizhengguan/article/details/122468043)





## RRT算法：

RRT（快速随机探索树），它是通过维护一颗路径树：在空间中随机采样，并找到路径树上与采样点最接近却能与它无障碍地连接的点，并将这个点当作采样点（最近点），同时将采样点加入路径树，直到到达终点附近被探索到

原理步骤：

样例图：

![在这里插入图片描述](E:\Markdown\markdown\算法.assets\72b2b34195d4631ac78f737da8a159a1-1747963466678-3.png)

如图所示，红色的圆是**起点**，黄色的圆是**终点**，黑色是**障碍物**

1. 在空间中随机采样

如图中的蓝色圆， 我们将其作为目标点

![在这里插入图片描述](E:\Markdown\markdown\算法.assets\b17ec56b1d655bd58c8f8f283da94626-1747963634471-7.png)



2. 确定生长树的生长方向

以刚才生成的**随机点**为**目标点**，遍历生长树上的所有点，找到生长树中距离目标点的距离最近的那个点作为**最近点**，并以最近点为基点，目标点相对于基点的方向作为生长树的方向，如下图所示的红色箭头：

![在这里插入图片描述](E:\Markdown\markdown\算法.assets\c7430193bbece72be0fdc8a349040d31-1747963932950-11.png)



3. 向目标点生长

生长树的生长步长是固定的，如同神经网络的固定学习率（learning_rate)一样，过小会导致规划的时间过长，太大会直接错过目标点

我们在下图中通过固定的生长步长，以及方向，就能在生长方向的指定距离处得到一个生长点（如下图的紫点）

![在这里插入图片描述](E:\Markdown\markdown\算法.assets\c0f4fd3de591f89b9f85f11bd587faea-1747964177531-15.png)



4. 重复第一第二步

* 随机采样：

![在这里插入图片描述](E:\Markdown\markdown\算法.assets\80e13550360abd645b5ad2a0041c4494-1747964361768-19.png)

遍历生成树中的所有点，然后找出<u>生成树中距离目标点最近的点</u>作为**最近点**，然后以随机点与最近点的连线作为生长的方向

![在这里插入图片描述](E:\Markdown\markdown\算法.assets\d1dbe3061407175c6bd9513fce036dda-1747964462447-23.png)

* 生长

并以生长步长沿着生长方向生长出一个生长点，如下图（紫圆）

![在这里插入图片描述](E:\Markdown\markdown\算法.assets\91f0d8530648a9b02cf1514a0c6615b3-1747964540121-30.png)



* 继续随机生长





算法步骤：

* 初始化整个空间，定义初始点、终点、采样点数、点与点之间的步长t等信息
* 在空间中随机产生一个点xrand
* 在已知树的点集合中找到距离这个随机点最近的点xnear
* 在xnear到xrand的直线方向上从xnear以步长t截取点xnew
* 判断从xnear到xnew之间是否存在障碍物，若存在则舍弃该点
* 将new点加入到树集合中
* 循环2~6，循环结束条件：有一个new点在终点的设定邻域内





## RRT\*算法

学习地址：[RRT*算法原理详解+MATLAB演示 - 知乎](https://zhuanlan.zhihu.com/p/709123692)

RRT\*算法是在RRT的基础上增加了两个功能：

1. rewrite\(重写\)

在生成随机的生长点p\_new过后，在新生成的随机点附近以定义的半径范围内寻找“近邻”（在定义的半径范围内的所有点），并依次计算“近邻”节点到起点的代价距离，根据计算这些紧邻节点的路径代价，选出一个从起始点到生长点代价最小的路径，并将该路径中生长点的上一级最近点作为改生长点的父节点

2. random relink



* rewrite具体步骤

如下图：

![img](E:\Markdown\markdown\算法.assets\v2-a1056b72f6c135f812d4cfec72c3f9e8_1440w-1747967755243-34.jpg)

我们在生成了一个全新的生长点过后，以生长点为基准，在生长点的指定半径范围内寻找“近邻点”，作为替换生长点的**父节点的备选**，并计算该**新生成点到备选父节点的代价值**，<u>以及</u>这些备选父节点到起始点的代价值，**并将这个对应的到备选父节点的代价值以及备选父节点到起始点的代价值相加**，作为**生长点选择对应父节点的路径代价值**，找到最小的代价值的那个备选父节点并作为新的父节点

在这里我们的9号生长点的父节点原本为4号生长点，然后再指定的半径范围内找到其他的备选父节点，分别为：5、8、6号生成点，然后我们将新生成点（p_new）与备选的父级生成点的到初始点的代价路径值计算出来，然后找到从新生成点到父级生成点到初始点的最小代价路径，并以这条路径中的新生成点的上一级的备选父节点作为新的父节点，即：

| 父节点 | 路径      | 代价       |
| ------ | --------- | ---------- |
| 4      | 0-4-9     | 10+4=14    |
| 5      | 0-1-5-9   | 3+5+3=11   |
| 6      | 0-4-6-9   | 10+5+1=16  |
| 8      | 0-1-5-8-9 | 3+5+1+3=12 |

从上表可以看出选择5号节点为新的父节点后路径的代价值见笑了，因此更改新生长点的父节点为5，因此新的生成树如下：

![img](E:\Markdown\markdown\算法.assets\v2-3204333a3938481a2e087a239449cc5d_1440w-1747969940623-42.jpg)





* random relink步骤：

在为P_new重新选择父节点之后，为进一步使得随机树节点间连接的代价尽量小，为随机树进行重新布线。过程也可以被表述成：如果近邻节点的父节点改为P_new节点可以减小路径代价，则进行更改。如下图：

![img](E:\Markdown\markdown\算法.assets\v2-66073e31ae4ce3b72ca3a0b38b56a08b_1440w-1747969989806-46-1747969991532-48.jpg)

9节点为新生成的节点 P_new节点，其近邻节点分别为节点4 , 5，6 , 8 ，它们父节点分别为节点0 ,1，4 , 5，它们原来的路径和代价如下表：

| 临近节点 | 父节点 | 路径    | 代价      |
| -------- | ------ | ------- | --------- |
| 4        | 0      | 0-4     | 10        |
| 5        | 1      | 0-1-5   | 3+5=8     |
| 6        | 4      | 0-4-6   | 10+5=15   |
| 8        | 5      | 0-1-5-8 | 3+5+1+3=9 |

如果改成以P_new节点9作为近邻节点4，5，6，8的父节点，则它们的新路径和新代价如下表：

| 临近节点 | 父节点 | 路径      | 代价       |
| -------- | ------ | --------- | ---------- |
| 4        | 9      | 0-1-5-9-4 | 3+5+3+4=15 |
| 5        | 9      | 0-4-9-5   | 10+4+3=17  |
| 6        | 9      | 0-1-5-9-6 | 3+5+3+1=12 |
| 8        | 9      | 0-1-5-9-8 | 3+5+3+3=14 |

由上表可知，如果将4，5，8节点的父节点改为9节点路径代价大于原来的路径代价，因此不改变4，5，8节点的父节点。

如果改变6节点的父节点为9节点路径代价为小于原来的路径代价，因此将6的父节点改为节点9，生成的新随机树如图下图：

![img](E:\Markdown\markdown\算法.assets\v2-831ebdee59b561b3d86aed21ac8f82eb_1440w-1747969668063-38.jpg)

重布线过程的意义在于每当生成了新的节点后，是否可以通过重新布线，使得某些节点的路径代价减少。如果以整体的眼光看，并不是每一个重新布线的节点都会出现在最终生成的路径中，但在生成随机树的过程中，每一次的重布线都尽可能的为最终路径代价减小创造机会。









RRT算法与RRT\*算法之间最大的区别就在于，RRT算法的路径不一定是最佳的路径，而RRT\*算法是最佳的





