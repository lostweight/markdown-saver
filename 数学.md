## 乘法的意义是什么？

乘法就是一把瑞士军刀，这个乘法具有很多种作用，相当于有很多个刀头，主要是有三个刀头：

1. 重复累加刀
    用于：一次性的重复叠加
2. 比例叠加刀
    用于：把两个尺子拼接成二维面积尺，并且这个尺子能够使用比例进行计算后再进行叠加，相当于缩放刀与重复叠加刀
3. 缩放镜刀
    用于：这个缩放刀能够将测量的东西将测量的东西按照比例进行计算，从而得到测量的东西的某个比例的值



## 那为什么乘法会后面衍生成了积分，微分这样的形式？他的作用真的就是累加、比例叠加缩减嘛？



## 卷积

卷积的意义：https://www.bilibili.com/video/BV1VV411478E/?spm_id_from=333.1387.favlist.content.click

![image-20250324183155960](E:\Markdown\markdown\数学.assets\image-20250324183155960.png)

这个公式可以理解成：

两个函数$f(x)$、$g(x)$组成一个系统，其中$x$为时间，在x时发生了$f(x)$这件事情，受到的$t-x$范围内发生的$g(t-x)$​这件事的影响，即**现在发生的这件事情收到过去发生的事情的累计影响**

将这个映射成我们所学到的卷积神经网络进行视觉处理，我们可以将具体的卷积机制理解成，**在指定的像素点f(x)与指定卷积核的对应t-x位置的权重之间的关联程度，关联程度高则输出卷积操作后的保留值就越高**，简单点理解就是卷积核就是图像特征的像素关系，即特征的检测器

**在数学上，卷积就是将一个函数作为一个输入，另一个函数当作“滑动权重”（固定且变化率确定的随事件变化的权重），一次性算出“过去所有的输入对当前时刻的加权总和”**





在数学上，卷积核实际上就是周围指定位置的像素值，对中心像素的







### 影响的数学定义

过去每一个输入的事件，都会在后续所有时刻持续产生一个输出的分量，其中的每个分量，就是这个输入的事件对于后续某个时刻的影响的多少



同时**影响的多少是一个线性的系统（已知，并且指定时刻的输出已知），并且满足叠加原理**

比如：先敲一下锣、再敲一下锣，最终声音 = 第一次敲击的余音 + 第二次敲击的余音，二者互不干扰，直接相加。

既然可以分开算再加总，把过往所有时刻的单点影响全部累加，结果自然就是**当下时刻受到的全部总影响**。









### 但是我又有一点疑惑，那就是为什么有的时候在图像处理的时候会使用到卷积核对图片进行处理，然后图片就能将相应模板的特征保留下来呢？在图像处理中的卷积核又有什么作用呢？他和视觉模型的卷积核之间有什么关系，还有一个就是，这个计算机视觉中，卷积操作就是加权求和，那信号处理这里呢？不也是加权求和吗？信号处理计算的累计影响，不也是一种随变量的变化而累加的加权求和吗？那这个加权求和和向量内积有什么关系？为什么向量内积能够去计算出与指定纹理的类似程度呢？为什么这样说，为什么向量积就能够代表类似程度我感觉很迷惑，为什么数学能够代表出这些意义

​	
