单片机最小系统指： 让 stm32 这个主控能够正常运行的**最少且必须的器件所组成的系统** 

 当该系统建立好后，此时没有任何的功能，而在确保系统正确之后加入相应的功能模块，此时 stm32  能够让单片机具有实际的功能

最小系统包括：

```
主控
电源电路
晶振电路
复位电路
下载电路

滤波电路
BOOT 启动电路
```



STM32F103CBT6



# 杂七杂八

stm32 中的引脚的含义：

| 引脚名 | 作用                                                         |
| ------ | ------------------------------------------------------------ |
| VDD    | 器件的（数字）正电源接口，为芯片内部的数字电路、核心逻辑（CPU、寄存器、GPIO 控制器登）提供工作电压 |
| VSS    | 器件的（数字）地引脚，作为内部电路的电流回流路径，是整个芯片的参考点位零点 |
|        |                                                              |





## 为什么 stm32 会有模拟电源引脚？他不是数字微控制器嘛，为什么会需要模拟电源？

因为 STM32 内部集成了大量的模拟功能模块，

一、VSS 和 VDD 的基本定义和作用

​	这两个标识是通用的命名规则，核心是给芯片内部电路供电或提供参考地

二、为什么会有模拟电极？
		STM32 虽然是数字微控制器，但内部集成了大量的模拟功能模块（如 ADC\[模数转换器\]、OPA\[运算放大器\]等等），这些功能模块的工作特性和数字电路差异极大，因此需要独立的模拟电源引脚



## stm32 的 VSSA 和 VDDA 和 VDD 和 VSS 有什么区别？为什么他会有模拟输入和数字输入呢？一般stm32 是通过什么接口接入电源以让stm32 芯片正常供电的呢？如果使用的是 VDDA 来供电的话，那 STM32 是怎么件规模你电源转换为数字电源输出的呢？

STM32 里面的 VDD/VSS 和 VDDA/VSSA 核心区别是**供电对象**和**抗干扰的要求**不同
	而模拟输入和数字输入的划分是因为芯片要处理两种不同类型的信号；

STM32 芯片的**主供电靠 VDD 引脚**引入，而 VDDA 仅给模拟模块供电，且并不存在“ VDDA 转数字电源”的说法，二者是并行供电的关系，下面是他们的核心区别

| 引脚组    | 类型     | 供电对象                              | 核心作用                       | 抗干扰要求               |
| --------- | -------- | ------------------------------------- | ------------------------------ | ------------------------ |
| VDD/VSS   | 数字电源 | CPU、GPIO、定时器、串口等**数字模块** | 给芯片核心逻辑电路提供工作电压 | 一般，可容忍轻微数字噪声 |
| VDDA/VSSA | 模拟电源 | ADC、DAC、内部参考电压等**模拟模块**  | 给高精度模拟电路提供纯净供电   | 极高，                   |

也就是说，STM 32 芯片主要是通过数字电源供电，



## 什么叫做低电平，什么叫做高电平，这个电平的概念是什么意思？

**电平**  是指电路中某点的 **电压高低状态**  

而在数字电路中，电平并不是一个绝对的电压值，而是一个电压的范围

例如在 STM32 最常用的 3.3v 供电为例：

高电平通常代表逻辑 1， 而在 stm32 系统中，输入高电平的最低阈值通常为 2.0V，也就是说输入引脚的电压  ≥ 2.0V的时候就被认定为高电平

低电平通常代表逻辑 0， 而在 stm32 系统中，输入低电平的最低阈值通常是 0.8V，也就是说只要输入引脚的电压为 ≤ 0.8V 的时候就会被认定为低电平



## 我还是没能明白这个数字电路的含义，为欸什么就都是高阻抗呢？难道说是因为通过高阻抗来作为为数字电路才是正确的？明明电压可以通过电流和电阻共同决定，但是为什么就是非要就只是使用高阻而不是高电流呢？

这是因为 数字电路的输入引脚，它的任务是 “**识别外部电压是高还是低**”，而不是 “**从外部获取大电流进行工作**”

我们可以将 数字芯片的输入引脚，当作是 “电压检测器”

* 它只需要 “感知” 引脚的电压，就像是电压表一样，电压表的内阻极大，就是为了不影响被测电路的电压
* 而数字输入引脚的高阻抗，和电压表的高阻抗是一个道理：都是**尽可能不消耗外部电路的电流**，**不干扰外部信号的电压状态**

### 为什么必须是“高阻抗输入”？

1. 高阻抗 ≈ 几乎不耗电，低功耗 + 不干扰外部电路
2. 高阻抗 = 抗干扰能力强，逻辑状态更稳定
    数字电路一般工作在复杂的环境中（比如说有电机、继电器的电磁干扰），这些干扰通常以 “**干扰电流**” 的形式存在，如果使用“ **低阻抗高电流** ”的方式，则电流会产生较大的波动，进而产生电压的波动导致逻辑混乱
3. 高阻抗 = 易集成、易扩展、适合大规模的数字电路
    要做成大规模的数字电路，则必须要满足 “引脚之间的互不影响”，而 高阻抗输入的电路，相当于彼此之间相互绝缘，每个引脚仅仅感知自己的电压，不会因为其他引脚的状态而产生电流串扰







## 移位寄存器是什么？

### 寄存器

寄存器是 CPU 用来暂存指令、数据和地址的电脑存储器，也可以说寄存器就是软件与硬件沟通的桥梁，软件通过寄存器读取硬件状态，同时能通过设置寄存器来控制硬件





## \_\_attribute\_\_((weak))  这个修饰符的作用是什么？

这是一个 GCC/Clang 编译器的**专属扩展指令**

它本质上是 <u>编译器说明符</u>， 与 static/const/extern 同级，可以写在 **函数 / 变量 / 结构体定义 / 声明** 之前

并且所有的生成开销是在：仅编译 / 链接阶段生效，无运行开支

该指令主要的用处属性有：

1. 链接属性：作用与编译过后的链接阶段，控制 函数 / 全局变量 的优先级、链接规则
    像是 \_\_attribute\_\_((weak)) 弱定义，该指令将被修饰的 函数 / 全局变量 标记为 **弱符号**， 而普通无修饰的定义为 **强符号**，当链接阶段时遇到多个文件中有同名的，优先保留标记为 强符号（未标记为 \_\_attribute\_\_ 的）的

     这种强定义 与 弱定义是为了 C 语言多文件开发的刚需，让 C 语言能够提供 默认实现，允许用户重写覆盖，同时解决多文件同名符号的冲突，因为如果多个 C 语言文件中定义了同名的 强符号函数，则就会报出 多重定义 错误

2. 编译阶段代码检查属性：在编译的阶段让编译器给我们做 严格语法 / 逻辑检查，提前发现 bug
    像是：
     \_\_attribute\_\_((unused)) 这个指令就是用于告诉编译器，我明确知道这个而函数 / 变量未被使用，**抑制 未使用变量 / 函数 的编译警告**



## 手册文档分类说明

STM32 的官方手册会有不同的用途分别，分别对应了 **硬件选型 / 硬件设计 、全功能原理 + 软件编程 、专项操作方案**

主要分为：

### 芯片数据手册（Datasheet）

* 命名特征：无固定前缀，一般以芯片型号命名（如 `STM32F103CBT6 规格书`），单型号专属文档。

* 覆盖范围：**仅针对某一款 / 某一小批同封装芯片**（比如你的 F103 中密度系列）。

<u>核心内容（只讲硬件参数、引脚、物理特性）</u>

适用于：**硬件工程师画原理图、PCB、芯片选型、焊接调试**；查引脚编号、电气极限、晶振匹配电容、电源滤波参数；



### 芯片参考手册（Reference Manual）

* 命名特征：前缀 `RM+数字`（RM0008），**按芯片系列划分**，不是单芯片。

RM0008 覆盖 **整个 STM32F1 全系列**（F101/F102/F103/F105/F107），你手里的 F103CBT6 完全兼容。

* 篇幅：几百页～上千页，是 STM32 **最全的官方技术手册**。

<u>核心内容（硬件逻辑 + 软件编程 双核心）</u>

 核心用途：**软件开发、底层驱动编写、理解芯片内部工作逻辑**； 硬件进阶：查外设时序、电路工作逻辑（比如晶振起振原理、复位电路规则）；



### 应用笔记（Application Note）

* 命名特征：前缀 `AN+数字`（AN2606），**专项专题文档**，全系列通用；

* 定位：官方**实操教程、方案案例、问题排查指南**，是对 RM/Datasheet 的**补充落地文档**。
* 

#### 三类文档对比总表

|   文档类型    |     标识     |     覆盖范围      |                 核心内容                 |           核心使用场景           |                 对应你的 Boot 问题分工                  |
| :-----------: | :----------: | :---------------: | :--------------------------------------: | :------------------------------: | :-----------------------------------------------------: |
|   Datasheet   | 芯片型号命名 | 单款 / 同型号芯片 |  引脚、电气参数、封装、极限值、功能简介  | 选型、画原理图 / PCB、查硬件参数 |             查 BOOT0 引脚位置、引脚电气耐压             |
| 参考手册 (RM) |    RMxxxx    |   整个芯片系列    | 内部逻辑、寄存器、时序、真值表、完整规则 |    写驱动、学原理、查逻辑表格    | 查 **BOOT0/BOOT1 电平组合、采样规则**（核心表格在这里） |
| 应用笔记 (AN) |    ANxxxx    |    全系列通用     |      专项实操、电路方案、案例、排错      |   功能落地、电路设计、问题排查   |        查 Boot 电路接法、串口烧录流程、故障排查         |



#### 二、为什么 ST 要拆分这么多文档？

很多新手疑惑 “为什么不合成一本”，核心是**分工 + 效率 + 复用**，结合工业场景解释：

1. 受众不同，拆分降低阅读成本

    - 硬件工程师：只关心引脚、电压、尺寸 → 看薄薄的 Datasheet 即可，不用啃上千页的参考手册；
    - 软件工程师：只关心外设逻辑、寄存器 → 看 RM，不用关注封装、电流参数；
    - 做专项功能（如串口下载、低功耗）：直接看短小的 AN，不用在大手册里翻找。

    

2. 复用性最大化

    

    STM32F1 上百款芯片（F101/F103/F105...）

    内核、外设逻辑完全一致

    ，所以只用一份 RM0008 即可覆盖全系列，不用每款芯片单独写大手册；

    

    而每款芯片引脚、封装不同，所以单独做 Datasheet。

3. 版本迭代灵活

    

    芯片硬件参数基本不变（Datasheet 极少更新）；

    

    外设逻辑、实操方案会持续优化（RM、AN 可单独升级，互不影响）。

简单类比：

- Datasheet = 手机**硬件规格单**（屏幕尺寸、电池电压、接口位置）；
- RM = 手机**完整系统说明书**（所有功能、设置、底层逻辑）；
- AN = 手机**专项使用教程**（比如 “如何进入刷机模式”“如何省电”）。





# STM32 三种启动模式

stm32 芯片上电之后，会**根据 BOOT0、BOOT1 两个引脚的电平**，来决定 CPU 从哪一块存储器读取程序运行，而这三种启动方式本质就是 **程序的起始地址不一样**：

1. 主内存启动（BOOT0=0，最常用）

程序烧录在芯片内部**Flash 闪存**里，上电直接跑 Flash 里的代码。我们平时下载程序、正常设备运行全是这个模式。Flash 掉电数据不丢失。

2. 系统存储器启动（BOOT0=1、BOOT1 任意）

系统存储器是 ST 出厂固化好的一段专用 ROM，里面预装了**串口 ISP 下载程序**。

作用：不用仿真器，用串口就能给 Flash 刷程序；比如老款串口下载、一键烧录模式都靠它。这段出厂代码用户不能修改。

3. 片内 SRAM 启动（BOOT0=1，BOOT1=1）

SRAM 是运行内存，掉电数据清空。一般**调试临时用**：把代码拷贝进 RAM 运行，速度极快，用于调试、测试小段程序，产品成品几乎不会用这个模式。

简单总结：三个启动只是**换程序存放的启动位置**，硬件内核本身没变。





# 