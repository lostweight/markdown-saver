[TOC]

名词解释

什么是shell

shell是一个命令解释器，是系统的用户界面，提供了用户与内核进行交互操作的一种接口，他接收用户输入的命令并把它送进内核去执行。



什么是gcc

gcc是可以在多种硬件平台上编译出可执行程序的超级编译器，gcc能够将多种语言源程序编译连接成可执行的文件



什么是jdb

jdb是java debugger的缩写，他是java开发工具包中的一部分，用于在java程序中进行调试和跟踪错误，使用jdb可以在运行java程序时暂停程序的执行，并允许开发人员逐行执行代码，查看变量的值、设置断点、监视变量的变化等



什么是ssh

SSH是一种较可靠，专为远程登录会话和其他网络服务提供安全性的协议，利用SSH协议可以有效防止远程管理过程中的信息泄露问题

linux一般作为服务器使用，我们通常使用ssh服务远程登录到linux服务器来管理维护系统

SSH由服务器和客户端组成，在整个通信过程中，为建立安全的SSH通道，会经历一下几个阶段：

连接建立：SSH服务器在指定的端口侦听客户端的连接请求，在客户端向服务器发起连接请求后，双方建立一个					TCP连接

版本协商：SSH协议目前存在SSH1.X（SSH2.0之前的版本）和SSH2.0版本。SSH2.0协议相比SSH1.X协议来说，					在结构上做了扩展，可以支持更多的认证方法和密钥交换方法，同时提高了服务能力。SSH服务器和					客户端通过协商确定最终使用的SSH版本号。

算法协商：SSH支持多种加密算法，双方根据各自支持的算法，协商出最终用于产生会话密钥的密钥交换算法、用					于数据信息加密的加密算法、用于进行数字签名和认证的公钥算法以及用于数据完整性保护的HMAC					算法。

密钥交换：服务器和客户端通过密钥交换算法，动态生成共享的会话密钥和会话ID，建立加密通道。会话密钥主					要用于后续数据传输的加密，会话ID用于在认证过程中标识该SSH连接。



用户认证：SSH客户端向服务器端发起认证请求，服务器端对客户端进行认证。SSH支持以下几种认证方式：

- 密码（password）认证：客户端通过用户名和密码的方式进行认证，将加密后的用户名和密码发送给服务器，服务器解密后与本地保存的用户名和密码进行对比，并向客户端返回认证成功或失败的消息。
- 密钥（publickey）认证：客户端通过用户名，公钥以及公钥算法等信息来与服务器进行认证。
- password-publickey认证：指用户需要同时满足密码认证和密钥认证才能登录。
- all认证：只要满足密码认证和密钥认证其中一种即可。



会话请求：认证通过后，SSH客户端向服务器端发送会话请求，请求服务器提供某种类型的服务，即请求与服务器					建立相应的会话。

会话交互：会话建立后，SSH服务器端和客户端在该会话上进行数据信息的交互。



什么是vim

vim是从vi发展出来的一个文本编辑器，并且还有代码补完、编译及错误跳转等方便编程的功能，广受程序员的欢迎

vi/vim 的使用

　　基本上 vi/vim 共分为三种模式，分别是命令模式（Command mode），输入模式（Insert mode）和底线命令模式（Last line mode）。 这三种模式的作用分别是：

　　命令模式：

　　用户刚刚启动 vi/vim，便进入了命令模式。

　　此状态下敲击键盘动作会被Vim识别为命令，而非输入字符。比如我们此时按下i，并不会输入一个字符，i被当作了一个命令。

　　以下是常用的几个命令：

　　i 切换到输入模式，以输入字符。

　　x 删除当前光标所在处的字符。

　　： 切换到底线命令模式，以在最底一行输入命令。

　　若想要编辑文本：启动Vim，进入了命令模式，按下i，切换到输入模式。

　　命令模式只有一些最基本的命令，因此仍要依靠底线命令模式输入更多命令。

　　输入模式

　　在命令模式下按下i就进入了输入模式。

　　在输入模式中，可以使用以下按键：

　　字符按键以及Shift组合，输入字符；

　　ENTER，回车键，换行；

　　BACK SPACE，退格键，删除光标前一个字符；

　　DEL，删除键，删除光标后一个字符；

　　方向键，在文本中移动光标；

　　HOME/END，移动光标到行首/行尾；

　　Page Up/Page Down，上/下翻页；

　　Insert，切换光标为输入/替换模式，光标将变成竖线/下划线；

　　ESC，退出输入模式，切换到命令模式；

　　底线命令模式

　　在命令模式下按下：（英文冒号）就进入了底线命令模式。

　　底线命令模式可以输入单个或多个字符的命令，可用的命令非常多。

　　在底线命令模式中，基本的命令有（已经省略了冒号）：

　　q 退出程序；

　　w 保存文件；

　　按ESC键可随时退出底线命令模式。

　　简单的说，我们可以将这三个模式想成底下的图标来表示：
![Linux vim的使用方法](https://img-service.csdnimg.cn/img_convert/3854fb29b9316f74e0a4a15396d07a1d.png)





什么是嵌入式文件系统

嵌入式文件系统是指嵌入式系统中实现文件存取、管理等功能的模块，提供一系列的文件输入输出等文件管理功能，为嵌入式系统和设备提供文件系统支持，



什么是文件系统

文件存储系统用于明确存储设备或分区上的文件的方法和数据结构，即在存储设备上组织文件的方法。操作系统中负责管理和存储文件信息的软件机构被称为文件管理系统，简称文件系统



选择题

环境变量

linux观看内存使用情况的命令

free、top



显示文本文件的内容的命令

cat

​	

分页显示文件的命令

more + 要显示的文件名



默认情况下管理员创建一个用户的时候会在哪里创建同样的文件

/home目录下



添加用户组使用什么命令

groupadd + groupname



删除用户使用什么参数能够将用户的主目录也一并删除

userdel -r        # 在删除用户的同时，递归删除用户的主目录



一般用户用于存放用户密码的目录是什么

etc/shadow



填空题

创建文件使用什么命令

touch



查看文件内容用什么

cat、head、tail



用vim要存盘退出使用什么命令

wq



vim的两种操作模式，

输入模式，命令模式，底线命令模式



在linux中压缩一个包使用什么命令

tar -zcvf



在vim中使用什么键能够切换模式

1.正常模式：esc键，主要用来浏览和修改文本内容的

2.插入模式：i、a、o、s，用来向文本中添加内容的

3.命令模式：shift+：

4.可视模式：v或V，用于选择文本块并对其执行操作，例如复制粘、剪切



rm命令的参数，在删除不同的参数

不存盘退出

q!



创建.gz的压缩包使用什么命令

tar -zcvf  \*\*.gz  file1 file2



使用什么命令查看进程

ps



产生进程的函数是什么命令

fork()：通过系统调用创建一个与原来进程几乎完全相同的进程，也就是两个进程可以左完全相同的事情，也可以做完全不同的事

一个进程调用fork（）函数后，系统先给新的进程分配资源，例如存储数据和代码的空间。然后把原来的进程的所有值都复制到新的新进程中，只有少数值与原来的进程的值不同。相当于克隆了一个自己。

system()





编程题

写shell脚本，比较两个数的大小

```
read -p "请输入第一个：" num_1

read -p "请输入第二个："  num_2

res = \`awk -v num1=$num_1 -v num2=$num_2 'BEGIN{print(num1>num2)? "num1>num2": "num1<num2"}'\`

echo $res
```



用shell脚本实现显示日期与时间的功能

```
i=100

while [ $i < 100 ];

do

​	current_time=$(date -d today "+%Y年%m月%d日—%H点%M分%S秒")

​	echo $current_time

done
```



在根目录下有四个目录，用shell脚本生成4个目录，然后再这4个目录中生成txt文件，

1.自动创建目录

2.创建目录文件

```
for i in {1..4}
do	
	dir_name=`uuidgen`
	mkdir -p $dir_name && cd $dir_name
	for j in {1..4}
	do
		touch `uuidgen`.txt
	done
	cd ..
done


```

使用for循环在usr目录下

创建10个不同随机字符的目录

```
for _ in {1..10}
do
	dir_name=`pwgen 5`
	mkdir $dir_name
done
```



编写脚本

1.包含一段注释，包含用户名，时间

2.写问候

3.显示日期以及时间

4.显示日历

5.显示机器名

6.显示当前操作系统的版本名，以及名字





1.在某时间删除目录的子目录以及文件

2.在指定的时间将指定的文件data归档压缩，并备份，指定为某个名字

3.在指定的时间，将ide接口的第二个逻辑分区卸载

```
i=0

while [ $i == '0' ];

do

​    current_time=$(date -d today "+%H%M")

​    if [ $current_time -ge "1600" ];

​    then

​        rm -r /home/m0pbys/everyday_be_create.tar.gz

​        tar -czf /home/m0pbys/everyday_be_create.tar.gz /home/m0pbys/linux_exercise

​        i=$i+1

​    fi

done
```



写出一个makefile脚本文件



简答题

常见的linux发行版本有，举例6个

RedHat， ubuntu， C entOS，Deepin，Debian，SUSE，Fedora



描述linux目录下的目录颜色以及含义

白色：表示普通文件

蓝色：表示目录

绿色：表示可执行文件

红色：表示压缩文件

浅蓝色：链接文件

红色闪烁：表示链接的文件有问题

黄色：表示设备文件

灰色：表示其他文件





描述如何更换下载源

1.首先备份原来的下载源使用cp /etc/apt/sources.list /etc/apt/sources.list.bak

2.使用命令sudo vim /etc/apt/sources.list打开下载源文件

3.使用命令模式：%d删除所有下载源文件的文本内容

4.将新的源文本内容重新写入到/etc/apt/sources.list文件中

5.按esc键进入命令行模式，并输入wq进行写入退出

6.使用sudo apt update 和sudo apt upgrade命令来更新软件包



描述linux系统的启动过程

1.主机加电自检，加载bios硬件信息

2.读取MBR的引导文件（GRUB、LILO）

3.引导Linux内核

4.运行第一个进程init

5.进入相应的运行级别

6.运行终端



ls -l所有属性的含义

1.最开头处-表示普通文件，d表示目录，l表示符号链接，c表示字符设备文件， b表示块设备文件， s表示套接字文件， p表示管道文件

2.r表示读取权限，w表示写入权限， x表示执行权限，-表示没有对应权限

3第三个表示硬链接数量

4.为文件所有者

5.所有者的组别

6.文件大小

7.文件修改时间

8.文件名称





描述在linux中如何增删改一个用户

1.使用命令useradd或adduser，adduser在使用过程中会主动将设置参数的程序执行，让我们输入密码等用户信息，而useradd如果不指定的话就直接在root目录下创建一个该用户的主目录，并且不会要求我们设置密码等信息，若需要在指定家目录创建用户的话，则需要使用useradd -d + 指定家目录的路径地址 + 用户名，来创建用户以及指定主目录，

2.给用户指定或修改密码，则我们需要使用passwd + 用户名的形式进行设置或修改用户的密码，

3.删除用户，使用命令userdel + 用户名来删除指定的用户，如果需要把该用户的家目录也一并删除，则可以使用userdel -r + 用户名来将用户删除，同时将其主目录也一并删除

4.修改用户组，当我们需要把用户组别修改到新的组的时候，我们可以使用usermod -g +新用户组 + 用户名来进行修改













系统休眠与虚拟内存



### 休眠模式

在启动休眠模式后，计算机会将随机存取存储器（内存RAM）中的所有数据注入到一个容器中，然后关闭计算机

这个容器无法被删除，但可以关闭，当进行关闭的时候，休眠功能将会失效



#### 虚拟内存

当物理内存不足时，系统就会将硬盘中的一部分空间模拟成内存来使用，以保证系统的稳定运行，

具体来说就是将应用程序在逻辑上的内存地址映射到真实的物理内存中，这种逻辑上的内存就是虚拟内存

通常虚拟内存会存储为一个总体的虚拟内存地址，当cpu需要调用使用这个地址的时候，cpu会通过映射表，将虚拟内存的信息映射到真实的物理内存中，并且虚拟内存的空间可以无限大，每个地址对应不同的程序，不互相干



并且由于物理内存空间有限，可能无法一次性将一整个进程读取到内存中，因此内存划分为若干块，这些块被称为**页面**，其中只有一部分的进程会在这个进程中，当cpu需要访问的地址不再内存中的时候，可以从磁盘中加载对应的内存数据，并且当内存不足的时候也可以将访问不积极或长时间不访问的页面保存在磁盘中，这一步称为**换出**





#### makefile

是一个代码编译辅助工具

