# 大模型实际部署知识

### 这个模型加载了之后，不是应该将完整的模型以及所有的模型权重都加载到 RAM 中吗，而且 VRAM 这个加载的时候是不是也要将完整的模型权重以及模型都加载到 VRAM 呢？

先给你一个最重要的结论：

> **模型的“权重”最终必须存在于某个计算设备能够访问的内存里。**而具体要存放到哪个计算设备的内存中，就需要看你是用的模型推理方式：
> 
> 如果让 GPU 负责计算，那么 GPU 至少必须能够访问它要计算的那些权重。
> 
> 但**不一定要求“完整权重同时存在 RAM + 完整权重同时存在 VRAM”**。



具体这些内存占用需要看你是通过 CPU 推理 还是 GPU 推理，还是 CPU + GPU 协同推理：



## 最简单的情况：CPU 推理

假设：

```
模型 = 40GB
```

你完全不用 GPU。

那么：

```
SSD ↓RAM ↓CPU
```

此时：

```
RAM：[ 完整模型权重 40GB ]VRAM：[ 不需要 ]
```

CPU直接从 RAM 里面读取权重进行计算。

所以：

> **CPU 推理主要依赖 RAM。**





## 最简单的 GPU 推理

反过来，如果你让 GPU 完全负责推理：

```
SSD ↓VRAM ↓GPU
```

那么理想情况下：

```
RAM：模型文件可以只是加载过程中的中转或者根本不长期保存完整权重VRAM：┌─────────────────┐│ 完整模型权重     ││ 40GB             │└─────────────────┘
```

这时候：

> **完整模型权重在 VRAM 中。**

而不是：

> RAM 一份 + VRAM 一份。

当然，具体框架的加载过程可能会暂时在 RAM 中建立 CPU-side copy，但那是**加载过程/运行时实现细节**，不意味着你必须永久占用一份完整 RAM。



## 为什么“24GB显卡跑不了40GB模型”这个说法也不完全准确

假设：

```
模型 = 40GBGPU VRAM = 24GBRAM = 64GB
```

你可能会说：

> VRAM只有24GB，模型40GB，装不进去啊。

没错。

**但是可以做 GPU + CPU 混合推理。**

例如：

```
模型 40GB         ┌──────────────┐         │   40GB权重    │         └──────┬───────┘                │        ┌───────┴────────┐        ↓                ↓     VRAM 24GB        RAM 16GB        ↓                ↓       GPU              CPU
```

模型的一部分在 GPU：

```
Layer 1Layer 2Layer 3...
```

另一部分留在 CPU：

```
Layer 20Layer 21...
```

这就是：

> **GPU offloading / CPU offloading**

所以：

**模型可以运行，但通常速度会下降。**

---

## 为什么会慢？

因为 GPU 和 RAM 之间的数据交换速度远远不如 GPU 自己的 VRAM。

例如：

```
GPU │ │ 高速访问 ↓VRAM
```

这是 GPU 最舒服的工作方式。

但是：

```
GPU │ │ PCIe ↓RAM
```

就慢很多。

所以：

```
全部权重在 VRAM        ↓🚀 快部分权重在 RAM        ↓🐢 慢频繁从 SSD 读取        ↓🐌🐌🐌 非常慢
```





## 注意：这些都只是模型加载阶段，实际上模型运行的时候的硬件资源占用远不止这些东西

举个非常直观的例子。

假设：

```
Q4模型文件≈ 40GB
```

你可能以为：

> 那我需要40GB显存。

其实应该想：

```
40GB模型权重+KV Cache+Runtime+Context+其他开销
```

所以可能需要：

```
45GB
50GB
60GB
...
```

具体多少取决于：

- 模型架构
- context length
- batch size
- KV cache precision
- 推理框架
- quantization
- 是否 MoE
- 是否 CPU offload





# 模型整体的存储与运行架构

                         大模型
                           │
                  ┌────────┴────────┐
                  │                 │
               模型文件           运行
                  │                 │
                  ↓                 ↓
                 SSD           ┌────┴─────┐
                              │          │
                             CPU        GPU
                              │          │
                              ↓          ↓
                             RAM       VRAM
                              │          │
                              └────┬─────┘
                                   ↓
                                推理计算
                                   │
                                   ↓
                                  Token

而权重在运行时的位置可以是：

```
① CPU：SSD → RAM → CPU② GPU：SSD → VRAM → GPU③ CPU + GPU：SSD → RAM + VRAM → CPU + GPU
```





# 为什么他们的这个在 GPU 不够大的时候，CPU + GPU 的时候，需要将模型分别存储到 RAM 和 VRAM ，那这样加载的模型，实际上不会混乱吗？RAM 加载模型的 encoder ，VRAM 又加载模型的 encoder 和剩下的 decoder ，那 CPU 和 GPU 计算的这些数据又要怎么进行协使用，让他好像一直都是在使用一个设备计算那样？

容易造成误解的地方：

> **CPU + GPU 并不是“CPU 算前半个模型、GPU 算后半个模型”这么简单。**
> 
> 实际上最常见的做法是把**不同的模型层（layers）放到不同设备上**，然后一个 token 的数据按照层的顺序，在 CPU 和 GPU 之间传递。







# 为什么模型推理时候的内存占用会和模型本身的文件大小会不一样呢？

## 一、先建立一个“模型运行时”的完整组成图

假设我们有一个模型：

> **Qwen3.5-35B-A3B，Q4 量化**

为了方便理解，我们假设它的量化后权重大约：

```
模型权重 ≈ 22 GB
```

注意这里是为了教学方便的假设数字，真实大小会随具体量化版本变化。

当它**没有运行**的时候：

```
SSD┌──────────────────────────┐│ model.gguf / safetensors ││                          ││  模型权重 ≈ 22GB         │└──────────────────────────┘
```

这时候基本就是：

> **一个文件。**

---

## 二、启动模型以后，发生什么？

假设你运行：

```
ollama run xxx
```

或者：

```
vllm serve xxx
```

系统开始加载模型。

如果 GPU 足够：

```
SSD ↓VRAM ↓GPU
```

然后 VRAM 里面不只是：

```
22GB 模型权重
```

而是大概：

```
GPU VRAM┌─────────────────────────┐│                         ││ Model Weights            ││ ~22GB                   ││                         │├─────────────────────────┤│ KV Cache                ││                         │├─────────────────────────┤│ Activations             ││                         │├─────────────────────────┤│ Temporary Buffers       ││                         │├─────────────────────────┤│ CUDA / Runtime          ││                         │└─────────────────────────┘
```

这就是为什么：

> **“模型文件 22GB”**

不等于：

> **“22GB 显存就够了”。**

---

## 三、那 KV Cache 到底是什么？

这个东西特别重要。

假设你问模型：

> “请介绍一下人工智能。”

模型开始处理：

```
请介绍一下人工智能
```

Transformer 在处理这些 token 的时候，会产生大量中间结果。

其中有一部分叫：

> **Key / Value**

所以叫：

> **KV Cache**

---

## 四、为什么需要 Cache？

假设现在模型已经处理：

```
我今天想学习
```

然后你继续输入：

```
大模型
```

模型并不是每生成一个 token 都完全忘掉前面的东西。

它需要知道：

```
我 今天 想 学习 大模型
```

于是 Transformer 的 Attention 需要参考之前 token 的信息。

如果每次都重新计算：

```
我今天想学习
```

那会非常浪费。

所以模型把之前计算出来的：

```
KV
```

保存起来。

下一次直接使用。

这就是：

> **KV Cache**

---

## 五、所以 KV Cache 是“对话内容”吗？

不完全是。

这是一个非常容易误解的地方。

它不是：

```
"你好，我叫张三"
```

这种文本。

而是模型在 Attention 计算过程中产生的：

> **数学中间状态。**

例如：

```
Token ↓Embedding ↓Layer 1 ↓K / V ↓Layer 2 ↓K / V ↓...
```

这些 K/V 数值被保存起来：

```
KV Cache
```

下一次继续生成的时候可以直接使用。

---

## 六、为什么 KV Cache 会吃掉大量显存？

因为：

> **上下文越长 → token 越多 → KV Cache 越大。**

例如：

```
Context = 4K
```

可能只需要几 GB。

但是：

```
Context = 128K
```

就可能变成：

```
十几 GB几十 GB
```

具体取决于模型架构、KV heads、KV 精度等。

所以对于你想玩的：

> Hermes Agent

这一点特别重要。

因为 Hermes 经常是：

```
用户 ↓模型 ↓工具 ↓工具输出 ↓模型 ↓工具 ↓工具输出 ↓模型 ↓……
```

上下文会不断增长。

所以：

> **模型权重能塞进 GPU ≠ Hermes 能舒服地跑长时间 Agent。**

## 七、第二个：Activations 是什么？

这个其实更简单。

假设：

```
模型权重
```

是：

> **模型自己的“知识/参数”。**

而：

```
Activation
```

可以理解成：

> **当前这一次输入在模型里面流动产生的临时计算结果。**

比如：

```
输入："法国首都是哪里？"
```

经过：

```
Embedding ↓Layer 1 ↓Activation ↓Layer 2 ↓Activation ↓Layer 3 ↓Activation ↓...
```

每一层都会产生新的中间张量。

这些就是：

> **Activations**

---

## 九、第三个：Temporary Buffers

这个也没有那么神秘。

GPU 进行计算的时候，经常需要：

```
临时空间
```

例如：

```
矩阵乘法AttentionSoftmaxQuantization通信
```

这些操作可能需要临时 Tensor。

例如：

```
A × B ↓Temporary Tensor ↓C
```

计算结束：

```
Temporary Tensor ↓释放
```

所以它不像模型权重：

> 永久存在。

而是：

> **计算的时候临时占用显存。**

---

## 十、第四个：CUDA Runtime

这个其实更简单。

如果你用 NVIDIA GPU：

```
你的模型 ↓PyTorch / vLLM / llama.cpp ↓CUDA ↓NVIDIA GPU
```

CUDA 是 NVIDIA 的 GPU 计算平台。

GPU 运行的时候需要：

```
CUDA runtimeGPU kernelslibrariesmemory managementcommunication buffers
```

所以也会占一部分显存。

不过：

> **它通常不是显存大头。**

不要把它理解成：

> “CUDA 要几十 GB。”

不是。

真正的大头一般还是：

```
模型权重+KV Cache
```

---

## 十一、所以一个模型真正运行的时候，到底有什么？

现在可以总结成：

```
GPU VRAM│├── ① Model Weights│      模型参数│├── ② KV Cache│      已经处理过的上下文的 Attention 状态│├── ③ Activations│      当前 token 的中间计算结果│├── ④ Temporary Buffers│      临时计算空间│└── ⑤ Runtime / CUDA 等       推理框架和 GPU运行需要的空间
```

其中通常：

```
模型权重████████████████████KV Cache██████其他██
```

但比例并不是固定的。

## 十二、现在回答你第二个问题：CPU + GPU 为什么不会“混乱”？

这个问题问得非常好。

你说：

> RAM 加载模型的 encoder，VRAM 又加载模型的 encoder 和剩下的 decoder？

这里需要纠正：

### Transformer 大语言模型通常不是 Encoder + Decoder 这样简单划分。

比如现在大量 LLM：

```
Decoder-only Transformer
```

它大致是：

```
Embedding ↓Layer 1 ↓Layer 2 ↓Layer 3 ↓... ↓Layer N ↓LM Head ↓Next Token
```

所以 CPU/GPU offload 通常更像：

```
RAM│├── Layer 1├── Layer 2├── Layer 3└── Layer 4VRAM│├── Layer 5├── Layer 6├── Layer 7└── Layer 8
```

也就是说，CPU / GPU 的分层是通过将模型的多少个前向层放到 CPU ，然后将 剩下的层放到 GPU 中进行分别运算，那么这样运算的话，那就需要将模型相应层的 权重 以及 模型的相应结构 存放到相应的 CPU/GPU 



---

## 十三、然后一个 token 是怎么跑的？

假设：

```
CPU：Layer 1～4GPU：Layer 5～8
```

用户输入：

> “法国的首都是哪里？”

首先：

```
CPU ↓Layer 1 ↓Layer 2 ↓Layer 3 ↓Layer 4
```

然后把计算结果：

```
Activation
```

传给 GPU：

```
RAM ↓PCIe ↓VRAM
```

GPU继续：

```
Layer 5 ↓Layer 6 ↓Layer 7 ↓Layer 8
```

最后：

```
LM Head ↓"巴黎"
```

---

## 十四、所以它实际上真的在 CPU 和 GPU 之间“传数据”

你刚才担心：

> “CPU 和 GPU 计算的数据怎么协作？”

答案就是：

> **通过内存拷贝/数据传输。**

例如：

```
CPU RAM   │   │ PCIe   ↓GPU VRAM   │   ↓GPU计算   │   │ PCIe   ↓CPU RAM
```

但通常不会每一层来回：

```
CPU Layer 1 ↓GPU Layer 2 ↓CPU Layer 3 ↓GPU Layer 4
```

那会非常慢。

更常见的是：

```
CPU：Layer 1Layer 2Layer 3        ↓ 一次传输GPU：Layer 4Layer 5Layer 6Layer 7        ↓CPU：Layer 8
```

当然具体调度取决于推理框架和 offload 策略。

## 十五、为什么这样还能“像一个模型”？

因为：

> **模型本质上就是一个数学函数。**

比如：

```
Y = Layer8(      Layer7(        Layer6(          ...            Layer1(X)        )      )    )
```

Layer 1 在 CPU：

```
X → Layer1 → A
```

Layer 2 在 CPU：

```
A → Layer2 → B
```

然后：

```
B↓PCIe↓GPU
```

Layer 3：

```
B → Layer3 → C
```

数学上完全没有问题。

所以：

> **CPU 和 GPU 只是执行计算的不同设备。**

模型本身并不关心：

> “你到底是 CPU 算的还是 GPU 算的。”

只要输入输出的数据正确：

```
Layer N 输出=下一层 Layer N+1 输入
```

就行。

---

## 十六、但是为什么我们不把整个模型都放 RAM，然后 GPU 每次需要什么就去 RAM 拿？

理论上可以。

但是：

## 太慢。

因为：

```
VRAM ↔ GPU
```

速度非常高。

而：

```
RAM ↔ GPU
```

通过 PCIe。

速度和延迟都差很多。

所以最理想：

```
模型权重 ↓全部 VRAM ↓GPU
```

次优：

```
大部分 VRAM少部分 RAM
```

再次：

```
大部分 RAM少部分 VRAM
```

最慢：

```
SSD ↔ GPU
```

## 十七、所以你可以把显存理解成“GPU的工作台”

这个比喻特别好用。

比如：

```
仓库 = SSD工厂旁边的储藏室 = RAM生产线旁边的工作台 = VRAM工人 = GPU
```

模型文件：

```
仓库 ↓储藏室 ↓工作台
```

GPU真正工作的时候，希望：

```
模型权重+当前数据+KV Cache
```

都在：

> **工作台（VRAM）**

这样最快。

## 十八、现在回答你最后一个问题：视觉模型是不是也一样？

### **是的。**

这也是非常重要的认知。

你之前学习 YOLO、CNN、视觉神经网络的时候，其实底层硬件逻辑完全一样。

例如一个 YOLO 模型：

```
SSD ↓RAM / VRAM ↓GPU ↓CNN / Backbone ↓Neck ↓Detection Head ↓Bounding Boxes
```

运行期间同样存在：

```
Model WeightsActivationsTemporary BuffersRuntime
```

如果涉及 Transformer，也可能存在：

```
KV Cache
```

---

## 十九、但视觉模型和 LLM 有一个非常重要的区别

LLM：

```
输入：token输出：token
```

例如：

```
"你好" ↓Token ↓Transformer ↓下一个Token ↓"你好，很" ↓下一个Token ↓"你好，很高兴"
```

所以它会产生：

> **KV Cache**

而 YOLO：

```
图片 ↓CNN / Transformer ↓一次前向传播 ↓Bounding Boxes
```

通常没有 LLM 那种：

> **不断生成 token，并且把历史 Attention 缓存起来**

的过程。

所以：

### LLM

```
Weights+KV Cache+Activations+Buffers
```

### YOLO/CNN

```
Weights+Activations+Buffers
```

通常没有同等意义上的长序列 KV Cache。

# 二十、这也解释了你之前学 YOLO 时的很多东西

比如你之前看到：

```
CNN ↓Feature Map ↓Detection Head ↓Bounding Box
```

其中：

> **Feature Map**

其实就可以理解成一种：

> **Activation**

例如：

```
输入图片1920×1080 ↓CNN ↓Feature Map ↓Feature Map ↓Feature Map ↓检测头
```

这些 Feature Map 都是运行过程中产生的中间数据。

它们和 LLM 里面的：

```
hidden statesactivations
```

在“硬件运行”的概念上是非常类似的。
