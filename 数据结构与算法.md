# 数据结构

数据结构就是关系，就是数据元素相互之间存在的一种或多种特定关系的集合



## 逻辑结构

逻辑结构是指数据对象中数据元素之间的相互关系

### 集合结构

集合结构中的数据元素之间只是同属同一个集合，并没有其他任何关系

![1706713295659](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1706713295659.png)



### 线性结构

![1706713322959](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1706713322959.png)



### 树形结构

![1706713368812](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1706713368812.png)





图形结构

![1706713412352](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1706713412352.png)









## 物理结构

物理结构是指逻辑结构在计算机中存储的形式

根据物理结构的定义，我们实际上研究的就是如何将数据元素存储到计算机的存储器中。

数据元素的存储结构形式有两种：**顺序存储**和**链式存储**



### 顺序存储

顺序存储是将数据元素存放在地址连续的存储单元中，其数据元素之间的逻辑关系和物理关系是一致的

例如C语言中的结构体一样，结构体的成员的内存地址都是连续的而成员间的关系也和内存地址一样是连续的，类似于**线性结构**

![1706713774641](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1706713774641.png)



### 链式存储

链式存储结构就是将数据元素存放到任意的存储单元中，	这组存储单元可以是连续的，也可以是不连续的，但是这种存储数据元素之间的存储关系并不能直接反映其逻辑关系，因此需要用一个指针存放下一个与之相关的数据元素的地址（指针），这样就可以通过指针来寻找到下一个数据元素

![1706714143011](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1706714143011.png)







# 算法

算法就是解决特定问题的步骤







# 算法时间复杂度

时间复杂度就是算法的时间度量，记作$T(n)=O(f(n))$。它表示随问题规模n的增大，算法的执行时间的增长率









# 图

**图**是由**顶点**的有穷非空集合$V$和**顶点之间边**的集合$E$组成，通常表示为$G=(V,E)$

其中 $G$ 表示整个图， $V$ 是图中的所有顶点的集合， $E$ 是图中所有的边的集合

若 $V= \{v_1, v_2,...,v_n\}$ ，则用 $∣ V ∣$ 表示图G GG中顶点的个数，也称图G GG的阶，$E= \{(u, v) |u∈V, v∈V\}$ ，用 $∣ E ∣$表示图 $G$ 中边的条数

> 注意:线性表可以是空表，树可以是空树，但图不可以是空图。就是说，图中不能一个顶点也没有，图的顶点集V一定非空，但边集E可以为空，此时图中只有顶点而没有边。



## 有向图
