# 一、先建立视觉模型的统一结构

现代视觉模型可以拆成三个层次：

```
输入图像
   ↓
骨干网络 Backbone
   ↓
特征图 Feature Maps
   ↓
任务专用预测头 Head
   ↓
分类、检测、分割等输出
```

例如：

## 图像分类

```
整张图
  ↓
ResNet
  ↓
特征图
  ↓
全局平均池化 + 全连接层
  ↓
一个图像类别
```

## 目标检测

```
整张图
  ↓
ResNet/VGG/MobileNet 等骨干
  ↓
带有空间位置的特征图
  ↓
检测头
  ↓
多个目标的类别 + 边界框
```

## 语义分割

```
整张图
  ↓
ResNet 等编码器
  ↓
特征图
  ↓
分割解码器
  ↓
每个像素的类别
```

因此：

> AlexNet、VGG、ResNet 不是只能做分类；它们最初主要在分类任务上证明自己的特征提取能力，但去掉分类头以后，可以作为检测、分割、姿态估计等任务的骨干网络。

可以。你现在的疑问，本质上是把以下四个不同层次的“窗口”与“网络”混在了一起：

1. **卷积核的局部滑动窗口**；
2. **早期目标检测的图像滑动窗口**；
3. **R-CNN 的候选区域 Proposal**；
4. **SSD/Faster R-CNN 的锚框 Anchor**。

它们都可能表现为“一个框在图上移动”，但计算含义完全不同。

另外，还有一个更重要的关系需要纠正：

> **LeNet、AlexNet、VGG、GoogLeNet、ResNet 是“特征提取网络/分类骨干”的发展主线；R-CNN、Fast R-CNN、Faster R-CNN、SSD、YOLO 是“目标检测框架”的发展分支。它们不是一条简单的前后替代链。**

下面按照这个逻辑，重新整理完整内容。

---

# 一、先建立视觉模型的统一结构

现代视觉模型可以拆成三个层次：

```text
输入图像
   ↓
骨干网络 Backbone
   ↓
特征图 Feature Maps
   ↓
任务专用预测头 Head
   ↓
分类、检测、分割等输出
```

例如：

## 图像分类

```text
整张图
  ↓
ResNet
  ↓
特征图
  ↓
全局平均池化 + 全连接层
  ↓
一个图像类别
```

## 目标检测

```text
整张图
  ↓
ResNet/VGG/MobileNet 等骨干
  ↓
带有空间位置的特征图
  ↓
检测头
  ↓
多个目标的类别 + 边界框
```

## 语义分割

```text
整张图
  ↓
ResNet 等编码器
  ↓
特征图
  ↓
分割解码器
  ↓
每个像素的类别
```

因此：

> AlexNet、VGG、ResNet 不是只能做分类；它们最初主要在分类任务上证明自己的特征提取能力，但去掉分类头以后，可以作为检测、分割、姿态估计等任务的骨干网络。

---

# 二、AlexNet、VGG、GoogLeNet、ResNet 到底改进了什么

它们的改进绝不只是“换了不同大小的卷积核”。

这些网络都大量使用卷积，但它们解决的是不同的工程和数学问题。

---

## 1. LeNet-5：建立 CNN 的基本结构

LeNet-5 的主要任务是手写数字识别。

典型结构：

```text
图像
→ 卷积
→ 池化
→ 卷积
→ 池化
→ 全连接
→ 类别
```

它建立了 CNN 的基本范式：

- 局部连接；
- 卷积核权重共享；
- 特征逐层组合；
- 池化降低空间尺寸；
- 最后用全连接层分类。

但是它：

- 网络较浅；
- 输入图像小；
- 数据集较简单；
- 还没有证明 CNN 能处理大规模自然图像。

---

## 2. AlexNet：把 CNN 扩展到大规模自然图像

AlexNet 于 2012 年在 ImageNet 分类任务上取得突破。

它相对 LeNet 的关键改进不只是卷积核，而是整套规模化训练方案：

- 更深、更宽的网络；
- 使用 GPU 训练；
- 使用 ReLU 激活；
- 使用 Dropout；
- 使用数据增强；
- 使用最大池化；
- 在百万级自然图像上训练。

AlexNet 的卷积核包括：

- 前层较大的 \(11\times11\)；
- 中间的 \(5\times5\)；
- 后面的 \(3\times3\)。

但 AlexNet 的历史意义并不是“用了多尺度卷积核”，而是：

> 证明大数据、GPU、深层 CNN 和合适训练方法组合起来，能够在复杂自然图像分类上显著超过传统视觉方法。

AlexNet 主要完成分类任务，但其卷积部分后来也被 R-CNN 当作特征提取器。

---

## 3. VGG：统一使用小卷积核，加深网络

VGG 于 2014 年提出，代表模型是 VGG-16 和 VGG-19。

它的核心思想不是使用多种尺度的卷积核，而是反过来：

> 尽量统一使用 \(3\times3\) 小卷积核，通过堆叠扩大感受野。

例如，在步长为 1 时：

- 两层 \(3\times3\) 的感受野相当于一层 \(5\times5\)；
- 三层 \(3\times3\) 的感受野相当于一层 \(7\times7\)。

感受野可以写成：

$R=1+\sum_l(k_l-1)
$

这里暂时假设所有卷积步长均为 1。

两层 \(3\times3\) 相比一层 \(5\times5\) 有两个主要优点。

### 参数更少

如果输入和输出通道都为 \(C\)：

$2\times3\times3\times C^2=18C^2
$

而一个 \(5\times5\) 卷积为：

$5\times5\times C^2=25C^2
$

### 非线性更多

两层卷积可以放置两次 ReLU，而单层大卷积通常只有一次 ReLU，因此表达能力更强。

VGG 解决的是：

- 网络结构不规则；
- 如何系统研究深度；
- 如何用小卷积堆叠获得大感受野。

它的缺点是参数量和计算量很大，尤其全连接层参数很多。

---

## 4. GoogLeNet：多分支、多尺度、降低计算量

正确拼写是 **GoogLeNet**，其核心组件叫 **Inception 模块**。

一个 Inception 模块会并行执行多条分支：

```text
输入特征图
 ├─→ 1×1 卷积
 ├─→ 1×1 → 3×3 卷积
 ├─→ 1×1 → 5×5 卷积
 └─→ 池化 → 1×1 卷积
             ↓
          通道拼接
```

这里确实体现了多尺度卷积：

- \(1\times1\)：通道混合和降维；
- \(3\times3\)：提取中等局部结构；
- \(5\times5\)：获得更大感受野；
- 池化分支：提供另一种局部不变特征。

其核心创新还包括：

- 使用 \(1\times1\) 卷积降低通道数；
- 多分支并行处理；
- 使用全局平均池化减少全连接层参数；
- 在较低计算成本下加深网络。

所以 GoogLeNet 不只是“换了卷积核”，而是改变了网络的拓扑结构：

> 从单一路径顺序堆叠，变成多个尺度的并行分支，再进行特征融合。

---

## 5. ResNet：用残差连接解决深层网络退化

ResNet 于 2015 年提出。

它最重要的创新不是卷积核大小，而是残差连接：

$y=F(x)+x
$

其中：

- \(x\) 是残差块输入；
- \(F(x)\) 是若干卷积层学习的变换；
- \(x\) 通过捷径直接加到输出。

传统顺序网络希望直接学习：

$H(x)
$

ResNet 改成让卷积分支学习：

$F(x)=H(x)-x
$

于是：
$H(x)=F(x)+x
$

如果新增的卷积层暂时没有学到有用信息，可以让：

$F(x)\approx0
$

此时：

$y\approx x
$

因此深层网络至少能够近似保留浅层网络已经学到的表示。

反向传播时：

$\frac{\partial L}{\partial x}
=

\frac{\partial L}{\partial y}
\left(
I+\frac{\partial F}{\partial x}
\right)
$

梯度中存在一条通过恒等捷径直接传播的路径，从而改善超深网络的优化。

ResNet 解决的主要是：

- 普通网络加深后训练误差反而上升的退化问题；
- 深层网络难以优化；
- 梯度和信息传播路径过长。

ResNet 仍然使用大量 \(3\times3\) 和 \(1\times1\) 卷积，但它的决定性创新是**残差拓扑结构**。

---

# 三、这些网络是不是都用来做分类

更准确的回答是：

> 它们的核心架构创新主要是在图像分类基准上建立和验证的，但这些网络并不只能用于分类。

可以将一个 CNN 分成：

$\text{CNN}=\text{特征提取骨干}+\text{任务预测头}
$

例如 ResNet 分类器：

```text
输入图像
→ ResNet 卷积骨干
→ 最后一张特征图
→ 全局平均池化
→ 全连接分类层
→ ImageNet 类别
```

把最后的分类头去掉：

```text
输入图像
→ ResNet 卷积骨干
→ 多层特征图
```

这些特征图就可以交给：

- Faster R-CNN 做目标检测；
- Mask R-CNN 做实例分割；
- DeepLab 做语义分割；
- FPN 做多尺度特征融合；
- RetinaNet 做单阶段检测。

所以需要区分：

## 骨干网络 Backbone

负责从图像中提取特征：

- VGG；
- ResNet；
- MobileNet；
- EfficientNet；
- ConvNeXt；
- Swin Transformer。

## 任务框架或检测器（head）

负责根据特征完成具体任务：

- R-CNN；
- Faster R-CNN；
- SSD；
- YOLO；
- RetinaNet；
- DETR。

例如：

```text
Faster R-CNN + ResNet-50
```

意思是：

- Faster R-CNN 是检测框架；
- ResNet-50 是它的骨干网络。

同样：

```text
SSD + VGG-16
SSD + MobileNet
```

都是可能的组合。原始 SSD300 使用的是经过修改的 VGG-16 骨干。

---

# 四、CNN 的卷积是不是滑动窗口

## 简短答案

是的：

> 从数学概念上看，标准卷积就是让一个局部卷积核在输入或特征图上滑动，并在每个位置执行乘加运算。

例如单输入通道时：

$Y(i,j)
=

\sum_m\sum_n
X(i+m,j+n)K(m,n)
$

其中：

- \(X\) 是输入；
- \(K\) 是卷积核；
- \((i,j)\) 是当前输出位置；
- 卷积核在不同位置使用同一组参数。

多通道卷积则为：

$Y_{c_{\mathrm{out}}}(i,j)
=

\sum_{c_{\mathrm{in}}}
\sum_m\sum_n
X_{c_{\mathrm{in}}}(i+m,j+n)
K_{c_{\mathrm{out}},c_{\mathrm{in}}}(m,n)
+b_{c_{\mathrm{out}}}
$

但这里必须区分两种“滑动窗口”。

---

## 1. 卷积核的滑动窗口

这是 CNN 内部的一次局部运算：

```text
一张输入特征图
  ↓
3×3 卷积核在所有位置滑动
  ↓
得到一张新的输出特征图
```

特点：

- 窗口通常很小，如 ($3\times3$)；
- 窗口参数在所有位置共享；
- 每个位置只执行乘加；
- 整个卷积层只产生新的特征图；
- 不会为每个窗口单独运行一遍完整 CNN。

**<u>其中每个卷积核窗口卷积输出的是特征值，这些特征值就是用来表明原图像素与卷积核表示的那个特征的相似程度的，而卷积核将整张图滑动一边后输出的所有特征值组成的矩阵就是特征图</u>**

---

## 2. 早期检测器的滑动窗口

传统滑动窗口目标检测是：

```text
在原图上截取区域1 → 完整分类器
在原图上截取区域2 → 完整分类器
在原图上截取区域3 → 完整分类器
……
```

而且为了检测不同大小的物体，通常还要建立图像金字塔：

```text
原尺寸图片
缩小后的图片
更小的图片
```

然后在每个尺度上扫描大量区域。

**它的计算单位是“候选图像区域**”，而不是卷积层里的 \(3\times3\) 局部乘加。

---

## 二者的核心区别

| 概念     | 卷积核滑动    | 检测滑动窗口         |
| ------ | -------- | -------------- |
| 窗口内容   | 卷积局部感受野  | 一个可能包含目标的图像区域  |
| 每次做什么  | 局部乘加     | 对区域执行完整分类      |
| 参数是否共享 | 同一个核处处共享 | 通常同一个分类器处理不同区域 |
| 输出     | 新特征图     | 每个区域的目标分数      |
| 计算粒度   | 一个卷积层内部  | 整个目标检测流程       |
| 计算成本   | 可以高度并行   | 朴素实现重复计算严重     |

所以：

> “CNN 卷积使用滑动窗口”是正确的；但不能因此把所有 CNN 都叫作“滑动窗口目标检测器”。
> 
> 这是因为上面我们提到的，<u>有些 CNN 是通过筛选出可能存在目标的图像窗口，然后在窗口里面进行卷积并分类</u>，而<u>有些原始一点的 CNN 是通过整张图进行卷积滑动窗口并做穷举分类</u>，两个都是做滑动窗口，但是具体的方式以及滑动的东西不太一样

另外，现代深度学习框架在底层可能使用矩阵乘法、Winograd、FFT 或专用 GPU 内核实现卷积，并不一定真的按程序循环逐格移动窗口。但数学语义仍然等价于局部卷积核滑动。

---

# 五、CNN 到底有没有“看整张图片”

这要分三个观察层级。

## 1. 从网络输入看：看了整张图片

对于 AlexNet、ResNet、SSD、YOLO、Faster R-CNN 等现代网络，通常是把整张图作为一次输入：

$X\in\mathbb R^{H\times W\times3}
$

因此可以说：

> 网络整体接收了整张图片。

---

## 2. 从单个卷积核看：只看局部区域

第一层的一个 \(3\times3\) 卷积核，一次只看到 \(3\times3\) 像素。

所以也可以说：

> 单个浅层卷积单元只处理局部窗口。

---

## 3. 从深层特征看：感受野逐渐覆盖大范围

多个卷积层堆叠后，深层单元对应的原图区域越来越大。

例如，假设步长都是 1：

```text
第一层 3×3 → 感受野 3×3
第二层 3×3 → 感受野 5×5
第三层 3×3 → 感受野 7×7
```

加入步长卷积、池化后，感受野会扩大得更快。

所以完整结论是：

> CNN 整体输入整张图，但浅层卷积核局部观察；通过逐层堆叠和下采样，深层特征逐渐整合更大范围乃至接近整图的信息。

这三句话并不矛盾。

---

# 六、什么是特征图

输入图像本身可以看成一个三通道张量：
$X\in\mathbb R^{H\times W\times3}
$

其中三个通道通常是 RGB。

经过一个有 $(C_{\mathrm{out}})$ 个卷积核的卷积层后，得到：

$F\in\mathbb R^{H'\times W'\times C_{\mathrm{out}}}
$

这就是特征图。

例如：

```text
224×224×3 输入图像
  ↓ 卷积、下采样
112×112×64 浅层特征图
  ↓
56×56×128 中层特征图
  ↓
28×28×256 中深层特征图
  ↓
14×14×512 深层特征图
  ↓
7×7×2048 高层特征图
```

不同层的特征具有不同性质：

- 浅层：边缘、方向、颜色、纹理；
- 中层：角点、形状、局部部件；
- 深层：对象部件、类别语义、上下文。

这里的“特征图”不是一张普通可视图像，而是网络产生的三维张量。

每个通道可以看成一种由训练自动学到的模式响应。

---

# 七、“共享特征图”到底是什么意思

这里有两种不同的“共享”，需要严格区分。

---

## 1. 卷积核权重共享

同一个卷积核在整张输入的不同位置使用相同参数：

```text
左上角使用 K
中央使用 K
右下角仍然使用 K
```

这叫：

> 卷积核权重共享。

它使卷积具有较少参数，并且具有平移等变性质。（也就是说，**卷积核在进行滑动计算的时候使用的卷积核的参数都不会改变**）

---

## 2. 检测器的特征计算共享

这是 Fast R-CNN、Faster R-CNN 中“共享特征图”的含义。

假设图像中有 2000 个候选框。

### 原始 R-CNN

```text
候选框1 → 裁剪原图 → CNN → 特征
候选框2 → 裁剪原图 → CNN → 特征
候选框3 → 裁剪原图 → CNN → 特征
……
候选框2000 → 裁剪原图 → CNN → 特征
```

同一片图像区域可能被不同候选框重复覆盖，于是 CNN 重复计算很多次。

可以写成：

$F_i=\operatorname{CNN}(\operatorname{Crop}(I,b_i))
$

其中 $(b_i)$ 是第 $(i)$ 个候选框。

---

### Fast R-CNN

Fast R-CNN 攻克了这个重复计算问题：

```text
整张图
  ↓ CNN 只运行一次
共享特征图 F
  ↓
候选框1 → 从 F 取对应区域
候选框2 → 从 F 取对应区域
候选框3 → 从 F 取对应区域
……
```

数学上：

$F=\operatorname{CNN}(I)
$

然后：

$r_i=\operatorname{RoIPool}(F,b_i)
$

最后：

$(\text{类别}_i,\text{框偏移}_i)
=

\operatorname{Head}(r_i)
$

所有候选框都复用同一个 \(F\)，因此叫：

> 候选区域之间共享整图卷积特征。

这不是说特征图之间互相共享，而是说：

> **整张图的卷积计算只做一次，多个候选框共同使用这一次计算的结果。**

---

# 八、为什么从特征图还能进行分类和边界框回归

因为特征图同时保留了两种信息：

1. **通道维度中的语义信息**：这里像不像猫、狗、汽车；
2. **空间维度中的位置信息**：这个模式出现在图像的哪个区域。

假设特征图为：
$F\in\mathbb R^{H_f\times W_f\times C}
$

其中：

- \(H_f,W_f\) 保留空间布局；
- \(C\) 个通道保存不同视觉模式。

检测头可以在每个位置预测：

$(\text{类别分数},\Delta x,\Delta y,\Delta w,\Delta h)
$

类别分支回答：

> 这个区域是什么？

边界框回归分支回答：

> 参考框应该向哪里移动、放大或缩小多少？

以锚框 \(a=(x_a,y_a,w_a,h_a)\) 为参考，可以预测：

$
t_x=\frac{x-x_a}{w_a},
\qquad
t_y=\frac{y-y_a}{h_a}
$

$t_w=\log\frac{w}{w_a},
\qquad
t_h=\log\frac{h}{h_a}
$

也就是说，网络不是直接从无到有猜一个框，而是学习：

> **当前参考框应该如何根据前面卷积层提取出来的特征组合调整检测头的参数，让检测头的输出能贴近真实目标。**

---

# 九、目标识别、图像分类与目标检测不要混用

“目标识别”在日常语言中比较模糊。学习时建议严格使用以下术语：

## 图像分类 Classification

```text
输入：整张图
输出：一个或多个类别
```

例如：

```text
这张图是猫
```

## 目标定位 Localization

```text
输入：整张图
输出：主要目标的类别和位置
```

例如：

```text
猫，位于 (x,y,w,h)
```

## 目标检测 Object Detection

```text
输入：整张图
输出：多个对象的类别和边界框
```

例如：

```text
猫1 + 框1
猫2 + 框2
狗1 + 框3
```

## 语义分割 Semantic Segmentation

```text
输出每一个像素的类别
```

## 实例分割 Instance Segmentation

```text
区分同一类别的不同对象，并为每个对象输出掩膜
```

所以我之前说的“第三阶段”更准确地应表述为：

> 深度 CNN 从整图分类扩展到检测、分割、姿态估计等结构化视觉任务。

YOLO、SSD 属于其中的**目标检测**分支，但第三阶段不只包含 YOLO 和 SSD。

---

# 十、视觉神经网络并不是一条单线发展链

你提出的链条：

```text
LeNet
→ AlexNet
→ VGG
→ GoogLeNet
→ ResNet
→ R-CNN
→ Fast R-CNN
→ Faster R-CNN
→ SSD
```

并不完全正确。

原因是它混合了两个维度：

## 第一条线：特征提取骨干的发展

```text
LeNet
  ↓
AlexNet
  ├─→ VGG
  ├─→ GoogLeNet / Inception
  └─→ 后续深层 CNN 探索
          ↓
       ResNet
          ↓
 DenseNet / ResNeXt / MobileNet / EfficientNet / ConvNeXt
```

这些模型主要研究：

> 怎样更有效地把图像变成高质量特征。

## 第二条线：目标检测框架的发展

```text
滑动窗口检测
   ↓
R-CNN
   ↓
Fast R-CNN
   ↓
Faster R-CNN
```

这是一条两阶段检测路线。

与此同时，又出现了另一条并行路线：

```text
密集预测
  ├─→ YOLO
  └─→ SSD
       ↓
 RetinaNet / 后续 YOLO / FCOS / CenterNet 等
```

SSD 不是 Faster R-CNN 的下一代，也不是从 Faster R-CNN 直接演化而来。

更准确地说：

> SSD、YOLO 与 Faster R-CNN 是在相近时期发展出的不同检测路线。Faster R-CNN 保留“先提候选区域、再精细分类”的两阶段思想；SSD 和 YOLO 则追求一次前向直接完成检测。

他们之间的**唯一区别就是具体的预测框是怎么来的**:

Faster R-CNN：就是 起

---

# 十一、按照时间和逻辑重新画完整发展树

下面这棵树比单线排列更准确。

```text
视觉神经网络
│
├─ 一、视觉特征提取与分类骨干
│  │
│  ├─ 1980  Neocognitron
│  │         局部感受野、层级特征思想
│  │
│  ├─ 1989/1998  LeNet / LeNet-5
│  │              卷积 + 池化 + 全连接
│  │              手写数字分类
│  │
│  ├─ 2012  AlexNet
│  │         GPU + ReLU + Dropout + 数据增强
│  │         大规模自然图像分类突破
│  │
│  ├─ 2014  VGG
│  │         统一堆叠 3×3 小卷积
│  │         系统加深网络
│  │
│  ├─ 2014  GoogLeNet / Inception
│  │         多分支、多尺度卷积
│  │         1×1 卷积降维
│  │         全局平均池化
│  │
│  ├─ 2015  ResNet
│  │         残差连接
│  │         解决超深网络退化和优化困难
│  │
│  └─ 后续骨干
│      ├─ DenseNet
│      ├─ ResNeXt
│      ├─ MobileNet
│      ├─ EfficientNet
│      ├─ ConvNeXt
│      └─ ViT / Swin Transformer
│
├─ 二、目标检测
│  │
│  ├─ 早期路线
│  │   ├─ 图像金字塔
│  │   ├─ 滑动窗口
│  │   └─ HOG / DPM 等传统检测器
│  │
│  ├─ 两阶段检测器
│  │   │
│  │   ├─ 2013/2014  R-CNN
│  │   │              Selective Search 产生候选框
│  │   │              每个框分别运行 CNN
│  │   │
│  │   ├─ 2015  Fast R-CNN
│  │   │         整张图只运行一次 CNN
│  │   │         候选框共享整图特征图
│  │   │         仍依赖 Selective Search
│  │   │
│  │   ├─ 2015  Faster R-CNN
│  │   │         RPN 网络生成候选框
│  │   │         RPN 与检测头共享骨干特征
│  │   │
│  │   ├─ 2017  FPN
│  │   │         融合多尺度特征
│  │   │
│  │   └─ 2017  Mask R-CNN
│  │             增加实例掩膜分支
│  │
│  ├─ 单阶段检测器
│  │   │
│  │   ├─ 2015/2016  YOLO v1
│  │   │              一次前向直接预测类别和框
│  │   │
│  │   ├─ 2015/2016  SSD
│  │   │              多层特征图
│  │   │              多尺度默认框
│  │   │              一次前向完成分类与回归
│  │   │
│  │   ├─ 2017  RetinaNet
│  │   │         FPN + Focal Loss
│  │   │         缓解密集检测正负样本不平衡
│  │   │
│  │   ├─ 后续 YOLO 系列
│  │   └─ FCOS / CenterNet 等无锚框检测器
│  │
│  └─ 集合预测检测器
│      ├─ 2020  DETR
│      │         Transformer + 对象查询
│      │         二分图匹配
│      └─ Deformable DETR / DINO / RT-DETR
│
├─ 三、图像分割
│  ├─ FCN
│  ├─ U-Net
│  ├─ DeepLab
│  ├─ Mask R-CNN
│  └─ Mask2Former
│
└─ 四、其他视觉任务
   ├─ 姿态估计
   ├─ 深度估计
   ├─ 目标跟踪
   ├─ 图像生成
   └─ 多模态视觉
```

---

# 十二、SSD 在这棵树上的准确位置

SSD 位于：

```text
目标检测
  ↓
单阶段目标检测
  ↓
基于锚框/默认框的密集预测
  ↓
SSD
```

时间上：

- 2015 年 12 月首次公开论文；
- 2016 年正式发表于 ECCV。

它与 YOLO v1 基本处在同一时期，也和 Faster R-CNN 的出现时间很接近。

三者代表不同方向：

```text
Faster R-CNN
= 两阶段
= 先产生少量内容相关候选框
= 再逐候选框精细分类和回归

SSD
= 单阶段
= 多尺度特征图 + 默认框
= 一次前向密集预测

YOLO v1
= 单阶段
= 将检测统一为整图上的直接回归
= 不采用 SSD 式多尺度默认框设计
```

SSD 的原始结构可以理解为：

```text
整张图
  ↓
VGG-16 骨干
  ↓
增加额外卷积层
  ↓
得到不同分辨率的特征图
  ├─ 38×38：偏向小目标
  ├─ 19×19：偏向中小目标
  ├─ 10×10：偏向中等目标
  ├─ 5×5 ：偏向较大目标
  ├─ 3×3 ：偏向大目标
  └─ 1×1 ：偏向全局大目标
  ↓
每个位置设置多个默认框
  ↓
预测类别 + 坐标偏移
  ↓
置信度过滤 + NMS
  ↓
最终检测结果
```

原始 SSD300 一共使用 8732 个默认框。

但这不意味着运行 8732 次 CNN。CNN 仍然只对整张图运行一次。

---

# 十三、SSD 的默认框与 R-CNN 候选框有什么区别

这是理解 D2L 页面最关键的一步。

| 机制                          | 是否依赖图像内容 | 是否为独立阶段 | 是否裁剪原图再跑 CNN | 是否送入第二阶段 |
| --------------------------- | --------:| -------:| ------------:| --------:|
| R-CNN Selective Search      | 是        | 是       | 是            | 是        |
| Fast R-CNN Selective Search | 是        | 是       | 否，读取共享特征图    | 是        |
| Faster R-CNN RPN            | 是        | 是       | 否            | 是        |
| SSD 默认框                     | 否，几何模板固定 | 否       | 否            | 否        |

SSD 的默认框只是一个参考坐标系。

网络在默认框 \(a\) 的基础上预测：

$(\Delta x,\Delta y,\Delta w,\Delta h)
$

并预测类别分数：

$(s_0,s_1,\ldots,s_C)
$

因此 SSD 的实际运算是：

```text
特征图一个位置的特征向量
  ↓
一个小卷积预测头
  ↓
该位置多个默认框的：
类别分数 + 坐标偏移
```

它不是：

```text
生成默认框
→ 裁剪默认框里的原图
→ 再运行一遍 CNN
```

---

# 十四、D2L 为什么重点讲特征图与多尺度锚框

你给出的 D2L 页面讲的是 SSD 体系中的“多尺度目标检测”。

它是在解释：

1. 为什么不同特征图对应不同目标尺度；
2. 为什么每个位置要设置多个锚框；
3. 如何让一套特征图同时覆盖大小不同、长宽比不同的目标。

它没有在讲原始 R-CNN 的 Selective Search 路线。

所以你听到的两种说法分别对应：

## “先生成选框，再让 CNN 预测”

如果指原始 R-CNN：

```text
Selective Search
→ 候选区域
→ 每个区域分别送入 CNN
```

这是正确的。

如果泛指所有 CNN 检测器，则是错误的。

## “在多尺度特征图上生成锚框并预测”

这是 SSD：

```text
整张图一次经过 CNN
→ 多尺度特征图
→ 每个位置密集预测默认框
```

也是正确的。

二者描述的是不同检测框架，不存在矛盾。

---

# 十五、用一张最终逻辑图把所有概念串起来

```text
一、CNN 的底层运算
卷积核在局部窗口上滑动
          ↓
每一层产生特征图
          ↓
深层感受野扩大、语义增强

二、CNN 骨干的发展
LeNet
  ↓
AlexNet：规模化训练
  ↓
VGG：小卷积规则堆叠
  ├────────────┐
  │            │
GoogLeNet      ResNet
多分支多尺度    残差连接
  │            │
  └────→ 更强视觉骨干 ←────┘

三、骨干特征接入不同任务头
特征图
 ├─→ 分类头 → 整图类别
 ├─→ 检测头 → 类别 + 边界框
 └─→ 分割头 → 逐像素类别

四、目标检测的两条主要路线
目标检测
 ├─→ 两阶段
 │    R-CNN
 │      ↓
 │    Fast R-CNN
 │      ↓
 │    Faster R-CNN
 │
 └─→ 单阶段
      YOLO
      SSD
      RetinaNet
      FCOS 等

五、两阶段与单阶段的根本区别
两阶段：
先产生少量候选区域
→ 再逐候选区域精细预测

单阶段：
直接在整张图的特征图上密集预测
→ 一次前向得到类别和框
```

---

# 十六、对你六个问题的直接回答

## 问题 1

AlexNet、VGG、GoogLeNet、ResNet 不只是换卷积核：

- AlexNet：规模化、GPU、ReLU、Dropout、数据增强；
- VGG：统一 \(3\times3\) 小卷积，规则加深；
- GoogLeNet：多分支多尺度、\(1\times1\) 降维；
- ResNet：残差连接，解决超深网络退化。

它们主要通过分类任务证明能力，但可以作为检测和分割骨干。

## 问题 2

这些网络的标准卷积在数学上都是局部滑动窗口，但完整网络不只有卷积，还包含：

- 激活；
- 池化或步长下采样；
- 归一化；
- 残差连接；
- 分支拼接；
- 分类头或检测头。

因此“都使用滑动卷积”不等于“网络结构相同”。

## 问题 3

第三阶段应理解为 CNN 向结构化视觉任务扩展，其中包括：

- R-CNN 系列；
- YOLO；
- SSD；
- 图像分割网络；
- 姿态估计等。

YOLO 和 SSD 只是目标检测中的单阶段分支。

## 问题 4

是的，CNN 标准卷积概念上是滑动窗口。但它与“在原图上裁剪很多框，并对每个框运行完整分类器”的检测滑动窗口不是同一个层次。

## 问题 5

CNN 每层卷积后都会得到特征图。所谓共享特征图，是：

> 整张图只计算一次 CNN 特征，然后让多个候选框或多个检测头复用这些特征。

它建立在 CNN 特征图之上，是避免重复卷积计算的检测框架设计。

## 问题 6

正确关系不是单线的，而是：

```text
骨干网络：
LeNet → AlexNet → VGG/GoogLeNet → ResNet → 后续骨干

检测框架：
R-CNN → Fast R-CNN → Faster R-CNN
                    ↘
并行路线：YOLO、SSD → RetinaNet、后续单阶段检测器
```

SSD 位于**单阶段、多尺度、基于默认框的密集目标检测分支**；它不是 Faster R-CNN 的后继版本。

按照你的要求，这次先只完整回答和修订内容，**暂不写入脑图**。待你确认后，再将经过确认的发展关系放入“视觉网络发展”节点的节点内脑图；以后大型模型谱系与发展树也优先采用对应主题节点的节点内脑图组织。

主要一手资料：

- [AlexNet：ImageNet Classification with Deep Convolutional Neural Networks](https://proceedings.neurips.cc/paper/2012/hash/c399862d3b9d6b76c8436e924a68c45b-Abstract.html)
- [VGG：Very Deep Convolutional Networks for Large-Scale Image Recognition](https://arxiv.org/abs/1409.1556)
- [GoogLeNet：Going Deeper with Convolutions](https://arxiv.org/abs/1409.4842)
- [ResNet：Deep Residual Learning for Image Recognition](https://arxiv.org/abs/1512.03385)
- [R-CNN：Rich Feature Hierarchies for Accurate Object Detection and Semantic Segmentation](https://arxiv.org/abs/1311.2524)
- [Fast R-CNN](https://arxiv.org/abs/1504.08083)
- [Faster R-CNN](https://arxiv.org/abs/1506.01497)
- [SSD：Single Shot MultiBox Detector](https://arxiv.org/abs/1512.02325)





## R-CNN 的 Selective Search 机制是什么东西？它在干什么？

**Selective Search 就是用于产生 proposal 区域的**

而 Proposal 的意思接近与：“这个区域值的拿出来做进一步的检查”，他就是一个候选区域，不是最终的检测结果

假设一张图片：

```
┌─────────────────────────┐
│                         │
│     🐕                  │
│                         │
│              🚗         │
│                         │
└─────────────────────────┘
```

我们不知道狗在哪里、汽车在哪里。

那么先对图片做一些低层次的分析：

- 哪些像素颜色相似？
- 哪些区域纹理相似？
- 哪些区域彼此相邻？
- 哪些区域大小合适？
- 把相似的区域合起来会不会形成一个比较完整的区域？

Selective Search **从初始分割区域开始，反复把相似的邻接区域合并，同时用多种颜色空间、相似度和起始区域策略产生大量候选位置**。论文明确把它定义成一种**class-independent object location hypothesis**：**它不是判断“这是狗”，而是尽量提出“这里可能是一个物体”的位置**。[论文原文](https://ivi.fnwi.uva.nl/isis/publications/2013/UijlingsIJCV2013/UijlingsIJCV2013.pdf?utm_source=chatgpt.com)

比如：

```
原图
┌───────────────────┐
│   蓝天   蓝天      │
│      🐕           │
│                   │
│           🚗      │
└───────────────────┘
```

Selective Search 可能给你：

```
proposal 1：
┌──────┐
│  🐕  │
└──────┘
proposal 2：
┌──────────┐
│    🐕    │
│          │
└──────────┘
proposal 3：
┌───────────────┐
│    🐕         │
│               │
└───────────────┘
proposal 4：
┌───────┐
│       │
│   🚗  │
└───────┘
proposal 5：
┌────────────────┐
│ 🐕             │
│           🚗   │
└────────────────┘
```

你会发现：

**它不需要知道哪个是狗。**

它只需要尽可能地把“可能的物体位置”都包括进来。

也就是说他就是一个猜测算法，就负责猜这个位置到底有没有东西，但是这个东西是什么，他不知道

**它不需要知道哪个是狗。**

它只需要尽可能地把“可能的物体位置”都包括进来。

所以 Selective Search 的目标是：

> **宁可多提出一些错误候选框，也别漏掉真正物体。**



## Fast R-CNN 的 Proposal 是怎么产生的？他怎么知道这些预测框里面会有这些目标的？

## Faster R-CNN 的 Proposal 是怎么产生的？他怎么知道这些预测框里面会有这些目标的？

Faster R-CNN 是将这个提出候选框的工作交给了神经网络（RPN），这个时候才是让模型：**学习什么样的位置像是一个目标**

RPN 在 feature map 的每个位置放若干 anchor，然后对每个 anchor 预测：

```
objectness+bbox adjustment
```

也就是：

```
这个区域像不像一个物体？这个框应该怎么调整？
```

Faster R-CNN 论文明确说，RPN 在每个位置同时预测 objectness score 和边界框。[Faster R-CNN论文](https://proceedings.neurips.cc/paper/2015/file/14bfa6bb14875e45bba028a21ed38046-Paper.pdf?utm_source=chatgpt.com)



### 它为什么会学会“像不像物体”？

因为训练的时候，**我们有真实答案。**

例如训练图片：

```
真实目标：┌───────────┐│    🐕     │└───────────┘
```

我们可以在很多 anchor 中寻找：

```
anchor Aanchor Banchor Canchor D...
```

然后计算每个 anchor 和真实框的 IoU。

比如：

```
anchor A → IoU = 0.82anchor B → IoU = 0.04anchor C → IoU = 0.63
```

那么训练时就可以告诉网络：

```
A：你很像目标 → 正样本B：你明显不是 → 负样本C：比较像      → 可能正样本/按规则处理
```

于是网络慢慢学会：

```
feature→ 这东西像不像一个真实目标
```

所以 RPN 的 “objectness” 并不是凭空知道的。

它是：

> **拿真实标注监督出来的。**

这也是为什么 D2L 会说 Faster R-CNN 的 RPN 是和整个检测网络一起训练的。





## Yolo 的网格机制和 Proposal 的机制之间有什么区别？为什么要引入网格机制？



### Yolo 的网格机制是怎么将第 `(i,j)` 个输出槽位，就负责原图第 `(i,j)` 个区域这个含义赋予进去的？是损失函数吗？如果他有这种空间关系的话，那如果我的目标物品处于图片中的不同位置的话，那他不就不能识别出来了？这不是 bug 吗？



## SSD 的核心思路是什么？它和 Yolo 之间有什么区别
