

[TOC]

### Linux内核版本号

内核版本号：r.x.y

r: 目前发布的内核主版本

x: 偶数表示稳定版本，奇数表示开发中版本

y: 错误修补的次数

### 探查进程命令ps

Unix风格选项：选项前加单连字符

BSD风格选项：选项前不加连字符

GNU风格选项：选项前加双连字符

格式：ps [option]

***Unix风格选项***

-A：显示所有进程

-a：显示除控制进程和无终端进程外的所有进程

-d：显示除控制进程外的所有进程

-e：显示所有进程

-F：显示更多的额外输出

-f：显示完整格式的输出

-l：显示长列表

-V：显示ps命令的版本号

##### -f选项中输出内容的部分信息列：

1. UID：启动该进程的用户
2. PID：进程ID
3. PPID：父进程的PID（如果该进程是由另一个进程启动的）
4. C：进程生命期中的CPU利用率
5. STIME：进程启动时的系统时间
6. TTY：进程是从哪个终端设备启动的
7. TIME：运行进程的累计CPU时间
8. CMD：启动的程序名称

##### 使用-l选项后出现的信息列含义：

F：内核分配给进程的系统标志

S：进程的状态（o代表正在运行；S代表在休眠；R代表可运行，正在等待运行；Z代表僵化，已终止但找不到其父进程；T代表停止）

PRI：进程的优先级（数字越大，优先级越低）

NI：谦让度（nice），用于觉得优先级

ADDR：进程的内存地址

SZ：进程被换出是所需交换空间的大致大小

WCHAN：进程休眠的内核函数地址

***BSD风格选项***

T：显示与当前终端关联的所有进程

a：显示与任意终端关联的所有进程

g：显示包括控制进程在内的所有进程

r：仅显示运行中的进程

##### BSD风格输出信息列含义：

VSZ：进程占用的虚拟内存大小（以KB为单位）

RSS：进程在未被交换出时占用的物理内存大小

STAT：代表当前进程状态的多字符状态码

**在BSD风格的l选项的STAT列进程状态码**

第一个字符采用了与Unix风格的s输出列相同的值：o代表正在运行；S代表在休眠；R代表可运行，正等																						待运行；Z代表僵化；T代表停止

第二个字符进一步说明进程的状态：

1. <：该进程以高优先级运行
2. N：该进程以低优先级运行
3. L：该进程有锁定在内存的页面
4. s：该进程时控制进程
5. l：该进程拥有多线程
6. +：该进程在前台运行



#### 结束进程

kill + 进程的PID（进程ID）

pkill + 进程名（还可使用通配符）

#### 数据排序

*sort*

sort命令会依据会话所指定的默认语言的排序规则来对文本文件中的数据进行排序

如果希望数字按值排序则需要使用***-n***选项



#### linux符号

1. 竖线‘|’ ，在linux中是作为管道符的，将‘|’前面命令的输出作为'|'后面的输入

2. 双竖线‘||’，用双竖线‘||’分割的多条命令，执行的时候遵循如下规则，如果前一条命令为真，则后面的命令不会执行，如果前一条命令为假，则继续执行后面的命令。
3. &同时执行多条命令，不管命令是否执行成功
4. && 可同时执行多条命令，当碰到执行错误的命令时，将不再执行后面的命令。如果一直没有错误的，则执行完毕。



#### 数据归档

tar命令

***tar命令操作：***

-A ：将一个tar归档文件追加到另一个tar归档文件末尾

-c ： 创建新的tar归档文件

-t ：列出tar归档文件的内容

-x ：从tar归档文件中提取文件

***tar命令选项：***

-C  dir ：切换到指定目录

-f  file ： 将结果输出到文件

-j ： 将输出传到gzip2命令进行压缩

-v ：在处理文件时显示文件名

-z ：将输出传给gzip命令进行压缩

#### grep命令

格式： grep [option] pattern [file]

例：

grep t file1

在file1文件中查找能够匹配t的文本

C:\Users\R\AppData\Local\Temp\CUDA



更换下载源

备份：

“/”为根目录

复制，压缩、解压缩应该使用sudo身份（管理员身份）

管理员的身份与usr身份的权限不一样



sudo cp -v 



修改权限：

sudo chmod 777 + 文件路径身份权限修改（所有人可读可写可执行）

sudo gedit + 文件路径 通过编辑器的方式打开文件夹

​		编辑器

sudo apt update

sudo apt upgrade



apt-get 是apt的升级进化版，通过网络进行安装

如果是在



绿色文件：可执行文件

红色文件：压缩包以及压缩文件

蓝色文件：目录

白色文件：一般性文件，如文本文件，配置文件，源码文件

浅蓝色文件：链接文件，主要是使用ln命令建立的文件

wget：获取网络地址路径，是linux中一个下载**文件**的工具，

基本语法：wget [参数列表] URL

-z或--gzip或--ungzip通过gzip指令处理备份文件

-x或--extract或--get从备份文件中还原文件

make是命令工具，它解释Makefile（文件/脚本）中的指令

makefile是一个脚本，这个文件中描述了所有文件的编译顺序、编译规则







bios是一个系统存储，系统参数

cmos是RAM是bios更改bios后将会经过cmos进行写入，bios存储在cmos中，cmos只要有电就可以运行







**第一阶段**：引导过程

==》系统上电

​		==》CPU自身初始化

——————————BIOS——————————

​			==》加电自检

​					==》加载内核引导程序

————————内核引导程序—————————

​							==》主引导程序

​								==》次引导程序

————————————————————————

​									==>调用内核





1.内核引导

- 当计算机打开电源后，首先是BIOS开机自检，按照BIOS设置的启动设备（通常是硬盘）来启动。、

- 操作系统接管硬件之后，首先读入/boot目录下iade内核文件

    

2.运行init

- init进程是系统所有进程的起点，没有这个进程，系统中任何进程都不会启动
- init程序首先是需要读取配置文件/etc/inittab

3.运行级别

- 许多程序需要开机启动，他们在windows叫做“服务”







linux的七个运行状态

- 运行级别0：系统停机状态，系统默认运行级别不能设为0，否则不能正常运行
- 运行级别1：单用户工作状态，root权限，用于系统系统维护，禁止远程登录
- 运行级别2：多用户状态（没有NFS）
- 运行级别3：完全的多用户状态（有NFS）
- 运行级别4：系统未使用，保留
- 运行级别5：X11控制台，登录后进入图形GUI模式
    - 运行级别6：系统正常关闭并重启，默认运行级别不能设为6，否则不能正常启动

![1678872042578](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1678872042578.png)

groupmod -g 标识号 新的组名 旧的组名



通过修改 /usr/share/vim/vimrc中的配置文件使得创建sh脚本有以下开头附属信息

使用vim创建sh文件

```
if filereadable("/etc/vim/vimrc.local")
  source /etc/vim/vimrc.local
endif

autocmd BufNewFile *.py,*.cc,*.sh,*.java exec ":call SetTitle()"
func SetTitle()
        if expand("%:e") == 'sh'
                call setline(1,"#!/bin/bash")
                call setline(2,"##################################")
                call setline(3,"# File Name:".expand("%"))
                call setline(4,"# Version:V1.0")
                call setline(5,"Author Z")
                call setline(6,"# Created Time:".strftime("%F %T"))
                call setline(7,"# Description")
                call setline(8,"##################################")

        endif
endfunc

```



————————————————————————————

#### 条件表达符号

- 大于 -gt (greater than)
- 小于 -lt (less than)
- 大于或等于 -ge (greater than or equal)
- 小于或等于 -le (less than or equal)
- 不相等 -ne （not equal）
- 相等 -eq （equal）

————————————————————————————

#### Test命令

Test命令一般用于判断与if的思想类似

它的应用类型有：

1.判断表达式

2.判断字符串

3.判断整数

4.判断文件



》**&&**：其中&&左边的命令返回真（即返回0，也就是成功被执行后）&&右边的命令才能被执行

》**||**：如果||左边的命令未执行成功，那么就执行||右边的代码





————————————————————————————

####  |（管道命令操作符）

”|”含义是管道命令操作符，简称管道符。利用Linux所提供的管道符“|”将两个命令隔开，**管道符左边命令的==输出==**就会**作为管道符右边命令的==输入==**。连续使用管道意味着第一个命令的输出会作为 第二个命令的输入，第二个命令的输出又会作为第三个命令的输入，依此类推。



————————————————————————————

#### chmod

更改文件权限，**r**为可读，**w**为可写， **x**为可执行

例：

```
sudo chmod +x 文件名    # 更改指定文件名的文件权限，这里我们为文件添加了课执行权限
```

————————————————————————————

#### vim命令行的方式下另存文件为指定的文件名

在vim中如果想要将文件另存为指定的文件名的时候我们可以使用

：wq +空格+ 指定的文件名

————————————————————————————



#### env

查看所有变量

```
env
```



————————————————————————————

#### echo $变量名

查看指定环境变量

```python
echo $变量名
```



————————————————————————————

#### 普通变量

本地变量在用户当前的shell生存期的脚本使用

​	如果在shell中启动一个进程或退出，本地变量值将无效

$变量名表示输出变量，可以用**$b**或**${b}**两种用法

————————————————————————————

#### export

设置或显示环境变量

可将本地变量变成全局变量（环境变量）

```
#!/bin/bash

var='hello,world'
export var
```

在sh脚本中执行**export定义环境变量**

然后在命令行中执行

echo $环境变量名

可以输出环境变量中定义的值

**注：**如果希望环境变量永久生效，则可以将其放在用户环境变量文件或全局变量文件中

————————————————————————————

#### 变量中的引号

- 当变量里面的值中有空格的时候，就会使用引号
- 单引号与双引号的区别在于，单引号能够解析特殊符号，而双引号却不能



————————————————————————————

#### 反引号

反引号主要是用来引用一些函数、命令的输出等

并且当命令被执行的时候系统会尝试将反引号的内容当作一个命令来执行

————————————————————————————

#### ${}

${}通常是获取变量的值，但也可以进一步对变量的值进行“加工”处理，例如，字符串的截取，替换



————————————————————————————

#### 变量总结

变量名只能为字母、数字或者是下划线，只能以字母或下划线开头

变量名的定义要有一定的规范

一般的变量定义、赋值常用**双引号**；简单连续的字符串可以**不加引号**；希望原样输出的就使用**单引号**

希望变量的内容是命令的解析结果时，要用反引号**``**，或则用**$()**把命令括起来再赋值



**在使用脚本文件进行环境变量赋值的时候应该使用source**

理由：

source与bash的不同之处

当使用bash执行脚本文件的时候，会创建一个子进程来执行脚本文件的内容，也就是说如果我们使用bash来进行变量赋值的话，变量只会被限制在设置环境变量的脚本子进程中，而不会影响到父进程或其他进程。，因此我们直接输出这个变量将会为空。

但使用source来执行赋值脚本文件的时候，将会在当前的shell中执行，那么这个变量就会设置在当前的shell进程的环境变量中。

————————————————————————————

#### linux中的||

双竖线"||"，用双竖线分割多条命令，执行的时候遵循如下规则：

如果前一条命令为真，则后面的命令不会执行

如果前一条命令为假，则继续执行后面的命令



————————————————————————————

#### 位置变量



##### $0

获取当前执行的shell脚本的文件名，如果执行脚本带路径那么就包括脚本路径



##### $n

获取当前执行的shell脚本的第n个参数值，n=1…9，当n为0时表示脚本的文件名，如果n大于9用大括号括起来如{10}，参数以空格隔开



##### $#

获取当前执行的shell脚本后面接的参数的总个数



##### $*

获取当前shell的所有传参的参数，不加引号同$@；

如果给$*加上双引号，例如：“$\*”，则表示将**所有的参数视为单个字符串**，相当于“12$3”



##### $@

与$*功能相同， 获取当前shell的所有传参的参数， 如果不加引号功能与$\*功能相同

如果给$@加上双引号，例如“$@”，则表示将所有参数是为不同的独立字符串，相当于“$1” "$2" "$3"等等，这是将参数传递给其他程序的最佳方式，因为他会保留所有内嵌在每个参数里的任何空白

**注：**当“$\*”和“$@”都加上双引号，两者会有区别，前者返回的是将所有传递的参数作为一个字符串的形式返回

后者则是将所有传递的参数视为不同的独立字符串再返回

当这两个命令都不加双引号的时候，两者没有区别

应用：

![$特殊变量代码及结果](C:\Users\R\Desktop\linux_exercise\特殊变量\$特殊变量代码及结果.png)

![$星代码及结果](C:\Users\R\Desktop\linux_exercise\特殊变量\$星代码及结果.png)



——————————————————————————————————————



#### read交互式设置变量

用法：

- 用于从标准输入读取数据（键盘）
- 也可以从文件中读取（读取一行文件）

`read [-ers] [-a aname] [-d delim] [-i text] [-n nchars] [-p prompt] [-t timeout] [-u fd] [name....]`



**参数：**

选项说明

| 选项                          | 说明                                                         |
| ----------------------------- | :----------------------------------------------------------- |
| -a array                      | 把读取的数据赋值给数组 array，从下标 0 开始                  |
| -d delimiter                  | 用字符串 delimiter 指定读取结束的位置，而不是一个换行符（读取到的数据不包括 delimiter） |
| -e                            | 在获取用户输入的时候，对功能键进行编码转换，不会直接显式功能键对应的字符。 |
| -n num                        | 读取 num 个字符，而不是整行字符。                            |
| -p prompt                     | 显示提示信息，提示内容为 prompt。                            |
| -r	原样读取（Raw mode）    | 不把反斜杠字符解释为转义字符。                               |
| -s	静默模式（Silent mode） | 不会在屏幕上显示输入的字符。当输入密码和其它确认信息的时候，这是很有必要的。 |
| -t seconds                    | 设置超时时间，单位为秒。如果用户没有在指定时间内输入完成，那么 read 将会返回一个非 0 的退出状态，表示读取失败 |
| -u fd                         | 使用文件描述符 fd 作为输入源，而不是标准输入，类似于重定向。 |

![read交互式设置变量](C:\Users\R\Desktop\linux_exercise\特殊变量\read交互式设置变量.png)





————————————————————————————————————————————————



#### 进程状态变量



##### $?

获取执行上一个指令的执行状态返回值（0表示成功，非零表示失败）



##### $

获取当前执行的Shell脚本的进程号（PID）



##### $!

获取上一个在后台工作的进程的进程号（PID）



##### $\_

获取在此之前执行的命令或脚本的最后一个参数



———————————————————————————————————————————————



#### 传参（传递参数）

linux中的传递参数的方法很简单

一般来说可以使用**$+变量名**来进行传参





#### 脚本传参

普通的bash能够跟任意的参数，并且在脚本中我们能够根据传递的参数进行脚本的传参



执行“vim test.sh”创建一个新的shell脚本。脚本test.sh的内容如下：

#!/bin/sh

name=$1

echo "the ${name} are great man!"

给新创建的test.sh的脚本赋可执行权限，命令为“chmod +x test.sh”

执行'./test.sh "xiao wang" '命令，可以看到自己编写脚本的结果“the xiao wang are great man!”

"name=$1"中$1为系统提供的位置参数，$0代表程序的名称（./test.sh），[$1 $2...]从1开始为传递的参数。linux系统除了提供位置参数还提供内置参数，所有内置参数如下：　

$0 ----当前脚本文件名

$n ----传递给脚本或函数的参数，n是一个数字，表示第几个参数，$1表示第一个参数，$2表示第二个参数 ...

$# ----传递给脚本或函数的参数个数

$* ----传递给脚本或函数的所有参数，当它被双引号（" "）包含时，"$*" 会将所有的参数作为一个整体，以"$1 $2 ... $n"的形式输出所有参数

$@ ----传递给脚本或函数的所有参数，当它被双引号（" "）包含时，与$*稍有不同，"$@" 会将各个参数分开，以"$1" "$2" … "$n" 的形式输出所有参数

$* 和$@不被双引号(" ")包含时，都以"$1" "$2" … "$n" 的形式输出所有参数

下面写个小程序实践一下$* 和$@

$? ----上一个命令的退出状态，或函数的返回值，如果正常退出则返回0，反之为非0值

$$ ----当前shell进程pid，对于shell脚本，就是这些脚本所在的进程ID

$! ----上一个命令的pid

新建test.sh chmod +x test.sh 执行./test.sh "a" "b" "c" "d"，观察打印结果

```
#!/bin/bash
echo "\$*=" $*
echo "\$@=" $@

echo "print each param from \$*" 
for var in $*
do
    echo $var
done

echo "print each param from \$@"
for var in $@
do
    echo $var
done

echo "print each param from \"\$*\""
for var in "$*"
do
    echo $var
done

echo "print each param from \"\$@\""
for var in "$@"
do
    echo $var
done


打印结果：
$*= a b c d
$@= a b c d
print each param from $*
a
b
c
d
print each param from $@
a
b
c
d
print each param from "$*"
a b c d
print each param from "$@"
a
b
c
d
```



——————————————————————————————————————————————

#### 变量的子串

| 表达式                      | 说明                                                         |
| :-------------------------- | ------------------------------------------------------------ |
| ${parameter}                | 返回变量$parameter的内容                                     |
| ${#parameter}               | 返回变内容的长度（按字符），也适用于特殊变量                 |
| ${parameter**i**offset}     | 在变量${parameter}中，从位置offset之后开始提取字串到结尾     |
| ${parameter:offset:length}  | 在变量${parameter}中，从位置offset之后开始提取长度为length的子串 |
| ${parameter#word}           | 从变量${parameter}开头开始删除最短匹配的word子串             |
| ${parameter##word}          | 从变量${parameter}开头开始删除最长匹配的word子串             |
| ${parameter%word}           | 从变量${parameter}结尾开始删除最短匹配的word子串             |
| ${parameter%%word}          | 从变量${parameter}结尾开始删除最长匹配的word子串             |
| ${parameter/pattem/string}  | 使用string代替第一个匹配的patterm                            |
| ${parameter//pattem/string} | 使用string代替所有匹配的patterm                              |

小结：

- #表示从开头删除匹配最短
- ##表示从开头删除匹配最长
- %表示从结尾删除匹配最短
- %%表示从结尾删除匹配最长
- a*c表示匹配的字符串，\*表示匹配所有，a\*c表示匹配开头为a、中间为任意多字符、结尾为c的字符串





——————————————————————————————————————————————



#### 特殊扩展变量



##### ${parameter:-word} 

如果parameter的变量值为空或未赋值，则会返回word字符串并且替代变量的值用途，如果变量未定义，则返回备用的值，防止变量未空值或因未定义而导致异常



##### ${parameter:=word}

如果parameter的变量值为空或未赋值，则设置这个变量为word，并返回其值。

位置变量和特殊变量不适用用途：基本同上一个${parameter>word}，但该变量有额外给parameter变量赋值了



##### ${parameter:?word}

如果parameter变量值为空或未赋值，那么word字符串将被作为标准错误输出，否则输出变量的值。

用途：用于捕捉由于变量未定义而导致的错误，并退出程序



##### ${parameter:+word}

如果parameter变量值为空或未赋值，则什么都不做，否则word字符串将替代变量的值





————————————————————————————————————————————————



#### Test命令或 []

在linux下的test命令是脚本编写中一个非常重要的命令

其主要用于文件（目录）的检测、属性判读、权限判断等

而在使用该命令的过程中，一般不直接使用test -n字符串这类的表达式去表示

而是使用“[ ]”代替test命令。“ [ ] ”里使用的参数是继承自test命令的



——————————————————————————————————



#### &&

&&左边的命令（命令1）返回真（即返回0，成功被执行）后，&&右边的命令（命令2）才能被执行



————————————————————————



#### ||

如果||左边的命令未执行成功，即返回非零整数，那么就执行||右边的命令





————————————————————————





#### 文件判断 [  ]

| 常用文件测试操作符               | 说明                                                         |
| -------------------------------- | ------------------------------------------------------------ |
| -d文件，d的全拼为directory       | 文件存在且为目录则为真，即测试表达式成立                     |
| -f文件，f的全拼为file            | 文件存在且为普通文件则为真，即测试表达式成立                 |
| -e文件，e的全拼为exist           | 文件存在则为真，即测试表达式成立。注意区别于“-f”， -e不是辨别是目录还是文件 |
| -r文件，r的全拼为read            | 文件存在且可读则为真，即测试表达式成立                       |
| -s文件，s的全拼为size            | 文件存在并且文件大小不为0则为真，及测试表达式成立            |
| -w文件，w的全拼为write           | 文件存在且可写则为真，即测试表达式成立                       |
| -x文件，x的全拼为executable      | 文件存在且可执行则为真，即测试表达式成立                     |
| -L文件，L的全拼为link            | 文件存在且为链接文件则为真，即测试表达式成立                 |
| f1 -nt f2， nt的全拼为newer than | 文件f1比文件f2新则为真，即测试表达式成立，根据文件的修改时间来计算 |
| f1 -ot f2， ot的全拼为older than | 文件f1比文件f2就则为真，即测试表达式成立，根据文件的修改时间来计算 |





———————————————————————



### 基本命令



#### whatis

查看变量或数组或函数的数据类型





#### list查看源代码









#### userdel

删除指定的用户







#### $>/dev/null

将不需要的变量重定向，释放内存空间，删除变量G







#### 左方括号代表着Test命令





####  perror

查看上一条命令执行错误的类型以及原因





#### rpm

软件包管理器，即linux中的软件包

![1687355597365](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1687355597365.png)





#### tr命令





#### cut命令





#### pwgen软件生成随机密码

**-c** or –capitalize 
密码中至少包含一个大写字母

**-A** or –no-capitalize 
密码中不包含大写字母

**-n** or –numerals 
密码中至少包含一个数字

**-0** or –no-numerals 
密码中不包含数字

**-y** or –symbols 
密码中至少包含一个特殊符号

**-s** or –secure 
生成完全随机密码

**-B** or –ambiguous 
密码中不包含歧义字符（例如1,l,O,0）

**-H** or –sha1=path/to/file[#seed] 
使用SHA1 hash给定的文件作为一个随机种子

**-C** 
在列中打印生成的密码

**-1** 
不要在列中打印生成的密码，即一行一个密码

**-v** or –no-vowels 
不要使用任何元音，以避免偶然的脏话



——————————————————————————————————、



## 源代码编译的过程

- **预处理：**对源代码文件中的文件包含（include）、预编译语句（如宏定义define等）进行分析
- **编译：**将c/c++代码进行翻译成汇编语言
- **汇编：**将第二步输出的汇编代码翻译成符合一定格式的机器代码，生成以.s为后缀的目标文件
- **链接：**将上步生成的目标文件和系统库的目标文件和库文件链接起来，最终生成了可以在特定平台运行的可执行文件

![1687573431081](C:\Users\R\AppData\Roaming\Typora\typora-user-images\1687573431081.png)

简单点概括：

1.首先将源程序中的include等的语句进行扩展，将头文件include进行头文件引入，将原来的头文件（include）以及宏代码（define）替换为具体文件的内容，可使用gcc —E \*.c来进行查看（这一步也可称为头文件引入以及宏展开）

2.将预处理好的文件进行编译，编译成汇编语言

3.将编译好的汇编语言组合起来

4.将编译好组合好的.c文件转换为.o文件，也就是所谓的目标文件，然后使用链接器，将这些目标文件.o链接成一个可执行文件



这些步骤的命令分别为：

 编译：  gcc -c xxx.c/xxx.s

 链接： gcc  *.o   -o  xxx



——————————————————————————————————



### gcc

-o *file*

将经过gcc处理过的结果存为文件file，这个结果可能是预处理文件、编译文件、目标文件或者最终的可执行文件，这里的-o的作用就是将编译过后的文件指定到指定的文件中



-c 

只编译，不链接成为可执行文件，编译器只是由输入的.c等源代码文件生成.o后缀的目标文件

















### make工具

在linux的程序开发下，一般不具有集成开发环境（IDE），因此当需要大量编译工程文件时，就需要用到make这个自动编译工具

make工具最初设计的目的时为了维护c的文件，防止不必要的重新编译，简单点来说就是

`当我们的工程文件中的一个需要在命令行中进行了修改，并且修改的是已经编译过的文件，那么当我们重新编译的时候，make工具就会自动的确定工程的那部分源程序文件需要重新编译，然后执行命令却编译他，而不去重新编译没有更改的程序`



通常多个源文件的编译使用的就是makefile，它定义了一套规则，决定了哪些文件需要先编译，哪些文件后编译，哪些文件需要重新编译

**2. Makefile的优点**

1. 管理代码的编译，决定该编译什么文件，编译顺序，以及是否需要重新编译；
2. 节省编译时间。如果文件有更改，只需重新编译此文件即可，无需重新编译整个工程；
3. 一劳永逸。Makefile通常只需编写一次，后期就不用过多更改。









**3. 命名规则**

一般来说将Makefile命名为**Makefile**或**makefile**都可以，但很多源文件的名字是小写的，所以更多程序员采用的是Makefile的名字，因为这样可以将Makefile居前显示。

如果将Makefile命为其它名字，比如Makefile_demo，也是允许的，但使用的时候应该采用以下方式：

> make -f Makefile_demo

**4. 基本规则**

Makefile的基本规则为：

```
目标：依赖

(tab)规则
```

目标 --> 需要生成的目标文件 依赖 --> 生成该目标所需的一些文件 规则 --> 由依赖文件生成目标文件的手段 tab --> **每条规则必须以tab开头**，使用空格不行

例如我们经常写的gcc test.c -o test，使用Makefile可以写成：

> test: test.c  gcc test.c -o test

其中，第一行中的test就是要生成的目标，test.c就是依赖，第二行就是由test.c生成test的规则。

Makefile中有时会有多个目标，但Makefile会**将第一个目标定为终极目标**。

**5. 工作原理**

> **目标的生成：** a. 检查规则中的依赖文件是否存在； b. 若依赖文件不存在，则寻找是否有规则用来生成该依赖文件。

![img](https://pic3.zhimg.com/80/v2-c818aa04205e2b120a0adadc20d513b6_720w.webp)

比如上图中，生成calculator的规则是gcc main.o add.o sub.o mul.o div.o -o，Makefil会先检查main.o, add.o, sub.o, mul.o, div.o是否存在，如果不存在，就会再寻找是否有规则可以生成该依赖文件。

比如缺少了main.o这个依赖，Makefile就会在下面寻找是否有规则生成main.o。当它发现gcc main.c -o main.o这条规则可以生成main.o时，它就利用此规则生成main.o，然后再生成终极目标calculator。

整个过程是向下寻找依赖，再向上执行命令，生成终极目标。

> **目标的更新：** a. 检查目标的所有依赖，任何一个依赖有更新时，就重新生成目标； b. 目标文件比依赖文件时间晚，则需要更新。

![img](https://pic1.zhimg.com/80/v2-898e9b8060577381c03abb43dacb1898_720w.webp)



比如，修改了main.c，则main.o目标会被重新编译，当main.o更新时，终极目标calculator也会被重新编译。其它文件的更新也是类推。

**6. 命令执行**

**make:** 使用此命令即可按预定的规则生成目标文件。 如果Makefile文件的名字不为Makefile或makefile，则应加上**-f**选项，比如：

> make -f Makefile_demo

**make clean:**

清除编译过程中产生的中间文件（.o文件）及最终目标文件。

如果当前目录下存在名为clean的文件，则该命令不执行。

-->解决办法：伪目标声明：.PHONY:clean。

**特殊符号：**

**-** ：表示此命令即使执行出错，也依然继续执行后续命令。如：

-rm a.o build/

**@**：表示该命令只执行，不回显。一般规则执行时会在终端打印出正在执行的规则，而加上此符号后将只执行命令，不回显执行的规则。如：

> @echo $(SOURCE)

**7. 普通变量**

**变量定义及赋值：**

变量直接采用赋值的方法即可完成定义，如：

> INCLUDE = ./include/

**变量取值：**

用括号括起来再加个美元符，如：

> FOO = $(OBJ)

系统自带变量：

通常都是大写，比如**CC，PWD，CFLAG**，等等。

有些有默认值，有些没有。比如常见的几个：

> CPPFLAGS : 预处理器需要的选项 如：-I CFLAGS：编译的时候使用的参数 –Wall –g -c LDFLAGS ：链接库使用的选项 –L -l

变量的默认值可以修改，比如CC默认值是cc，但可以修改为gcc：**CC=gcc**

**8. 自动变量**

**常用自动变量：**

Makefile提供了很多自动变量，但常用的为以下三个。这些自动变量只能在规则中的命令中使用，其它地方使用都不行。

$@ --> 规则中的目标

$< --> 规则中的第一个依赖条件

$^ --> 规则中的所有依赖条件

例如：

> app: main.c func1.c fun2.c gcc $^ - o $@

其中：$^表示main.c func1.c fun2.c，$<表示main.c，$@表示app。

**模式规则：**

模式规则是在目标及依赖条件中使用%来匹配对应的文件，比如在目录下有main.c, func1.c, func2.c三个文件，对这三个文件的编译可以由一条规则完成：

> %.o:%.c  $(CC) –c $< -o $@

这条模式规则表示：

main.o由main.c生成，  func1.o由func1.c生成，  func2.o由func2.c生成

这就是模式规则的作用，可以一次匹配目录下的所有文件。

**9. 函数**

makefile也为我们提供了大量的函数，同样经常使用到的函数为以下两个。需要注意的是，**makefile中所有的函数必须都有返回值**。在以下的例子中，假如目录下有main.c，func1.c，func2.c三个文件。

**wildcard:**

用于查找指定目录下指定类型的文件，跟的参数就是目录+文件类型，比如：

> src = $（wildcard ./src/*.c)

这句话表示：找到./src 目录下所有后缀为.c的文件，并赋给变量src。

命令执行完成后，src的值为：main.c func1.c fun2.c。

**patsubst:**

匹配替换，例如以下例子，用于从src目录中找到所有.c 结尾的文件，并将其替换为.o文件，并赋值给obj。

```text
obj = $(patsubst %.c ,%.o ,$(src))
```

把src变量中所有后缀为.c的文件替换成.o。

命令执行完成后，obj的值为main.o func1.o func2.o

特别地，如果要把所有.o文件放在obj目录下，可用以下方法：

```text
ob = $(patsubst ./src/%.c, ./obj/%.o, $(src))
```







## GDB调试工具

GDB是用于linux系统中的Debug工具，简单点来说就是一个调试工具，我们在编程的时候能够测试自己编写好的程序，保证万无一失

其中我们在对自己的程序使用gcc进行编译的时候需要使用`gcc -g`命令编译程序，并生成调试信息，然后才使用gcc工具对这个已经编译好的程序进行调试





#### run指令

将指定编译好的程序执行，也可以直接简写成`r`





#### quit

退出gdb调试工具，也可以简写成`q`





#### file指令

如果我们在进入gdb调试工具的时候后面没有跟需要调试的文件名，而是直接使用`gdb`进入调试工具的话，我们需要在进入gdb调试工具过后使用 **file**命令来指定需要调试的文件





#### break

为程序设置断点，可以使用`break`或`b`来为程序设置断点

它有好几种设置方法：

1.break  + 要进行断点的函数名

2.break + 调试文件名：要设置的行号

3.





#### continue指令

当我们设置好断点过后我们就可以直接运行我们的调试程序，当程序运行到我们设置好的断点位置的时候程序就会暂停，而如果当我们想要让程序继续运行的时候我们可以使用`continue`或`c`指令来继续运行程序





#### next指令

当我们有断点的位置为循环或其他情况，我们想要进行单步调试的时候，我们可以使用`next`或`n`来对程序断点进行单步调试





#### end键

当我们在使用了单步调试命令的时候，当程序运行的步数比较多的时候，我们每次都需要执行一次next命令或n命令，那么这样就非常的耗费时间，但我们可以使用`end`键来重复执行上一条调试指令





### step命令

通常情况下，step命令和next命令的功能相同，都是单步执行程序，不同之处在于，当step命令所执行的代码行中包含函数时，会进入到函数的内部，并在函数第一行代码处暂停运行







#### list指令

当我们在调试的过程中想要查看一下源代码的时候，我们可以使用`list`指令，他会**显示接下来将会运行的10行代码**





#### print指令

当我们在调试程序的过程中查看程序中某个变量的值的时候，我们可以使用`print`或`p`指令来查看变量的当前值

具体的格式为

print + 变量名 

当然不仅仅时变量的值，我们还可以打印函数的值（函数的当前地址）、**数组的值**（同时在打印数组的值的时候，我们也可以使用像c语言那样的**索引方式来打印数组中指定索引的值**）





#### info指令

用于查看程序结果外的信息，例如断点信息，显示值的表达式等等

当我们想要看设置过哪些断点的时候我们可以使用

`info b（break）`来查看已经设置的断点信息







#### delete指令

删除断点，同时也可以缩写成`d`

如果我们在使用delete指令的时候没有指定要删除哪个序号的断点，那么这个时候delete指令将会将所有的断点都删除

而如果我们需要删除指定的断点则需要在delete后面跟上断点的序号`delete 序号`或`d 序号`



