

# 什么是强化学习？

强化学习是通过与环境进行交互来实现具体目标的一种计算方法

具体的交互是指：机器在指定的环境作用下进行决策出一种动作（action），并将这个动作应用到环境中，这个环境发生响应的变化并提供一个奖励反馈以及下一轮状态传回给机器，然后不断地循环这个过程，而机器的目标就是在这个环境中最大化得到的奖励。具体如下图：

<img src="E:\Markdown\markdown\强化学习.assets\11.da5ee18f-1748391358912-3.png" rate=80%>



## 强化学习与深度学习之间的区别是什么？

强化学习是找到最优的方式，他是一种决策，这种决策又可以成为是“最优动作”的预测任务

深度学习是找到概率最大的方式，他是一种预测

其次，因为决策任务是多轮的，智能体就需要在每轮做决策时考虑未来环境相应的改变，所以当前轮带来最大奖励反馈的动作，在长期来看并不一定是最优的。



## 强化学习的环境

由上面的第一章中可以发现，强化学习的智能体是在<u>和一个动态环境的交互</u>中完成决策，也就是智能体能够随着某些因素的变化而不断演变，这在数学和物理中往往用随机的过程来进行刻画，而对于这个随机过程，最关键的要素就是状态以及状态转移的条件概率分布。

而如果在环境自身演变的随机过程中加入一个外来的干扰因素，即智能体的动作，那么环境的下一刻状态的概率分布将会由当前状态与智能体的动作共同来决定，使用数学公式进行表示则是：

![image-20250528224744832](E:\Markdown\markdown\强化学习.assets\image-20250528224744832-1748443666596-1-1748443667692-3-1748443669106-5.png)

根据这个公式，我们可以知道智能体决策的动作作用到环境中，使得环境发生响应的状态变化，而<u>智能体接下来则需要在新的状态</u>下进一步给出决策。

而由于面向决策任务的智能体进行交互的环境是一个动态却随机的过程，其未来状态的分布由当前状态和智能体的动作共同决定，并且每一轮状态转移都伴随两方面的随机性：

一、是智能体决策动作的随机性

二、是环境基于当前状态和智能体动作而进入的下一刻状态的随机性

由这两方面的不同我们可以发现，在动态随机过程中进行学习与固定数据分布下进行学习是非常不同的

总的来说强化学习的环境就是一个随机状态的环境，它是动态且无法具体确定的

## 强化学习的目标

由第一章中我们可以得知，智能体和环境每次进行交互的时候，**环境**都会产生一个**奖励值**，且该奖励值往往使用实数标量来表示。这个<u>奖励值一般是用来诠释当前状态或者动态的好坏的及时反馈信号</u>，而整个交互过程的每一轮获得的奖励信号可以进行叠加，形成智能体的整体回报（return），同时由环境的动态性我们可以知道，即使环境和智能体的策略不变，智能体的初始化状态也不变，智能体和环境交互产生的结果也可能是不相同的，对应的回报也会是不同的。因此，强化学习将会关注回报的期望



强化学习的核心组成部分：智能体与环境的 “互动循环”
强化学习的本质是智能体通过与环境互动，学会做出最优决策。其核心组成可拆分为以下 5 大模块，用 “玩游戏” 类比更易理解：
1. 智能体（Agent）：做决策的主体
角色：比如玩《王者荣耀》的 AI 玩家，负责观察游戏画面并决定 “放技能”“移动” 等操作。
目标：通过学习，让自己的决策（动作）最大化长期奖励（比如赢游戏）。
2. 环境（Environment）：智能体互动的对象
定义：智能体之外的一切，包括状态、规则和反馈。比如《王者荣耀》的游戏地图、敌方英雄位置、技能冷却机制等。
特点：环境对智能体的动作会产生 “状态转移” 和 “奖励反馈”。
3. 状态（State）：环境的当前快照
举例：玩游戏时，屏幕上显示的 “我方英雄血量、敌方位置、技能 CD” 等所有信息，就是当前状态 s。
作用：智能体根据状态 s 决定下一步动作，状态是决策的依据。
4. 动作（Action）：智能体的决策输出
举例：在某个状态下，智能体选择 “闪现”“普攻” 或 “移动”，这些就是动作 a。
关系：动作 a 会影响环境，导致状态从 s 转移到 s’，并获得奖励 r。
5. 奖励（Reward）：环境给智能体的反馈 “分数”
定义：智能体做动作后，环境给出的即时评价，比如 “击杀敌人 + 10 分”“被攻击 - 5 分”。
核心目标：智能体的所有学习行为，都是为了最大化未来奖励的总和（长期收益）。
6. 额外关键概念：策略、价值与模型
策略（Policy π）：智能体的 “决策规则”，即给定状态 s 时选择动作 a 的概率 π(a|s)。比如 “残血时 70% 概率闪现逃跑”。
价值函数（Value Function）：评估 “在状态 s 时，未来能拿多少分”，比如 V (s) 表示从 s 开始的期望总奖励。
模型（Model）：对环境的预测，比如预测 “我放技能后，敌人会怎么移动”，但很多算法不依赖模型（如 PPO）。



另一个重要的概念就是：见好就收与继续探索之间的平衡

==要训练好强化学习的决策模型，我们就需要通过尝试很多不同的策略，让模型能够向我们想要的方向前进==

# 多臂老虎机

我们使用多臂老虎机这个经典问题来看看强化学习的核心概念

## Q值

Q值表示智能体在某个状态下采取特定动作后，预期能够获取的总回报（即长期累计奖励）



它回答了：“如果在状态 s 下选择动作 a ，之后一直按照策略 Π 行动，最终能够拿到多少分？”



## 期望

期望是概率论与数理统计中对**随机事件长期结果的平均结果的预测 **



### 期望奖励

指在指定策略下，某个动作或者状态的**理论平均值**

### 经验奖励

指在指定的策略下，某个动作或者状态的**实际观察到的平均值**



## 累计懊悔

### 懊悔

即采取的动作的奖励值与最优动作的奖励值之差

### 累计懊悔

采取的动作的奖励值与最优动作的奖励值之差之和





## 探索与利用的平衡

在多臂老虎机的问题中，经典的问题就是==探索==与==利用==的平衡问题

其中：

* 探索：指尽可能多的去尝试拉动更多的拉杆，去试拉杆的奖励
* 利用：保守的利用现有的已知奖励的拉杆

而在老虎机问题中，核心问题是： 如何在不确定的环境中，通过有限的尝试次数，高效的找到最佳的策略，以最大化长期累计奖励，

因此我们在设计策略的时候就需要平衡==探索==与==利用==的次数，探索的次数多了，尝试的次数就过多，而尝试次数少了，决策未必是最优的



而常见的思路则是开始时探索较多次，而当对所有的拉杆的奖励分布已经有较为准确的估计过后，再进行利用

而已经有这类的算法解决上面的思路：







## 完全贪婪算法

完全贪婪算法即在每一时刻采取期望奖励估值最大的动作，即完全的**利用**



## $$\epsilon$$-贪婪算法

而 $\epsilon$ - 贪婪算法则是在贪婪算法的基础上添加噪声 $\epsilon$ , 即通过一个噪声 $\epsilon$ , 这个噪声随着时间衰减，每次进行决策的时候，以 $1-\epsilon$ 的概率采取**利用策略**（即在以往的经验中期望奖励值最高的那个拉杆），以 $\epsilon$ 的概率采用**探索策略**（不采用经验，单纯的去尝试），该算法的公式如下：
$$
a_{t}=\left\{\begin{array}{ll}
\arg \max _{a \in \mathcal{A}} \hat{Q}(a), & \text { 采样概率: } 1-\epsilon \\
\text { 从 } \mathcal{A} \text { 中随机选择, } & \text { 采样概率: } \epsilon
\end{array}\right.
$$


并且注意：<u>$\epsilon$ 不会在有限的步数内削减至 0</u>， 引入如果基于有限步观测的贪婪算法依旧是只有局部信息的算法，与最优解依旧会有差距







## 概率分布

[ 概率分布是什么——一些常见的概率分布 - 知乎](https://zhuanlan.zhihu.com/p/615643051)

概率分布描述了随机变量的所有可能取值，与其对应的取值可能出现的概率

<img src="E:\Markdown\markdown\强化学习.assets\v2-6b82463ba3e09bf6913fd33cbf9baf96_1440w-1755057343971-7-1755057344979-10.jpg" alt="img" style="zoom:47%;" />

上图中就是一个分布，其名为 ==正态分布==， 其中 $X$ 轴的值为所有的可能取值，而 $Y$ 轴则是所有可能取值的出现概率







## 上置新界算法

同样是在多臂老虎机问题中，一根拉杆的不确定性越大，它就越具有<u>探索的价值</u>， 这个<u>价值就是与它的不可能性相关</u>，因为探索之后可能会发现他的期望奖励会很大

因此我们就会使用到不确定性度量 $U(a)$，它会随着一个动作被尝试次数的增长而减小，因此我们就可以使用到不确定性的策略来综合考虑现有的期望奖励估值和不确定性

而**上置信界算法**就是一种经典的基于不确定性的策略算法，而这个算法的思想使用了一个著名的数学原理：==霍夫丁不等式==

### 霍夫丁不等式

霍夫丁不等式给出了随机变量的和与其期望值偏差的概率上限





# PPO算法



# Actor-Critic算法

Actor-Critic算法是一种强化学习的方法，结合了演员（Actor）和评论家（Critic）两个部分，从名字就能很直观地看出来这个算法的具体：

首先角色来看：

* Actor（演员）：它就像是一个冒险家，负责决定下一步往哪走，走多远， 但是它并不聪明，有可能会犯错
* Critic（评论家）：它就像是一个导师，站在一边，评论冒险家的行为，他会告诉冒险家：这一步走的好，那一步走的坏，接近了山顶或者原理了山顶

然后就是算法的具体步骤：

> 1. actor将会是观察环境（如坡度、方向），根据它的经验（Policy）选择下一个动作（action）
> 2. critic会根据actor的动作与环境的反馈（如：坡度的变化以及高度的增加或减小），计算一个“价值”（Value），来表示这个动作的好坏
> 3. actor很好学，根据critic的评价，来调整自己的策略，使得未来能够更加聪明的选择动作



1. 我遇到了什么问题，学习强化学习算法的时候：PPO算法的旧策略采样，与新策略更新问题

    这个问题主要就是节省了样本，这个问题主要做的步骤是：

    1. 原始旧的策略与环境进行交互，并收集观察值、状态、操作、得到的奖励等等参数
    2. 使用这些旧策略收集到的数据，通过数学方法（比如重要性权重等）来计算出“如果使用旧的策略做事情，并从中学习到新的方法，使得做得比上一次做得更好（即新的策略）的优化方向在哪里”

2. 离线方法是什么：简单点来说就是旧策略在进行收集数据的时候，一次旧收集一批次的样本，而且这些样本能重复使用100次来更新新的策略的参数

3. 重要性权重这个数学方法实际上的作用就是：从预期估计中找到策略模型的更新方向，即我要达到更好的目标，我的优化方向是什么？

    1. 什么是PPO算法，它到底在做什么
        PPO算法核心就在于两个策略的使用（旧策略与新策略）：而他的具体目标就是使用旧策略的经验教新的策略，同时防止新策略学偏

        PPO算法中的Actor(演员)-Critic(评论家)这两个角色又分别扮演了不同的角色
        其中Actor(演员)将会扮演“老玩家”和“新玩家两个角色”

        > 1. 其中老玩家将会收集样本
        > 2. 新玩家根据老玩家的样本进行修改自己的动作策略，但限于PPO的Clip机制（不能改太多）
        > 3. Critic(评论家)并不会生成直接的动作，而是评估“某个动作在某个决策下的得分是多少”（即价值函数或优势函数）
        >     * 例如老玩家在样本状态（S）下选择了动作（a），此时评论家将会计算“如果新玩家去选择做a这个动作会比平均水平的操作好多少？”（优势值），并通过计算出来的值告诉新玩家这个动作值不值得学，防止新玩家误入歧途
        > 4. <u>新玩家在学习完一次过后将会成为新的老玩家</u>，并开始重复上面的操作，不断迭代更新新的策略模型

        也就是说PPO算法就是由策略模型本体（actor）与评判工具（Critic）组成，

        类比：老玩家、新玩家和教练

        - **旧 Actor**：老玩家的操作记录（比如录像）；
        - **新 Actor**：新玩家照着录像模仿操作，但教练（PPO 的 clip）不让它乱改；
        - **Critic**：教练看完录像后点评：“这个跳跃时机很好（优势高），那个翻滚没必要（优势低）”，让新玩家重点学 “高分操作”。





# 奖励函数

奖励函数是一个函数，会生成一个标量，代表处于某个特定状态并采取响应特定动作的代理的“优度”

![	](E:\Markdown\markdown\强化学习.assets\v2-97029e5275ec361eee1cbe12167532e7_1440w-1750024921674-3.jpg)



## 势能函数

势能函数其实就是一个特殊的奖励函数，这个函数表达的含义就是，当离目标点越近，得到的奖励值就越大，即势能越大

这个势能与电压一样都是位置的因素，例如在高处就电压高（势能高），接近地电压就低（势能低），而一般的物理势能都是指引物体从势能高的地方向势能低的地方移动，通过势能差生成额外奖励，引导智能体向“势能增加”的方向移动（即靠近目标）

例如现在要让一个孩子做作业，孩子妈妈就说，你越靠近桌子，并做完一本作业，就有零花钱奖励，因此孩子越喜欢接近桌子并写作业，则得到的奖励值就越多

# 概率分布

概率分布一句话来说就是描述随机变量的所有的可能取值与对应出现概率的数学函数，他揭示了时间发生的统计规律

因此**分布**其实能够理解成一个函数，他表示所有情况的规律



## 高斯分布

高斯分布统称**正态分布**，他的公式为：
$$
f = e^{-x^2}
$$
更复杂的表达公式为：
$$
f = e^{-\frac{1}{2}(x/\sigma)^2}
$$
其中 $\sigma$ 表示分布的离散程度（即描述数据或者概率分布钟数值偏离中心值的程度指标，反映了数据的`分布性`与`波动性`

在强化学习中使用了高斯函数作为奖励函数，他的作用是量化智能体行为与动作的偏差，提供可以优化的梯度信号



# 离散与连续

现在有个信号函数$x（T）$，在某一时刻他的值为$x（nT）$，那么$x(T)$就是一个连续的信号，而$x(nT)$则是一个离散的信号，





# 问题

## 在强化学习中什么叫做模型，什么叫做算法

在强化学习中，模型与算法是两个不同的概念，模型主要定义了智能体是**学什么**的，模型依旧是policy，是大脑，而算法则是定义了智能体**怎么学**， 通过不同的算法，能决定模型如何从环境中学习，通过不同的算法，让模型能够完成不同的任务





## 四元数是什么，为什么能够使用四元数进行惯性坐标系的速度与基座坐标系的速度转换