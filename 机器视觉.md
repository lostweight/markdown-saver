# 视觉识别各个部分的作用

## 视觉识别部分，为什么会需要说 opencv 这些对图像进行初步的调整然后才输入到视觉模型呢？机器视觉到底是在做什么，怎么做，为什么这么做？

神经网络（你训练的视觉模型）**只擅长高层特征提取、分类 / 检测 / 分割**，它不懂成像硬件噪声、光照畸变、镜头变形、像素格式、尺寸匹配等底层图像物理问题；

OpenCV 承担**图像预处理管线**，把杂乱原始摄像头画面，标准化、清洗、对齐成模型能稳定吃进去的规范输入，大幅降低模型训练难度、提升实物场景鲁棒性。

分开讲三层：机器视觉整体在干什么 → 完整流水线怎么做 → 每一步为什么必须这么做。



二、机器视觉到底是在做什么？

机器人场景下机器视觉完整定位：

通过摄像头采集真实世界二维像素图像，逆向解算出**三维空间信息**，给上层运动规划提供目标位置、姿态、类别、障碍，让机器人知道 “眼前有什么、东西在哪、该怎么抓、哪里不能碰”。

整套分为两大模块，分工完全割裂：

1. **底层图像处理（OpenCV 负责）**：硬件原始图像清洗、校正、变换、裁剪、缩放、色彩转换（无 AI，纯信号处理）
2. **深度视觉模型（CNN/Transformer 等你训练的网络）**：目标检测、实例分割、特征匹配、深度估计（AI 高层语义）

二者是**前后串联关系**：原图 → OpenCV 预处理 → 标准化张量 → 视觉模型推理 → 输出目标坐标 → 手眼标定转换到机器人世界坐标 → 运动规划。



## 完整流水线：OpenCV 预处理到底做哪些操作？怎么做？

### 1. 相机原始图像格式转换

#### 怎么做

工业 / USB 摄像头输出常见格式：YUV420、NV12、BGR；

深度学习框架 (PyTorch/TensorRT) 要求标准 RGB、0~1 浮点归一化张量。

OpenCV 默认读取是 BGR，需要 `cvtColor(img, COLOR_BGR2RGB)`。

#### 为什么这么做

- 成像芯片 ISP 输出不是 RGB，直接丢进模型会色彩通道错乱，识别完全失效；
- 训练数据集全是 RGB，输入分布不匹配 = 域偏移，实物识别暴跌。

### 2. 镜头畸变校正（非常关键，实物机器人必做）

#### 怎么做

用棋盘格标定获取内参矩阵、畸变系数，OpenCV `undistort()` 矫正桶形 / 枕形畸变。

#### 为什么这么做

镜头物理结构导致边缘像素拉伸扭曲：图像里物体像素坐标是错的，后续算三维抓取坐标会出现巨大偏差，机械臂抓空。

仿真 Isaac Sim 镜头是理想无畸变，你训练模型时感受不到这个问题，一上实物必翻车。

### 3. 图像去噪（高斯模糊 / 中值滤波）

#### 怎么做

`GaussianBlur()`、`medianBlur()`，平滑画面颗粒噪点。

#### 为什么这么做

CMOS 暗光、低曝光会产生随机椒盐噪声、颗粒噪点；

噪点会产生大量无关高频像素特征，干扰 CNN 提取有效轮廓，小物体极易漏检、误检。

模型不会自动学会过滤随机噪声，靠预处理低成本消除干扰。

### 4. 亮度、对比度均衡（CLAHE 直方图均衡）

#### 怎么做

自适应直方图均衡，修复强光过曝、阴影死黑区域。

#### 为什么这么做

工厂 / 桌面光照不稳定：反光、阴影、单侧强光；

只靠模型很难学到全光照鲁棒特征，预处理统一亮度分布，训练集不用采集海量光照样本，降低训练成本。

### 5. 尺寸缩放、裁剪、Padding

#### 怎么做

`resize()` 把任意分辨率原图统一缩放到模型固定输入尺寸（如 640×640）。

#### 为什么这么做

神经网络输入张量尺寸固定；同时统一像素物理尺度，保证近处 / 远处物体像素大小分布和训练集一致。

不做缩放直接送模型：维度报错、特征尺度混乱，识别精度崩盘。

### 6. 像素归一化 / 标准化

#### 怎么做

像素 0~255 → 除以 255 映射到 0~1；或减去数据集均值、除以方差。

#### 为什么这么做

神经网络激活函数、梯度下降对输入数值范围极度敏感；

0~255 大数值会导致训练梯度爆炸、收敛极慢，标准化让模型训练稳定、收敛更快。

### 7. 掩码、轮廓、几何计算（模型推理后二次处理）

模型只输出像素级掩码 / 框，真正的空间计算还是靠 OpenCV：

- `findContours` 提取物体轮廓
- `minAreaRect` 计算物体旋转姿态
- `solvePnP` 结合标定板求解物体 6D 位姿

#### 为什么这么做

CNN 只能告诉你 “哪里是目标”，不会几何运算，无法输出机器人需要的三维坐标、旋转角度。





### 什么是计算机视觉（Computer Vision）？什么是机器视觉（Machine Vision）？

计算机视觉实际上就是强调的**理解世界**，

它的终点是看懂 “这是什么、场景是什么、发生了什么”，不绑定执行机构。

感知只是手段，核心是**抽象理解**。



而机器视觉还是强调的**感知世界**，将世界的一些东西转换为机器人坐标系内的信息，进而去控制机器人

## 